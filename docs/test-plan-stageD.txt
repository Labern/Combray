{
  "summary": "Exhaustive parallel test design for CombrayCore + dead-code/typo audit to drive the test-suite refactor",
  "agentCount": 10,
  "logs": [],
  "result": {
    "plans": [
      {
        "module": "CombrayCore",
        "newTests": [
          {
            "name": "testSaveBumpsUpdatedAtButPreservesCreatedAt",
            "intent": "save() must set updatedAt to ~now while leaving createdAt untouched (it only mutates updatedAt).",
            "asserts": "Save Letter(createdAt: Date(timeIntervalSince1970:0), updatedAt: same). Returned.updatedAt is recent (> 1970, within seconds of now); returned.createdAt == epoch 0. Re-fetch confirms persisted values.",
            "catchesTypo": false
          },
          {
            "name": "testSaveIsUpsertNotDuplicate",
            "intent": "Saving the same id twice updates the row, never inserts a second.",
            "asserts": "Save letter, mutate title to 'X', save again with same id. allLetters().count == 1; fetched.title == 'X'."
          },
          {
            "name": "testNextLetterNumberUsesMaxNotCount",
            "intent": "nextLetterNumber returns MAX(number)+1, not COUNT+1 — gaps must not collapse.",
            "asserts": "Save only Letter(number: 5). nextLetterNumber() == 6 (not 2).",
            "apiNotes": "Existing test saves 1,2 -> 3, which can't distinguish MAX from COUNT.",
            "catchesTypo": true
          },
          {
            "name": "testLetterReturnsNilForUnknownId",
            "intent": "letter(id:) returns nil for an id that was never saved.",
            "asserts": "a.letter(id: \"missing\") == nil on a fresh archive."
          },
          {
            "name": "testDeleteLetterCascadesPagesParticipantsAndSearchRow",
            "intent": "deleteLetter removes pages and letterPerson rows (ON DELETE CASCADE) AND the explicit FTS row.",
            "asserts": "Build letter with transcription 'fox', 2 pages, sender+recipient. deleteLetter. Then pages()==[], participants()==(nil,[]), search(\"fox\")==[], letter(id:)==nil.",
            "apiNotes": "Pins the explicit 'DELETE FROM letterSearch' since FTS is not covered by SQL cascade."
          },
          {
            "name": "testDeleteUnknownLetterIsNoOp",
            "intent": "Deleting a nonexistent id does not throw and leaves the archive unchanged.",
            "asserts": "Save one letter. deleteLetter(id:\"nope\") does not throw; allLetters().count == 1."
          },
          {
            "name": "testAllLettersNilYearSortsLastWithCreatedAtTieBreak",
            "intent": "allLetters ordering is 'dateYear DESC NULLS LAST, createdAt DESC' — nil years go last and ties break on createdAt DESC.",
            "asserts": "Save A(dateYear:1990, createdAt:t1), B(dateYear:1990, createdAt:t2>t1), C(dateYear:nil). allLetters().map(id) == [B, A, C].",
            "apiNotes": "createdAt is preserved by save() so it can control the tie-break.",
            "catchesTypo": true
          },
          {
            "name": "testAllLettersEmptyArchive",
            "intent": "allLetters on an empty archive is an empty array, not an error.",
            "asserts": "try a.allLetters() == []."
          },
          {
            "name": "testLettersForYearFiltersAndOrdersCreatedAtDesc",
            "intent": "letters(forYear:) returns only that year, ordered createdAt DESC.",
            "asserts": "Save two letters dateYear 1962 (createdAt t1<t2) and one dateYear 1970. letters(forYear:1962).map(id) == [t2Letter, t1Letter]; the 1970 letter is absent."
          },
          {
            "name": "testLettersForYearEmptyWhenNoMatch",
            "intent": "letters(forYear:) with no matching year is empty.",
            "asserts": "Save a 1962 letter. letters(forYear:1800) == []."
          },
          {
            "name": "testLettersForPersonOrdersByYearAscThenCreatedAtAsc",
            "intent": "letters(forPersonId:) orders ASC by dateYear then createdAt — the OPPOSITE of allLetters().",
            "asserts": "Person Alice on letter year 1970 and letter year 1962. letters(forPersonId: Alice.id).map(dateYear) == [1962, 1970].",
            "catchesTypo": false
          },
          {
            "name": "testLettersForPersonEmptyForUnknownPerson",
            "intent": "letters(forPersonId:) for an unknown person id is empty.",
            "asserts": "letters(forPersonId: \"nope\") == []."
          },
          {
            "name": "testLettersForPersonExcludesOtherPeoplesLetters",
            "intent": "Only letters linked to the given person are returned.",
            "asserts": "Letter1 has Alice; Letter2 has only Bob. letters(forPersonId: Alice.id) contains only Letter1.id."
          },
          {
            "name": "testYearsDistinctDescendingExcludingNil",
            "intent": "years() returns DISTINCT non-null years, newest-first — pin the ordering and distinctness the existing test hides by sorting.",
            "asserts": "Save letters with dateYear 1962,1962,1970,nil. years() == [1970, 1962] (DESC, deduped, nil excluded) WITHOUT calling .sorted().",
            "apiNotes": "Existing testPeopleAndYears asserts years().sorted(), which erases DESC order.",
            "catchesTypo": true
          },
          {
            "name": "testYearsEmptyArchive",
            "intent": "years() on an empty archive is empty.",
            "asserts": "try a.years() == []."
          },
          {
            "name": "testPagesReturnedInPageIndexOrder",
            "intent": "pages(forLetterId:) returns pages ordered by pageIndex regardless of insertion order.",
            "asserts": "setPages with pageIndex [2,0,1]. pages().map(pageIndex) == [0,1,2] (do not .sorted() the result).",
            "apiNotes": "Existing pagesRoundTrip sorts the result, hiding the ORDER BY."
          },
          {
            "name": "testSetPagesReplacesPreviousSet",
            "intent": "setPages deletes the prior page set before inserting (replace, not append).",
            "asserts": "setPages with 2 pages, then setPages with 1 page. pages().count == 1."
          },
          {
            "name": "testSetPagesEmptyClearsPages",
            "intent": "setPages([]) clears all pages for the letter.",
            "asserts": "setPages with 2 pages, then setPages([]) -> pages() == []."
          },
          {
            "name": "testPagesEmptyForUnknownLetter",
            "intent": "pages(forLetterId:) for an unknown letter is empty.",
            "asserts": "pages(forLetterId: \"nope\") == []."
          },
          {
            "name": "testFindOrCreatePersonTrimsWhitespace",
            "intent": "findOrCreatePerson trims leading/trailing whitespace and newlines from the name.",
            "asserts": "findOrCreatePerson(named: \"  Eleanor\\n\").displayName == \"Eleanor\"."
          },
          {
            "name": "testFindOrCreatePersonIsIdempotentAcrossWhitespace",
            "intent": "Names differing only by surrounding whitespace resolve to one person, not duplicates.",
            "asserts": "findOrCreatePerson(named:\"Alice\") then findOrCreatePerson(named:\" Alice \") return the same id; people().count == 1."
          },
          {
            "name": "testPeopleOrderedByDisplayNameAscending",
            "intent": "people() is ordered by displayName ascending.",
            "asserts": "Create Carol, Alice, Bob (in that order). people().map(displayName) == [\"Alice\",\"Bob\",\"Carol\"].",
            "apiNotes": "Existing test wraps people() in a Set, erasing order."
          },
          {
            "name": "testPeopleEmptyArchive",
            "intent": "people() on a fresh archive is empty.",
            "asserts": "try a.people() == []."
          },
          {
            "name": "testSetParticipantsReplacesPrevious",
            "intent": "setParticipants clears the letter's prior LetterPerson rows before inserting; the unlinked Person still exists.",
            "asserts": "setParticipants(sender:\"Alice\"...); then setParticipants(sender:\"Bob\", recipients:[]). participants().sender?.displayName == \"Bob\", recipients == []; people() still contains \"Alice\"."
          },
          {
            "name": "testSetParticipantsNilSenderEmptyRecipients",
            "intent": "A nil sender and empty recipients produces no participant rows.",
            "asserts": "setParticipants(letterId:l, sender:nil, recipients:[]). participants() == (nil, [])."
          },
          {
            "name": "testParticipantsRecipientsOrderedByDisplayName",
            "intent": "participants() returns recipients ordered by displayName.",
            "asserts": "setParticipants(recipients:[\"Zoe\",\"Anna\",\"Mary\"]). participants().recipients.map(displayName) == [\"Anna\",\"Mary\",\"Zoe\"].",
            "catchesTypo": false
          },
          {
            "name": "testSetParticipantsRefreshesSearchIndexBothWays",
            "intent": "Changing participants re-indexes names: the old name becomes unfindable and the new name findable.",
            "asserts": "setParticipants(sender:\"Alpha\"); search(\"Alpha\") finds it. setParticipants(sender:\"Beta\"); search(\"Alpha\")==[] and search(\"Beta\") finds it."
          },
          {
            "name": "testSamePersonAsSenderAndRecipientCreatesTwoRoles",
            "intent": "One person can be both sender and recipient (PK includes role) without creating a duplicate person.",
            "asserts": "setParticipants(sender:\"Pat\", recipients:[\"Pat\"]). people().count == 1; participants().sender?.displayName == \"Pat\"; recipients.map(displayName) == [\"Pat\"]."
          },
          {
            "name": "testDuplicateRecipientNamesThrow",
            "intent": "Document that duplicate recipient names violate the (letterId,personId,role) primary key and currently throw.",
            "asserts": "XCTAssertThrowsError(try a.setParticipants(letterId:l, sender:nil, recipients:[\"Bob\",\"Bob\"])).",
            "apiNotes": "Likely a latent limitation: applyTranscription passes r.recipients.compactMap(clean) unde-duplicated, so a model returning duplicate recipients would make applyTranscription throw. Flag for maintainers.",
            "catchesTypo": false
          },
          {
            "name": "testParticipantsEmptyForLetterWithNone",
            "intent": "participants() for a letter with no participants is (nil, []).",
            "asserts": "Fresh letter -> participants() == (nil, [])."
          },
          {
            "name": "testSearchFindsByTitle",
            "intent": "Search matches on the indexed title column.",
            "asserts": "Letter(title:\"Venice\", transcription:\"x\"). search(\"Venice\").first?.letterId == l.id."
          },
          {
            "name": "testSearchFindsBySummary",
            "intent": "Search matches on summary (refreshSearchIndex folds summary into the body column).",
            "asserts": "Letter(transcription:\"x\", summary:\"rendezvous\"). search(\"rendezvous\").first?.letterId == l.id.",
            "apiNotes": "body = [transcription, summary].joined(\\n)."
          },
          {
            "name": "testSearchIsCaseInsensitive",
            "intent": "unicode61 tokenizer makes search case-insensitive.",
            "asserts": "Letter(transcription:\"Fox\"). Both search(\"fox\") and search(\"FOX\") return the letter."
          },
          {
            "name": "testSearchImplicitAndAcrossTokens",
            "intent": "Multi-token queries AND together — a letter must contain every token.",
            "asserts": "L1 transcription \"quick brown fox\", L2 \"quick\". search(\"quick fox\") returns only L1."
          },
          {
            "name": "testSearchPrefixMatchesOnlyLastToken",
            "intent": "Only the final token is prefix-matched; earlier tokens are exact.",
            "asserts": "Letter transcription \"quick brown\". search(\"brow\") finds it (last-token prefix). search(\"quic brown\") returns [] (first token 'quic' is exact and not a token)."
          },
          {
            "name": "testSearchResultsOrderedByRankAscending",
            "intent": "Hits are returned ordered by BM25 rank ascending (best first).",
            "asserts": "Two letters both containing 'love'. The returned hits.map(rank) is non-decreasing (hits == hits.sorted{ $0.rank < $1.rank }).",
            "apiNotes": "Assert the ordering property, not an absolute BM25 value."
          },
          {
            "name": "testSearchSnippetWrapsMatchInGuillemets",
            "intent": "Snippets wrap the matched term with the « » markers and use them, not <b>.",
            "asserts": "Letter transcription contains 'rendezvous'. search(\"rendezvous\").first?.snippet contains \"«\" and \"»\".",
            "catchesTypo": true
          },
          {
            "name": "testSearchEmptyWhitespacePunctuationReturnEmpty",
            "intent": "Queries with no alphanumeric tokens yield [] (ftsPattern returns nil).",
            "asserts": "search(\"\"), search(\"   \"), search(\"!!! ---\") each == []."
          },
          {
            "name": "testSearchHandlesQuoteCharactersWithoutThrowing",
            "intent": "A query containing double quotes does not throw and still matches (quotes are token separators).",
            "asserts": "Letter transcription \"she said hi\". search(\"say \\\"hi\\\"\") does not throw and returns the letter."
          },
          {
            "name": "testSearchReflectsReSaveOfTranscription",
            "intent": "Re-saving a letter refreshes its FTS row.",
            "asserts": "Save letter transcription \"alpha\"; search(\"alpha\") finds it. Update transcription to \"beta\", save. search(\"alpha\")==[] and search(\"beta\") finds it."
          },
          {
            "name": "testFtsPatternSingleTokenGetsPrefixStar",
            "intent": "Pin ftsPattern output for one token.",
            "asserts": "Archive.ftsPattern(from:\"fox\") == \"\\\"fox\\\"*\".",
            "catchesTypo": true
          },
          {
            "name": "testFtsPatternMultiTokenOnlyLastPrefixed",
            "intent": "Pin ftsPattern: each token quoted, AND-joined by spaces, only the last prefix-starred.",
            "asserts": "Archive.ftsPattern(from:\"quick brown\") == \"\\\"quick\\\" \\\"brown\\\"*\".",
            "catchesTypo": true
          },
          {
            "name": "testFtsPatternSplitsOnNonAlphanumerics",
            "intent": "Tokenization splits on any non-letter/non-number.",
            "asserts": "Archive.ftsPattern(from:\"a-b, c\") == \"\\\"a\\\" \\\"b\\\" \\\"c\\\"*\"."
          },
          {
            "name": "testFtsPatternEmptyOrPunctuationReturnsNil",
            "intent": "No tokens -> nil.",
            "asserts": "Archive.ftsPattern(from:\"\") == nil; (from:\"   \") == nil; (from:\"!!!@@@\") == nil."
          },
          {
            "name": "testFtsPatternTreatsQuotesAsSeparators",
            "intent": "Embedded double quotes are token separators, so the quote-doubling escape branch is effectively unreachable.",
            "asserts": "Archive.ftsPattern(from:\"say \\\"hi\\\"\") == \"\\\"say\\\" \\\"hi\\\"*\".",
            "apiNotes": "Documents that the replacingOccurrences(of:\\\"\\\") line is defensive/dead given the tokenizer."
          },
          {
            "name": "testFtsPatternAllowsDigitsAndUnicodeLetters",
            "intent": "Digits and accented letters are valid token characters.",
            "asserts": "Archive.ftsPattern(from:\"café 1962\") == \"\\\"café\\\" \\\"1962\\\"*\"."
          },
          {
            "name": "testCleanTrimsEdgesAndNilsEmpty",
            "intent": "Archive.clean trims only surrounding whitespace/newlines and returns nil for empty/whitespace/nil.",
            "asserts": "clean(\"  x \")==\"x\"; clean(\"\")==nil; clean(\"   \")==nil; clean(nil)==nil; clean(\"a\\nb\")==\"a\\nb\" (interior newline preserved)."
          },
          {
            "name": "testComposedTitleRecipientsOnly",
            "intent": "With recipients but no sender, the title is 'Kind to <recipients>'.",
            "asserts": "Archive.composedTitle(documentType:\"letter\", sender:\"\", recipients:[\"Bob\"], summary:\"\") == \"Letter to Bob\"."
          },
          {
            "name": "testComposedTitleMultipleRecipientsJoinedWithComma",
            "intent": "Multiple recipients join with ', ' and the order is 'to <recipients> from <sender>'.",
            "asserts": "composedTitle(documentType:\"letter\", sender:\"Alice\", recipients:[\"Bob\",\"Carol\"], summary:\"\") == \"Letter to Bob, Carol from Alice\".",
            "catchesTypo": true
          },
          {
            "name": "testComposedTitleCorrespondenceNoPartiesUsesSummaryTruncatedTo8Words",
            "intent": "Correspondence type with no sender/recipients falls back to 'Kind — <first 8 words of summary>'.",
            "asserts": "composedTitle(documentType:\"letter\", sender:\"\", recipients:[], summary:\"one two three four five six seven eight nine\") == \"Letter — one two three four five six seven eight\".",
            "catchesTypo": true
          },
          {
            "name": "testComposedTitleCorrespondenceNoPartiesNoSummaryReturnsKind",
            "intent": "Correspondence with nothing to say returns just the kind.",
            "asserts": "composedTitle(documentType:\"letter\", sender:\"\", recipients:[], summary:\"\") == \"Letter\"."
          },
          {
            "name": "testComposedTitleNonCorrespondenceSummaryTruncatedTo8Words",
            "intent": "Non-correspondence types also cap the summary at 8 words.",
            "asserts": "composedTitle(documentType:\"list\", sender:\"\", recipients:[], summary:\"a b c d e f g h i j\") == \"List — a b c d e f g h\".",
            "catchesTypo": true
          },
          {
            "name": "testComposedTitleCapitalizesEachWordOfType",
            "intent": "document_type is .capitalized (every word), affecting both kind label and correspondence membership.",
            "asserts": "composedTitle(documentType:\"diary entry\", sender:\"\", recipients:[], summary:\"the garden\") == \"Diary Entry — the garden\".",
            "catchesTypo": true
          },
          {
            "name": "testComposedTitleCardNoteTelegramInvitationAreCorrespondence",
            "intent": "Pin the correspondence whitelist: card, note, telegram, invitation use to/from framing.",
            "asserts": "composedTitle(documentType:\"invitation\", sender:\"Alice\", recipients:[\"Bob\"], summary:\"\") == \"Invitation to Bob from Alice\"; same to/from shape for documentType \"card\",\"note\",\"telegram\".",
            "catchesTypo": true
          },
          {
            "name": "testComposedTitleWhitespaceTypeDefaultsToLetter",
            "intent": "A blank/whitespace document_type defaults to 'Letter'.",
            "asserts": "composedTitle(documentType:\"   \", sender:\"Alice\", recipients:[], summary:\"\") == \"Letter from Alice\"."
          },
          {
            "name": "testComposedTitleCleansSenderAndRecipientsInternally",
            "intent": "composedTitle applies clean() to sender and recipients, so whitespace-only parties drop out.",
            "asserts": "composedTitle(documentType:\"letter\", sender:\"  \", recipients:[\"\",\" \"], summary:\"hi\") == \"Letter — hi\"."
          },
          {
            "name": "testComposedTitleNonCorrespondenceWithPartiesNoSummaryReturnsNil",
            "intent": "A non-correspondence document with parties but no summary returns nil (so callers fall through).",
            "asserts": "composedTitle(documentType:\"receipt\", sender:\"Alice\", recipients:[], summary:\"\") == nil."
          },
          {
            "name": "testYearBoundaries",
            "intent": "year(from:) accepts only 4-digit values in 1000...9999.",
            "asserts": "Archive.year(from:\"0999\")==nil; year(from:\"1000\")==1000; year(from:\"9999\")==9999.",
            "catchesTypo": true
          },
          {
            "name": "testYearTruncatesToFirstFourDigits",
            "intent": "year(from:) takes prefix(4) — a 5-digit number truncates rather than failing.",
            "asserts": "Archive.year(from:\"19620\")==1962; year(from:\"1962-03\")==1962."
          },
          {
            "name": "testYearRejectsLeadingSpaceAndNonDigits",
            "intent": "year(from:) does not trim and requires the first 4 chars to be an integer.",
            "asserts": "Archive.year(from:\" 1962\")==nil; year(from:\"12ab\")==nil; year(from:\"abcd\")==nil."
          },
          {
            "name": "testApplyTranscriptionThrowsForUnknownLetter",
            "intent": "applyTranscription throws ArchiveError.letterNotFound when the letter id does not exist.",
            "asserts": "XCTAssertThrowsError(try a.applyTranscription(r, toLetterId:\"nope\")) and the thrown error is Archive.ArchiveError.letterNotFound.",
            "apiNotes": "Pins the only throwing path; ArchiveError is an internal enum reachable via @testable."
          },
          {
            "name": "testApplyPreservesExistingAiTranscriptionElseSeedsIt",
            "intent": "applyTranscription only sets aiTranscription when it is currently nil; otherwise the original model output is preserved.",
            "asserts": "Letter with aiTranscription:\"orig\" + apply r.transcription:\"new\" -> transcription==\"new\", aiTranscription==\"orig\". Separate letter with aiTranscription nil + apply -> aiTranscription==r.transcription."
          },
          {
            "name": "testApplyInvalidDateSourceFallsBackToUnknown",
            "intent": "An unrecognized date.source maps to .unknown.",
            "asserts": "apply r with date.source \"garbage\" -> letter.dateSource == .unknown.",
            "catchesTypo": true
          },
          {
            "name": "testApplyConfidenceValidAndInvalid",
            "intent": "date.confidence maps via Confidence(rawValue:) with NO fallback (invalid -> nil).",
            "asserts": "apply r with date.confidence \"medium\" -> dateConfidence == .medium; with \"garbage\" -> dateConfidence == nil."
          },
          {
            "name": "testApplyCleansMetaAndSummaryToNil",
            "intent": "Whitespace-only meta fields and summary clean to nil.",
            "asserts": "apply r with meta.location \"  \" and summary \"  \" -> metaLocation == nil and summary == nil."
          },
          {
            "name": "testApplyNotableQuotesJoinAndEmpty",
            "intent": "notable_quotes join with newline; an empty array becomes nil.",
            "asserts": "apply r with notable_quotes [\"a\",\"b\"] -> notableQuotes==\"a\\nb\". apply r with notable_quotes [] -> notableQuotes==nil."
          },
          {
            "name": "testApplyTitleFallsBackToExistingWhenComposedNil",
            "intent": "Title resolution is clean(r.title) ?? composedTitle ?? letter.title — pin the third fallback.",
            "asserts": "Existing letter.title=\"Keep\". apply r with title:\"\", document_type:\"receipt\", sender:\"Alice\", recipients:[], summary:\"\" (composedTitle==nil) -> updated.title == \"Keep\"."
          },
          {
            "name": "testApplyCleansParticipants",
            "intent": "Sender is cleaned (blank -> no sender) and recipients are compactMap(clean) (blank entries dropped).",
            "asserts": "apply r with sender:\"  \", recipients:[\"\",\"Bob\",\"  \"] -> participants().sender==nil and recipients.map(displayName)==[\"Bob\"]."
          },
          {
            "name": "testApplyOverwritesTranscriptionEvenToEmpty",
            "intent": "letter.transcription is assigned unconditionally from r.transcription, even when empty.",
            "asserts": "Letter transcription \"old\". apply r with transcription \"\" -> updated.transcription == \"\"."
          },
          {
            "name": "testApplyKeepsNonYearDateValueButNilYear",
            "intent": "A non-year date string is kept in dateValue but yields nil dateYear.",
            "asserts": "apply r with date.value \"spring\" -> dateValue==\"spring\", dateYear==nil."
          },
          {
            "name": "testTranscriptionDateDefaultSourceIsUnknown",
            "intent": "DateField default source is the literal \"unknown\" (not empty).",
            "asserts": "Decode {\"transcription\":\"t\"} -> date.source==\"unknown\", date.value==\"\", date.confidence==\"\".",
            "catchesTypo": true
          },
          {
            "name": "testTranscriptionDateAsBareStringDecodes",
            "intent": "A date given as a bare string (not an object) decodes into date.value via the string fallback branch.",
            "asserts": "Decode {\"date\":\"1962-03\"} -> date.value==\"1962-03\", date.source==\"unknown\".",
            "apiNotes": "Exercises the 'else if let s = decode(String) ' branch, currently untested."
          },
          {
            "name": "testTranscriptionDateObjectWrongTypesFallBackPerField",
            "intent": "Wrong-typed date fields fall back per-field, not whole-object.",
            "asserts": "Decode {\"date\":{\"value\":123}} -> date.value==\"\" (and source==\"unknown\")."
          },
          {
            "name": "testTranscriptionRecipientsWrongTypeFallsBackToEmpty",
            "intent": "A non-array recipients value decodes to [] rather than throwing.",
            "asserts": "Decode {\"recipients\":\"Bob\"} -> recipients == []."
          },
          {
            "name": "testTranscriptionMetaPartialAndExactKeys",
            "intent": "Meta decodes per-field and pins the snake_case keys relationship_state and writer_goals.",
            "asserts": "Decode {\"meta\":{\"relationship_state\":\"s\",\"writer_goals\":\"g\"}} -> relationship_state==\"s\", writer_goals==\"g\", location==\"\", relationship==\"\".",
            "catchesTypo": true
          },
          {
            "name": "testTranscriptionUncertainSpansDecodeAndPartial",
            "intent": "UncertainSpan decodes text/reason with per-field defaults.",
            "asserts": "Decode {\"uncertain_spans\":[{\"text\":\"x\"},{\"text\":\"y\",\"reason\":\"r\"}]} -> [0].text==\"x\" & [0].reason==\"\"; [1].reason==\"r\".",
            "apiNotes": "Existing full-object test decodes uncertain_spans but never asserts its contents."
          },
          {
            "name": "testTranscriptionIgnoresUnknownKeys",
            "intent": "Unknown top-level keys are ignored on decode (lenient, no additionalProperties strictness).",
            "asserts": "Decode {\"transcription\":\"t\",\"bogus\":1,\"extra\":{}} -> transcription==\"t\" (no throw)."
          },
          {
            "name": "testTranscriptionEmptyObjectAllDefaults",
            "intent": "An empty object decodes to all defaults — even transcription is optional.",
            "asserts": "Decode {} -> transcription==\"\", title==\"\", document_type==\"\", sender==\"\", recipients==[], date.source==\"unknown\"."
          },
          {
            "name": "testTranscriptionArrayRootThrows",
            "intent": "A non-object JSON root cannot decode (keyed container fails).",
            "asserts": "XCTAssertThrowsError(JSONDecoder().decode(TranscriptionResult.self, from: Data(\"[]\".utf8)))."
          },
          {
            "name": "testSchemaTopLevelAdditionalPropertiesFalse",
            "intent": "Pin the regression guard: the schema sets additionalProperties:false at top level.",
            "asserts": "AnthropicClient.schema[\"additionalProperties\"] as? Bool == false.",
            "catchesTypo": true
          },
          {
            "name": "testSchemaRequiredIsExactlyElevenFields",
            "intent": "required must list all 11 output fields, including sender and recipients (which the existing test omits).",
            "asserts": "Set(schema[\"required\"] as! [String]) == [\"transcription\",\"title\",\"document_type\",\"summary\",\"sender\",\"recipients\",\"date\",\"people_mentioned\",\"notable_quotes\",\"uncertain_spans\",\"meta\"]; count == 11.",
            "catchesTypo": true
          },
          {
            "name": "testSchemaPropertiesKeysMatchRequired",
            "intent": "With additionalProperties:false, the properties key set must equal the required set.",
            "asserts": "Set((schema[\"properties\"] as! [String:Any]).keys) == Set(schema[\"required\"] as! [String])."
          },
          {
            "name": "testSchemaPropertyTypes",
            "intent": "Pin scalar/array property types.",
            "asserts": "props[\"transcription\"].type==\"string\"; props[\"recipients\"].type==\"array\" and its items.type==\"string\"; props[\"people_mentioned\"] and props[\"notable_quotes\"] are arrays of string."
          },
          {
            "name": "testSchemaDateSubschemaEnums",
            "intent": "Pin the date subschema: required keys, source enum (excludes 'manual'), confidence enum, additionalProperties:false.",
            "asserts": "date.required==[\"value\",\"source\",\"confidence\"]; date.properties.source.enum==[\"written\",\"inferred\",\"unknown\"]; date.properties.confidence.enum==[\"high\",\"medium\",\"low\"]; date.additionalProperties==false.",
            "catchesTypo": true
          },
          {
            "name": "testSchemaMetaSubschemaKeys",
            "intent": "Pin meta subschema required snake_case keys and additionalProperties:false.",
            "asserts": "meta.required==[\"location\",\"relationship\",\"relationship_state\",\"writer_goals\"]; meta.additionalProperties==false.",
            "catchesTypo": true
          },
          {
            "name": "testSchemaUncertainSpansItemsRequireTextAndReason",
            "intent": "Pin uncertain_spans item schema.",
            "asserts": "uncertain_spans.items.required==[\"text\",\"reason\"]; items.additionalProperties==false."
          },
          {
            "name": "testSchemaSourceEnumMatchesDateSourceMinusManual",
            "intent": "Cross-check the schema source enum against DateSource cases (must be exactly the non-manual cases).",
            "asserts": "Set(date.source.enum) == Set(DateSource.allCases.map{ $0.rawValue }).subtracting([\"manual\"]).",
            "catchesTypo": true
          },
          {
            "name": "testSchemaConfidenceEnumMatchesConfidenceCases",
            "intent": "Cross-check the schema confidence enum against the Confidence enum.",
            "asserts": "Set(date.confidence.enum) == Set(Confidence.allCases.map{ $0.rawValue }).",
            "catchesTypo": true
          },
          {
            "name": "testBackupTranscriptionTxtIsTitleThenBodyJoinedByBlankLine",
            "intent": "transcription.txt content is title + \\n\\n + transcription.",
            "asserts": "Write LetterFile(title:\"T\", transcription:\"Body\"). Read <dir>/<number>/transcription.txt == \"T\\n\\nBody\".",
            "catchesTypo": true
          },
          {
            "name": "testBackupTranscriptionTxtOmitsNilTitle",
            "intent": "A nil title is dropped (compactMap) so there is no leading blank line.",
            "asserts": "Write LetterFile(title:nil, transcription:\"Body\"). transcription.txt == \"Body\"."
          },
          {
            "name": "testBackupJsonDoesNotEscapeSlashes",
            "intent": "letter.json uses .withoutEscapingSlashes.",
            "asserts": "Write LetterFile(summary:\"a/b/c\"). Raw letter.json string contains \"a/b/c\" and not \"a\\/b\\/c\".",
            "catchesTypo": true
          },
          {
            "name": "testBackupJsonEncodesDatesAsISO8601",
            "intent": "letter.json encodes dates as ISO-8601.",
            "asserts": "Write LetterFile(createdAt: Date(timeIntervalSince1970:0)). Raw letter.json contains \"1970-01-01T00:00:00Z\".",
            "catchesTypo": true
          },
          {
            "name": "testBackupJsonKeysSorted",
            "intent": "letter.json uses .sortedKeys (alphabetical).",
            "asserts": "In the raw json string, range(of:\"\\\"createdAt\\\"\").lowerBound < range(of:\"\\\"id\\\"\").lowerBound < range(of:\"\\\"number\\\"\").lowerBound."
          },
          {
            "name": "testScanIgnoresFoldersWithoutLetterJson",
            "intent": "scan compactMaps away subdirectories with no letter.json.",
            "asserts": "Create one valid letter folder via Backup.write plus an empty sibling folder. Backup.scan(lettersDir:dir).count == 1."
          },
          {
            "name": "testScanIgnoresCorruptJson",
            "intent": "scan skips folders whose letter.json fails to decode.",
            "asserts": "Write a valid letter, then create another folder with letter.json = \"not json{\". scan(...).count == 1 (only the valid one)."
          },
          {
            "name": "testScanReturnsEmptyForMissingDirectory",
            "intent": "scan returns [] (not throw) when the directory does not exist.",
            "asserts": "Backup.scan(lettersDir: tempDir().appendingPathComponent(\"nope\")) == []."
          },
          {
            "name": "testBackupFullFidelityRoundTrip",
            "intent": "Every LetterFile field survives write -> scan unchanged.",
            "asserts": "Write a fully-populated LetterFile (title, date.value/source/confidence, from, to:[2], summary, all meta fields, transcription, aiTranscription, pages:[2], notableQuotes:[2], pinned:true, createdAt/updatedAt). scan -> assert each field equals the original (createdAt/updatedAt compared at second granularity)."
          },
          {
            "name": "testLetterFilePinnedFalseDecodesAsFalseNotNil",
            "intent": "An explicit pinned:false decodes to Optional(false), distinct from absent (nil).",
            "asserts": "Decode a LetterFile json with \"pinned\":false -> file.pinned == false; absent -> nil.",
            "apiNotes": "Existing tests cover absent->nil and true; false-explicit is the gap. Use iso8601 dateDecodingStrategy."
          },
          {
            "name": "testBackupFileNilForUnknownLetter",
            "intent": "backupFile returns nil when the letter id is unknown.",
            "asserts": "a.backupFile(forLetterId:\"nope\") == nil."
          },
          {
            "name": "testBackupFilePageNamesAreLastPathComponents",
            "intent": "backupFile reduces each page imagePath to its lastPathComponent.",
            "asserts": "Letter with pages imagePath \"Letters/3/letter_3_page_1.jpg\",\"Letters/3/letter_3_page_2.jpg\". backupFile.pages == [\"letter_3_page_1.jpg\",\"letter_3_page_2.jpg\"]."
          },
          {
            "name": "testBackupFileMapsDateAndConfidenceRawValues",
            "intent": "backupFile writes dateSource.rawValue and dateConfidence?.rawValue.",
            "asserts": "Letter(dateSource:.inferred, dateConfidence:.low) -> backupFile.date.source==\"inferred\", date.confidence==\"low\"."
          },
          {
            "name": "testBackupFileUnpinnedWritesNilNotFalse",
            "intent": "An unpinned letter serializes pinned as nil (pinned ? true : nil), so old readers see it absent.",
            "asserts": "Letter(pinned:false) -> backupFile.pinned == nil. Letter(pinned:true) -> backupFile.pinned == true.",
            "catchesTypo": true
          },
          {
            "name": "testBackupFileNotableQuotesSplitToArrayAndNil",
            "intent": "backupFile splits notableQuotes on newline into an array; nil stays nil.",
            "asserts": "Letter(notableQuotes:\"a\\nb\") -> backupFile.notableQuotes==[\"a\",\"b\"]. Letter(notableQuotes:nil) -> backupFile.notableQuotes==nil."
          },
          {
            "name": "testBackupFileFromToFromParticipants",
            "intent": "backupFile.from/to are sourced from participants (recipients ordered by displayName).",
            "asserts": "Letter with sender Alice, recipients Carol,Bob -> backupFile.from==\"Alice\", backupFile.to==[\"Bob\",\"Carol\"]."
          },
          {
            "name": "testWriteBackupForUnknownLetterIsNoOp",
            "intent": "writeBackup for a missing letter writes nothing and does not throw.",
            "asserts": "writeBackup(forLetterId:\"nope\", lettersDir:dir) does not throw; dir has no contents."
          },
          {
            "name": "testWriteBackupThenScanReproducesLetter",
            "intent": "End-to-end: a saved letter written to disk is recoverable by scan with matching identity.",
            "asserts": "Save letter + participants + pages; writeBackup(forLetterId:l.id, lettersDir:dir); Backup.scan(dir).first matches id, title, from, to."
          },
          {
            "name": "testImportFromFilesSkipsAlreadyIndexed",
            "intent": "importFromFiles leaves already-indexed letters untouched (skip on existing id).",
            "asserts": "Pre-save Letter(id:\"x\", title:\"Original\"). importFromFiles([LetterFile(id:\"x\", title:\"Changed\", ...)]). letter(id:\"x\").title == \"Original\"."
          },
          {
            "name": "testImportFromFilesBuildsPagePathsAndIndices",
            "intent": "importFromFiles constructs page rows with imagePath \"Letters/<number>/<name>\" and 0-based pageIndex from enumeration.",
            "asserts": "Import LetterFile(number:4, pages:[\"a.jpg\",\"b.jpg\"]). pages(forLetterId:) -> imagePaths [\"Letters/4/a.jpg\",\"Letters/4/b.jpg\"] with pageIndex 0 and 1."
          },
          {
            "name": "testImportFromFilesPinnedDefaultsFalseWhenAbsent",
            "intent": "importFromFiles maps file.pinned ?? false.",
            "asserts": "Import LetterFile(pinned:nil) -> letter.pinned == false. Import LetterFile(pinned:true) -> letter.pinned == true."
          },
          {
            "name": "testImportFromFilesDerivesYearAndLenientEnums",
            "intent": "importFromFiles derives dateYear from date.value and falls back leniently on enums.",
            "asserts": "Import LetterFile(date:.init(value:\"1962-03\", source:\"garbage\", confidence:\"nope\")) -> letter.dateYear==1962, dateSource==.unknown, dateConfidence==nil."
          },
          {
            "name": "testImportFromFilesNilDateValueNoCrash",
            "intent": "A nil date.value is coalesced to \"\" before year parsing.",
            "asserts": "Import LetterFile(date:.init(value:nil, source:\"unknown\", confidence:nil)) does not throw; letter.dateYear == nil."
          },
          {
            "name": "testImportFromFilesMakesLetterSearchable",
            "intent": "Imported letters are indexed for search (save + setParticipants both refresh).",
            "asserts": "Import LetterFile(from:\"Solène\", transcription:\"moonlight\"). search(\"Solène\") and search(\"moonlight\") both find the imported letter."
          },
          {
            "name": "testImportFromFilesNotableQuotesEmptyVsPresent",
            "intent": "importFromFiles maps notableQuotes: empty/nil -> nil, non-empty -> newline-joined.",
            "asserts": "Import notableQuotes [] -> letter.notableQuotes==nil. Import notableQuotes [\"a\",\"b\"] -> \"a\\nb\"."
          },
          {
            "name": "testCorrespondenceReturnsThreadOldestFirst",
            "intent": "correspondence returns all letters between the letter's two principals, ordered dateYear ASC then createdAt ASC, excluding unrelated letters.",
            "asserts": "Three Alice<->Bob letters (years 1965,1960,1970) + one unrelated Carol->Dan letter. correspondence(forLetterId: any A-B letter).map(dateYear) == [1960,1965,1970]; the unrelated letter is absent."
          },
          {
            "name": "testCorrespondenceUsesFirstRecipientAsSecondPrincipal",
            "intent": "Only recipients.first is used as principal b.",
            "asserts": "LetterX Alice->[Bob,Carol]; LetterY Alice->Bob; LetterZ Alice->Carol. correspondence(forLetterId:X) contains X and Y but not Z.",
            "apiNotes": "Pins the recipients.first behavior (Carol is ignored as a principal)."
          },
          {
            "name": "testCorrespondenceFallsBackToSingleLetterWhenMissingPrincipals",
            "intent": "With no sender or no recipient, correspondence returns just that one letter.",
            "asserts": "Letter with sender only (no recipients) -> correspondence == [that letter]. Letter with no participants -> correspondence == [that letter]."
          },
          {
            "name": "testCorrespondenceUnknownLetterReturnsEmpty",
            "intent": "correspondence for an unknown id returns [].",
            "asserts": "correspondence(forLetterId:\"nope\") == []."
          },
          {
            "name": "testCorrespondentsDistinctExcludingSelfOrderedByName",
            "intent": "correspondents returns DISTINCT people sharing a letter, excluding the person, ordered by displayName.",
            "asserts": "Alice appears with Bob on two letters and with Carol on one. correspondents(ofPersonId:Alice.id).map(displayName) == [\"Bob\",\"Carol\"] (Bob once, Alice absent)."
          },
          {
            "name": "testCorrespondentsEmptyForUnknownOrLonePerson",
            "intent": "correspondents is empty for an unknown person or one with no co-participants.",
            "asserts": "correspondents(ofPersonId:\"nope\") == []; a person who is the sole participant on a letter -> correspondents == []."
          },
          {
            "name": "testImageStoreDatabaseURLFilename",
            "intent": "Pin the sqlite filename.",
            "asserts": "ImageStore(root: tempDir()).databaseURL.lastPathComponent == \"combray.sqlite\".",
            "catchesTypo": true
          },
          {
            "name": "testImageStoreUrlForPageJoinsRootAndImagePath",
            "intent": "url(for:) appends the page's relative imagePath to root.",
            "asserts": "store.url(for: Page(letterId:\"L\", pageIndex:0, imagePath:\"Letters/2/x.jpg\")).path == store.root.appendingPathComponent(\"Letters/2/x.jpg\").path."
          },
          {
            "name": "testImportImageDefaultsExtensionToJpgWhenNone",
            "intent": "A source file with no extension is named .jpg.",
            "asserts": "Write bytes to <tmp>/scan (no extension). importImage(...index:0).imagePath ends with \"letter_7_page_1.jpg\".",
            "catchesTypo": true
          },
          {
            "name": "testImportImageLowercasesAndPreservesNonJpgExtension",
            "intent": "The extension is lowercased and preserved for non-jpg inputs.",
            "asserts": "Source \"scan.PNG\". importImage(...index:0).imagePath == \"Letters/7/letter_7_page_1.png\"."
          },
          {
            "name": "testImportImagePageNumberIsIndexPlusOne",
            "intent": "Page filenames are 1-based (index+1).",
            "asserts": "importImage(index:0).imagePath ends \"page_1.<ext>\"; importImage(index:2).imagePath ends \"page_3.<ext>\".",
            "catchesTypo": true
          },
          {
            "name": "testImportImageOverwritesExistingDestination",
            "intent": "Importing to an existing destination removes and replaces it (no throw, single file, new bytes).",
            "asserts": "importImage twice for the same letterNumber/index with bytes \"A\" then \"B\". The stored file's bytes == \"B\"; no error thrown."
          },
          {
            "name": "testDefaultRootLastComponentIsCombray",
            "intent": "Pin that the default root folder is named 'Combray'.",
            "asserts": "ImageStore.defaultRoot().lastPathComponent == \"Combray\".",
            "catchesTypo": true
          },
          {
            "name": "testStoredCredentialExpiresAMinuteEarly",
            "intent": "isExpired treats a token as expired 60s before its real expiry.",
            "asserts": "StoredCredential(kind:.oauth, accessToken:\"a\", expiresAt: now+30s).isExpired == true; expiresAt: now+120s -> isExpired == false.",
            "catchesTypo": true
          },
          {
            "name": "testStoredCredentialNilExpiryNeverExpires",
            "intent": "A credential with no expiry is never expired.",
            "asserts": "StoredCredential(kind:.oauth, accessToken:\"a\", expiresAt:nil).isExpired == false."
          },
          {
            "name": "testStoredCredentialKindRawValues",
            "intent": "Pin StoredCredential.Kind rawValues and their JSON encoding.",
            "asserts": "Kind.apiKey.rawValue==\"apiKey\"; Kind.oauth.rawValue==\"oauth\"; encoding a .oauth credential produces JSON containing \"oauth\".",
            "catchesTypo": true
          },
          {
            "name": "testDateSourceRawValuesAndAllCases",
            "intent": "Pin DateSource cases, order, and rawValues (these are persisted in the DB and mirrored in the schema).",
            "asserts": "DateSource.allCases.map{ $0.rawValue } == [\"written\",\"inferred\",\"manual\",\"unknown\"].",
            "catchesTypo": true
          },
          {
            "name": "testConfidenceRawValuesAndAllCases",
            "intent": "Pin Confidence cases and rawValues.",
            "asserts": "Confidence.allCases.map{ $0.rawValue } == [\"high\",\"medium\",\"low\"].",
            "catchesTypo": true
          },
          {
            "name": "testPersonRoleRawValues",
            "intent": "Pin PersonRole rawValues (used in SQL role filters and the letterPerson PK).",
            "asserts": "PersonRole.sender.rawValue==\"sender\"; PersonRole.recipient.rawValue==\"recipient\".",
            "catchesTypo": true
          },
          {
            "name": "testSchemaVersionIsExactlyOne",
            "intent": "Pin the on-disk schema version constant (existing test only checks > 0).",
            "asserts": "Combray.schemaVersion == 1.",
            "catchesTypo": true
          },
          {
            "name": "testAnthropicErrorDescriptions",
            "intent": "Pin each user-facing AnthropicError message exactly.",
            "asserts": "AnthropicError.missingAPIKey.errorDescription == \"No Anthropic API key set. Add one in Settings.\"; .http(404,\"x\").errorDescription == \"Anthropic API error 404: x\"; .noContent.errorDescription == \"The model returned no content.\"; .badImage(\"p.jpg\").errorDescription == \"Couldn't read image: p.jpg\".",
            "catchesTypo": true
          },
          {
            "name": "testExtractJSONPlainObjectUnchanged",
            "intent": "A bare JSON object is returned as-is.",
            "asserts": "AnthropicClient.extractJSON(\"{\\\"a\\\":1}\") == \"{\\\"a\\\":1}\"."
          },
          {
            "name": "testExtractJSONStripsJsonFence",
            "intent": "A ```json fenced block is unwrapped.",
            "asserts": "AnthropicClient.extractJSON(\"```json\\n{\\\"a\\\":1}\\n```\") == \"{\\\"a\\\":1}\"."
          },
          {
            "name": "testExtractJSONStripsBareFence",
            "intent": "A bare ``` fence is unwrapped.",
            "asserts": "AnthropicClient.extractJSON(\"```\\n{\\\"a\\\":1}\\n```\") == \"{\\\"a\\\":1}\"."
          },
          {
            "name": "testExtractJSONFromSurroundingProse",
            "intent": "JSON embedded in prose is extracted from first { to last }.",
            "asserts": "AnthropicClient.extractJSON(\"Sure: {\\\"a\\\":1} done\") == \"{\\\"a\\\":1}\"."
          },
          {
            "name": "testExtractJSONKeepsOutermostBracesWithNesting",
            "intent": "Nested objects keep the outermost closing brace (lastIndex of '}').",
            "asserts": "AnthropicClient.extractJSON(\"x {\\\"a\\\":{\\\"b\\\":1}} y\") == \"{\\\"a\\\":{\\\"b\\\":1}}\"."
          },
          {
            "name": "testExtractJSONNoBracesReturnsTrimmedInput",
            "intent": "With no braces the trimmed input is returned unchanged.",
            "asserts": "AnthropicClient.extractJSON(\"  no json  \") == \"no json\"."
          },
          {
            "name": "testClaudeAuthConstants",
            "intent": "Pin OAuth client id, console redirect, and scope strings.",
            "asserts": "ClaudeAuth.clientID == \"9d1c250a-e61b-44d9-88ed-5944d1962f5e\"; ClaudeAuth.consoleRedirect == \"https://console.anthropic.com/oauth/code/callback\"; ClaudeAuth.scope == \"org:create_api_key user:profile user:inference\".",
            "catchesTypo": true
          },
          {
            "name": "testClaudeAuthLoopbackRedirect",
            "intent": "Pin the loopback redirect template.",
            "asserts": "ClaudeAuth.loopbackRedirect(port: 8080) == \"http://localhost:8080/callback\".",
            "catchesTypo": true
          },
          {
            "name": "testClaudeAuthAuthorizeURLParams",
            "intent": "Pin every query parameter of the authorize URL, including code_challenge_method S256 and code=true.",
            "asserts": "Parse authorizeURL(pkce) via URLComponents: host+path == claude.ai/oauth/authorize; query items: code==\"true\", client_id==clientID, response_type==\"code\", redirect_uri==consoleRedirect, scope==scope, code_challenge==pkce.challenge, code_challenge_method==\"S256\", state==pkce.state.",
            "catchesTypo": true
          },
          {
            "name": "testClaudeAuthAuthorizeURLCustomRedirect",
            "intent": "A provided redirectURI overrides the console default.",
            "asserts": "authorizeURL(pkce, redirectURI: ClaudeAuth.loopbackRedirect(port:1234)) has query redirect_uri == \"http://localhost:1234/callback\"."
          },
          {
            "name": "testMakePKCEChallengeIsSha256OfVerifierBase64URL",
            "intent": "makePKCE produces a base64url-safe verifier/state and a challenge equal to base64url(SHA256(verifier)); calls are random.",
            "asserts": "pkce=makePKCE(); Data(SHA256.hash(data: Data(pkce.verifier.utf8))).base64URLEncodedString() == pkce.challenge; verifier/challenge/state contain none of '+','/','='; two makePKCE() verifiers differ.",
            "apiNotes": "Test file must add `import CryptoKit`.",
            "catchesTypo": true
          },
          {
            "name": "testDataBase64URLEncoding",
            "intent": "Pin the base64url transform: + -> -, / -> _, '=' stripped.",
            "asserts": "Data([0xFB,0xFF]).base64URLEncodedString() == \"-_8\" (standard base64 is \"+/8=\").",
            "apiNotes": "base64URLEncodedString() is an internal Data extension reachable via @testable.",
            "catchesTypo": true
          },
          {
            "name": "testRelationshipTablePersists",
            "intent": "Cover the otherwise-unexercised relationship table / Relationship record mapping.",
            "asserts": "Insert a Person, then a Relationship(personId: p.id, relation:\"grandmother\"); fetch it back and assert relation and personId round-trip.",
            "apiNotes": "Requires `import GRDB` to use database.dbWriter.write { db in ... }. Lower priority if the test target does not link GRDB directly."
          },
          {
            "name": "testLetterDefaultsPersistRoundTrip",
            "intent": "A default-constructed Letter persists its defaults (number 0, empty transcription, .unknown source, unpinned, nil title).",
            "asserts": "Save Letter(); fetched.number==0, transcription==\"\", dateSource==.unknown, pinned==false, title==nil."
          },
          {
            "name": "testAnthropicInstructionMentionsKeyFields",
            "intent": "Light regression guard that the system prompt still references the structured fields it must elicit (prompt drift is a real failure mode).",
            "asserts": "AnthropicClient.instruction contains \"document_type\", \"notable_quotes\", \"uncertain_spans\", and \"[illegible]\".",
            "apiNotes": "Intentionally a non-exhaustive contains-check, not a full-string pin."
          }
        ]
      },
      {
        "module": "CombrayCore (Sources/CombrayCore/AnthropicClient.swift)",
        "newTests": [
          {
            "name": "testDecodesEmptyObjectToAllDefaults",
            "intent": "An empty JSON object {} must decode (not throw) with every field at its documented default, including the non-obvious date.source default 'unknown'.",
            "asserts": "let r = decode(\"{}\"); XCTAssertEqual(r.transcription, \"\"); XCTAssertEqual(r.title, \"\"); XCTAssertEqual(r.document_type, \"\"); XCTAssertEqual(r.summary, \"\"); XCTAssertEqual(r.sender, \"\"); XCTAssertTrue(r.recipients.isEmpty); XCTAssertTrue(r.people_mentioned.isEmpty); XCTAssertTrue(r.notable_quotes.isEmpty); XCTAssertTrue(r.uncertain_spans.isEmpty); XCTAssertEqual(r.date.value, \"\"); XCTAssertEqual(r.date.source, \"unknown\"); XCTAssertEqual(r.date.confidence, \"\"); XCTAssertEqual(r.meta.location, \"\"); XCTAssertEqual(r.meta.relationship, \"\"); XCTAssertEqual(r.meta.relationship_state, \"\"); XCTAssertEqual(r.meta.writer_goals, \"\")",
            "apiNotes": "decode = JSONDecoder().decode(TranscriptionResult.self, from: Data(json.utf8)). Existing tests only cover full + transcription-only; neither pins {} nor date.source default.",
            "catchesTypo": true
          },
          {
            "name": "testDateDecodesFromPlainStringFallback",
            "intent": "Pins the else-branch (lines 71-73) where `date` is a bare JSON string rather than an object: value gets the string, source stays the default 'unknown', confidence stays empty.",
            "asserts": "let r = decode(\"{\\\"date\\\":\\\"1962-03-04\\\"}\"); XCTAssertEqual(r.date.value, \"1962-03-04\"); XCTAssertEqual(r.date.source, \"unknown\"); XCTAssertEqual(r.date.confidence, \"\")",
            "apiNotes": "Not covered by any existing test. Exercises the `else if let s = try? c.decode(String.self, forKey: .date)` path.",
            "catchesTypo": true
          },
          {
            "name": "testDateObjectMissingSourceDefaultsToUnknown",
            "intent": "DateField with only `value` present must default source to 'unknown' and confidence to '' — pins the constant in DateField.init(from:).",
            "asserts": "let r = decode(\"{\\\"date\\\":{\\\"value\\\":\\\"1962\\\"}}\"); XCTAssertEqual(r.date.value, \"1962\"); XCTAssertEqual(r.date.source, \"unknown\"); XCTAssertEqual(r.date.confidence, \"\")",
            "apiNotes": "Distinct from the string-fallback test: here date IS an object but partial.",
            "catchesTypo": true
          },
          {
            "name": "testDateFieldDefaultConstructorConstants",
            "intent": "Directly pins DateField()'s stored-property defaults so a typo in the literal 'unknown' (or accidental change of value/confidence default) fails without needing JSON.",
            "asserts": "let d = TranscriptionResult.DateField(); XCTAssertEqual(d.value, \"\"); XCTAssertEqual(d.source, \"unknown\"); XCTAssertEqual(d.confidence, \"\")",
            "apiNotes": "DateField has a public init(). Reachable via @testable. The string 'unknown' appears in two places (property default + decode fallback); this pins the property default specifically.",
            "catchesTypo": true
          },
          {
            "name": "testMetaDefaultConstructorConstants",
            "intent": "Pins Meta()'s four defaults to empty string.",
            "asserts": "let m = TranscriptionResult.Meta(); XCTAssertEqual(m.location, \"\"); XCTAssertEqual(m.relationship, \"\"); XCTAssertEqual(m.relationship_state, \"\"); XCTAssertEqual(m.writer_goals, \"\")",
            "apiNotes": "Meta has a public init().",
            "catchesTypo": true
          },
          {
            "name": "testUncertainSpanDefaultConstructorConstants",
            "intent": "Pins UncertainSpan()'s two defaults to empty string.",
            "asserts": "let u = TranscriptionResult.UncertainSpan(); XCTAssertEqual(u.text, \"\"); XCTAssertEqual(u.reason, \"\")",
            "apiNotes": "UncertainSpan has a public init().",
            "catchesTypo": true
          },
          {
            "name": "testUncertainSpansPartialElementDefaultsReason",
            "intent": "A span object with only `text` keeps reason at '' — and a list of spans decodes in order.",
            "asserts": "let r = decode(\"{\\\"uncertain_spans\\\":[{\\\"text\\\":\\\"a\\\",\\\"reason\\\":\\\"b\\\"},{\\\"text\\\":\\\"c\\\"}]}\"); XCTAssertEqual(r.uncertain_spans.count, 2); XCTAssertEqual(r.uncertain_spans[0].text, \"a\"); XCTAssertEqual(r.uncertain_spans[0].reason, \"b\"); XCTAssertEqual(r.uncertain_spans[1].text, \"c\"); XCTAssertEqual(r.uncertain_spans[1].reason, \"\")",
            "apiNotes": "Existing full-decode test has a single complete span; this adds the partial-element default and ordering.",
            "catchesTypo": false
          },
          {
            "name": "testMetaPartialFillsRemainingDefaults",
            "intent": "Meta with only `location` keeps the other three fields at ''.",
            "asserts": "let r = decode(\"{\\\"meta\\\":{\\\"location\\\":\\\"Paris\\\"}}\"); XCTAssertEqual(r.meta.location, \"Paris\"); XCTAssertEqual(r.meta.relationship, \"\"); XCTAssertEqual(r.meta.relationship_state, \"\"); XCTAssertEqual(r.meta.writer_goals, \"\")",
            "catchesTypo": false
          },
          {
            "name": "testTypeMismatchedScalarsFallBackToDefaults",
            "intent": "Because every field uses `try?`, a wrong JSON type must silently fall back to the default instead of throwing (lenient decode).",
            "asserts": "let r = decode(\"{\\\"title\\\":5,\\\"summary\\\":true,\\\"sender\\\":[1,2]}\"); XCTAssertEqual(r.title, \"\"); XCTAssertEqual(r.summary, \"\"); XCTAssertEqual(r.sender, \"\")",
            "apiNotes": "title=number, summary=bool, sender=array — all should decode-fail per-field and default to \"\".",
            "catchesTypo": false
          },
          {
            "name": "testArrayWithNonStringElementFallsBackToEmpty",
            "intent": "decode([String]) is all-or-nothing: one non-string element makes the whole array fall back to [] rather than dropping just the bad element.",
            "asserts": "let r = decode(\"{\\\"recipients\\\":[\\\"a\\\",5],\\\"people_mentioned\\\":[\\\"x\\\"]}\"); XCTAssertTrue(r.recipients.isEmpty); XCTAssertEqual(r.people_mentioned, [\"x\"])",
            "apiNotes": "Pins the documented leniency boundary — partial arrays are discarded, not salvaged.",
            "catchesTypo": false
          },
          {
            "name": "testUnknownKeysAreIgnoredOnDecode",
            "intent": "Decode must tolerate extra/unknown keys (no additionalProperties enforcement on the inbound side, unlike the request schema).",
            "asserts": "let r = decode(\"{\\\"transcription\\\":\\\"hi\\\",\\\"mystery\\\":123,\\\"extra\\\":{\\\"q\\\":1}}\"); XCTAssertEqual(r.transcription, \"hi\")",
            "catchesTypo": false
          },
          {
            "name": "testDecodeDoesNotValidateEnumValues",
            "intent": "Pins that date.source / date.confidence are free strings on decode — the enum restriction lives only in the request schema, so out-of-enum model output is preserved verbatim.",
            "asserts": "let r = decode(\"{\\\"date\\\":{\\\"value\\\":\\\"x\\\",\\\"source\\\":\\\"guessed\\\",\\\"confidence\\\":\\\"ultra\\\"}}\"); XCTAssertEqual(r.date.source, \"guessed\"); XCTAssertEqual(r.date.confidence, \"ultra\")",
            "apiNotes": "Documents the asymmetry between lenient decode and the strict output schema enums.",
            "catchesTypo": false
          },
          {
            "name": "testRecipientsOrderPreserved",
            "intent": "Array order is preserved exactly as received (no sorting/dedup on decode).",
            "asserts": "let r = decode(\"{\\\"recipients\\\":[\\\"Zeb\\\",\\\"Abe\\\",\\\"Mum\\\",\\\"Abe\\\"]}\"); XCTAssertEqual(r.recipients, [\"Zeb\",\"Abe\",\"Mum\",\"Abe\"])",
            "catchesTypo": false
          },
          {
            "name": "testHashableAndEquatableSynthesis",
            "intent": "TranscriptionResult is declared Hashable; two identical decodes must be == (and share hashValue), and a differing decode must be !=. Guards accidental loss of Hashable conformance / a field dropped from synthesis.",
            "asserts": "let j = \"{\\\"title\\\":\\\"t\\\",\\\"recipients\\\":[\\\"r\\\"]}\"; let a = decode(j); let b = decode(j); XCTAssertEqual(a, b); XCTAssertEqual(a.hashValue, b.hashValue); let c = decode(\"{\\\"title\\\":\\\"other\\\"}\"); XCTAssertNotEqual(a, c)",
            "catchesTypo": false
          },
          {
            "name": "testAnthropicErrorMissingAPIKeyDescription",
            "intent": "Pins the exact user-facing copy for the missing-key error.",
            "asserts": "XCTAssertEqual(AnthropicError.missingAPIKey.errorDescription, \"No Anthropic API key set. Add one in Settings.\")",
            "apiNotes": "AnthropicError is public LocalizedError; no existing test touches it.",
            "catchesTypo": true
          },
          {
            "name": "testAnthropicErrorNoContentDescription",
            "intent": "Pins the exact 'no content' copy.",
            "asserts": "XCTAssertEqual(AnthropicError.noContent.errorDescription, \"The model returned no content.\")",
            "catchesTypo": true
          },
          {
            "name": "testAnthropicErrorHTTPDescriptionInterpolation",
            "intent": "Pins the http error format string and its interpolation of code + body.",
            "asserts": "XCTAssertEqual(AnthropicError.http(503, \"server down\").errorDescription, \"Anthropic API error 503: server down\")",
            "catchesTypo": true
          },
          {
            "name": "testAnthropicErrorBadImageDescriptionInterpolation",
            "intent": "Pins the bad-image format string and its interpolation of the path argument.",
            "asserts": "XCTAssertEqual(AnthropicError.badImage(\"scan.JPG\").errorDescription, \"Couldn't read image: scan.JPG\")",
            "catchesTypo": true
          },
          {
            "name": "testDefaultModelConstant",
            "intent": "Pins the default model id so an accidental edit to the constructor default fails.",
            "asserts": "XCTAssertEqual(AnthropicClient().model, \"claude-opus-4-8\")",
            "apiNotes": "AnthropicClient() with no args. The default string lives on line 105.",
            "catchesTypo": true
          },
          {
            "name": "testInitOverridesModel",
            "intent": "An explicit model argument is stored on the struct.",
            "asserts": "XCTAssertEqual(AnthropicClient(model: \"claude-sonnet-4-5\").model, \"claude-sonnet-4-5\")",
            "catchesTypo": false
          },
          {
            "name": "testExtractJSONPlainObjectUnchanged",
            "intent": "Bare JSON with no fences/prose passes through unchanged.",
            "asserts": "XCTAssertEqual(AnthropicClient.extractJSON(\"{\\\"a\\\":1}\"), \"{\\\"a\\\":1}\")",
            "apiNotes": "extractJSON is internal static; reachable via @testable. Entirely untested today.",
            "catchesTypo": false
          },
          {
            "name": "testExtractJSONTrimsSurroundingWhitespace",
            "intent": "Leading/trailing whitespace and newlines are trimmed before extraction.",
            "asserts": "XCTAssertEqual(AnthropicClient.extractJSON(\"  \\n {\\\"a\\\":1} \\n \"), \"{\\\"a\\\":1}\")",
            "catchesTypo": false
          },
          {
            "name": "testExtractJSONStripsJSONLanguageFence",
            "intent": "A ```json fenced block is unwrapped to its inner JSON.",
            "asserts": "XCTAssertEqual(AnthropicClient.extractJSON(\"```json\\n{\\\"a\\\":1}\\n```\"), \"{\\\"a\\\":1}\")",
            "catchesTypo": false
          },
          {
            "name": "testExtractJSONStripsBareTripleBacktickFence",
            "intent": "A bare ``` fence (no language tag) is also unwrapped — the first line is dropped regardless of its contents.",
            "asserts": "XCTAssertEqual(AnthropicClient.extractJSON(\"```\\n{\\\"a\\\":1}\\n```\"), \"{\\\"a\\\":1}\")",
            "catchesTypo": false
          },
          {
            "name": "testExtractJSONPullsObjectFromSurroundingProse",
            "intent": "When the model wraps JSON in prose, the {...} span is extracted.",
            "asserts": "XCTAssertEqual(AnthropicClient.extractJSON(\"Here is the JSON: {\\\"a\\\":1} thanks!\"), \"{\\\"a\\\":1}\")",
            "catchesTypo": false
          },
          {
            "name": "testExtractJSONIsGreedyFirstBraceToLastBrace",
            "intent": "Pins the greedy first-'{'..last-'}' behavior: with two objects in the text it returns the whole span between, not just the first object.",
            "asserts": "XCTAssertEqual(AnthropicClient.extractJSON(\"x {\\\"a\\\":1} y {\\\"b\\\":2} z\"), \"{\\\"a\\\":1} y {\\\"b\\\":2}\")",
            "apiNotes": "Documents that extractJSON is not a balanced-brace parser; downstream JSON decode would then handle/reject this. Important behavioral pin.",
            "catchesTypo": false
          },
          {
            "name": "testExtractJSONNoBracesReturnsTrimmedInput",
            "intent": "With no braces at all, the trimmed input is returned verbatim (decode then fails downstream).",
            "asserts": "XCTAssertEqual(AnthropicClient.extractJSON(\"  no json here  \"), \"no json here\")",
            "catchesTypo": false
          },
          {
            "name": "testExtractJSONClosingBraceBeforeOpeningReturnsWhole",
            "intent": "Pins the `start < end` guard: when '}' precedes '{', extraction is skipped and the trimmed string is returned.",
            "asserts": "XCTAssertEqual(AnthropicClient.extractJSON(\"} {\"), \"} {\")",
            "catchesTypo": false
          },
          {
            "name": "testExtractJSONUnterminatedFenceStillExtracts",
            "intent": "An opening fence with no closing ``` still works: first line dropped, no backwards-fence found, braces extracted.",
            "asserts": "XCTAssertEqual(AnthropicClient.extractJSON(\"```json\\n{\\\"a\\\":1}\"), \"{\\\"a\\\":1}\")",
            "catchesTypo": false
          },
          {
            "name": "testExtractJSONDropsProseAfterClosingFence",
            "intent": "Text after the closing ``` is discarded because the closing fence is found via reverse search.",
            "asserts": "XCTAssertEqual(AnthropicClient.extractJSON(\"```json\\n{\\\"a\\\":1}\\n```\\nthanks\"), \"{\\\"a\\\":1}\")",
            "catchesTypo": false
          },
          {
            "name": "testExtractJSONNestedObjectPreserved",
            "intent": "Nested braces are preserved because extraction spans to the LAST '}'.",
            "asserts": "XCTAssertEqual(AnthropicClient.extractJSON(\"{\\\"a\\\":{\\\"b\\\":2}}\"), \"{\\\"a\\\":{\\\"b\\\":2}}\")",
            "catchesTypo": false
          },
          {
            "name": "testExtractJSONEmptyAndWhitespaceOnlyReturnEmpty",
            "intent": "Empty or whitespace-only input trims to '' and (no braces) returns ''.",
            "asserts": "XCTAssertEqual(AnthropicClient.extractJSON(\"\"), \"\"); XCTAssertEqual(AnthropicClient.extractJSON(\"   \\n\\t \"), \"\")",
            "catchesTypo": false
          },
          {
            "name": "testJpegDataThrowsBadImageForNonImageFile",
            "intent": "A readable-but-not-an-image file makes NSImage(contentsOf:) fail and jpegData throw AnthropicError.badImage carrying the file's lastPathComponent.",
            "asserts": "let url = tempDir().appendingPathComponent(\"scan.txt\"); try Data(\"not an image\".utf8).write(to: url); XCTAssertThrowsError(try AnthropicClient.jpegData(from: url)) { error in guard case let AnthropicError.badImage(p) = error else { return XCTFail(\"wrong error: \\(error)\") }; XCTAssertEqual(p, \"scan.txt\") }",
            "apiNotes": "jpegData is internal static. Use the existing tempDir() helper. Pins both the error case and that the payload is lastPathComponent (not the full path).",
            "catchesTypo": true
          },
          {
            "name": "testJpegDataThrowsBadImageForMissingFile",
            "intent": "A nonexistent URL also yields AnthropicError.badImage with the filename.",
            "asserts": "let url = tempDir().appendingPathComponent(\"missing.jpg\"); XCTAssertThrowsError(try AnthropicClient.jpegData(from: url)) { error in guard case let AnthropicError.badImage(p) = error else { return XCTFail() }; XCTAssertEqual(p, \"missing.jpg\") }",
            "catchesTypo": false
          },
          {
            "name": "testJpegDataReEncodesValidImageToJPEG",
            "intent": "The happy path: a real (PNG) image is re-encoded and returns non-empty JPEG bytes (SOI marker 0xFF 0xD8), proving the HEIC/anything->JPEG conversion contract.",
            "asserts": "let rep = NSBitmapImageRep(bitmapDataPlanes: nil, pixelsWide: 4, pixelsHigh: 4, bitsPerSample: 8, samplesPerPixel: 4, hasAlpha: true, isPlanar: false, colorSpaceName: .deviceRGB, bytesPerRow: 0, bitsPerPixel: 0)!; let png = rep.representation(using: .png, properties: [:])!; let url = tempDir().appendingPathComponent(\"valid.png\"); try png.write(to: url); let jpeg = try AnthropicClient.jpegData(from: url); XCTAssertGreaterThan(jpeg.count, 0); XCTAssertEqual(Array(jpeg.prefix(2)), [0xFF, 0xD8])",
            "apiNotes": "Requires `import AppKit` in the test file. Builds a tiny valid image in-process so no fixture asset is needed; macOS-only (the whole module is AppKit-based).",
            "catchesTypo": false
          },
          {
            "name": "testSchemaRootTypeAndAdditionalProperties",
            "intent": "Pins the request schema root: type 'object' and additionalProperties == false (the regression that once silently dropped fields).",
            "asserts": "let s = AnthropicClient.schema; XCTAssertEqual(s[\"type\"] as? String, \"object\"); XCTAssertEqual(s[\"additionalProperties\"] as? Bool, false)",
            "apiNotes": "Cast Any values explicitly. Existing SchemaTests never checks root type or additionalProperties.",
            "catchesTypo": true
          },
          {
            "name": "testSchemaRequiredIsExactlyElevenKeys",
            "intent": "Pins the FULL required set including 'sender' and 'recipients', which the existing test's loop omits — a typo in either would currently pass.",
            "asserts": "let req = Set((AnthropicClient.schema[\"required\"] as? [String]) ?? []); XCTAssertEqual(req, Set([\"transcription\",\"title\",\"document_type\",\"summary\",\"sender\",\"recipients\",\"date\",\"people_mentioned\",\"notable_quotes\",\"uncertain_spans\",\"meta\"])); XCTAssertEqual((AnthropicClient.schema[\"required\"] as? [String])?.count, 11)",
            "catchesTypo": true
          },
          {
            "name": "testSchemaScalarPropertiesAreStringTyped",
            "intent": "Each scalar property declares type 'string'.",
            "asserts": "let props = (AnthropicClient.schema[\"properties\"] as? [String: Any]) ?? [:]; for key in [\"transcription\",\"title\",\"document_type\",\"summary\",\"sender\"] { let p = props[key] as? [String: Any]; XCTAssertEqual(p?[\"type\"] as? String, \"string\", \"\\(key)\") }",
            "catchesTypo": true
          },
          {
            "name": "testSchemaArrayPropertiesAreStringArrays",
            "intent": "recipients / people_mentioned / notable_quotes are arrays of strings.",
            "asserts": "let props = (AnthropicClient.schema[\"properties\"] as? [String: Any]) ?? [:]; for key in [\"recipients\",\"people_mentioned\",\"notable_quotes\"] { let p = props[key] as? [String: Any]; XCTAssertEqual(p?[\"type\"] as? String, \"array\", \"\\(key)\"); XCTAssertEqual((p?[\"items\"] as? [String: Any])?[\"type\"] as? String, \"string\", \"\\(key) items\") }",
            "catchesTypo": true
          },
          {
            "name": "testSchemaDateSubSchemaIncludingEnums",
            "intent": "Pins the nested date schema: object, additionalProperties false, required [value,source,confidence], and the EXACT source/confidence enum arrays — the highest-value typo guard.",
            "asserts": "let props = (AnthropicClient.schema[\"properties\"] as? [String: Any]) ?? [:]; let date = props[\"date\"] as? [String: Any]; XCTAssertEqual(date?[\"type\"] as? String, \"object\"); XCTAssertEqual(date?[\"additionalProperties\"] as? Bool, false); XCTAssertEqual(Set((date?[\"required\"] as? [String]) ?? []), Set([\"value\",\"source\",\"confidence\"])); let dp = date?[\"properties\"] as? [String: Any]; XCTAssertEqual((dp?[\"value\"] as? [String: Any])?[\"type\"] as? String, \"string\"); XCTAssertEqual((dp?[\"source\"] as? [String: Any])?[\"enum\"] as? [String], [\"written\",\"inferred\",\"unknown\"]); XCTAssertEqual((dp?[\"confidence\"] as? [String: Any])?[\"enum\"] as? [String], [\"high\",\"medium\",\"low\"])",
            "apiNotes": "Enum arrays compared in order to catch reordering/typos. These mirror the only valid date.source / date.confidence values.",
            "catchesTypo": true
          },
          {
            "name": "testSchemaMetaSubSchema",
            "intent": "Pins the nested meta schema: object, additionalProperties false, required exactly the four meta keys, each a string.",
            "asserts": "let props = (AnthropicClient.schema[\"properties\"] as? [String: Any]) ?? [:]; let meta = props[\"meta\"] as? [String: Any]; XCTAssertEqual(meta?[\"type\"] as? String, \"object\"); XCTAssertEqual(meta?[\"additionalProperties\"] as? Bool, false); XCTAssertEqual(Set((meta?[\"required\"] as? [String]) ?? []), Set([\"location\",\"relationship\",\"relationship_state\",\"writer_goals\"])); let mp = meta?[\"properties\"] as? [String: Any]; for key in [\"location\",\"relationship\",\"relationship_state\",\"writer_goals\"] { XCTAssertEqual((mp?[key] as? [String: Any])?[\"type\"] as? String, \"string\", \"\\(key)\") }",
            "catchesTypo": true
          },
          {
            "name": "testSchemaUncertainSpansSubSchema",
            "intent": "Pins uncertain_spans: array whose items are objects with additionalProperties false and required exactly [text,reason], both strings.",
            "asserts": "let props = (AnthropicClient.schema[\"properties\"] as? [String: Any]) ?? [:]; let us = props[\"uncertain_spans\"] as? [String: Any]; XCTAssertEqual(us?[\"type\"] as? String, \"array\"); let items = us?[\"items\"] as? [String: Any]; XCTAssertEqual(items?[\"type\"] as? String, \"object\"); XCTAssertEqual(items?[\"additionalProperties\"] as? Bool, false); XCTAssertEqual(Set((items?[\"required\"] as? [String]) ?? []), Set([\"text\",\"reason\"])); let ip = items?[\"properties\"] as? [String: Any]; XCTAssertEqual((ip?[\"text\"] as? [String: Any])?[\"type\"] as? String, \"string\"); XCTAssertEqual((ip?[\"reason\"] as? [String: Any])?[\"type\"] as? String, \"string\")",
            "catchesTypo": true
          },
          {
            "name": "testSchemaEveryRequiredKeyHasAProperty",
            "intent": "Internal-consistency guard: every key listed in `required` must also exist under `properties` (and vice-versa they line up), so the request can never require a field it didn't declare.",
            "asserts": "let s = AnthropicClient.schema; let req = Set((s[\"required\"] as? [String]) ?? []); let props = Set(((s[\"properties\"] as? [String: Any]) ?? [:]).keys); XCTAssertEqual(req, props)",
            "catchesTypo": false
          },
          {
            "name": "testInstructionMentionsSchemaFieldsAndJSONOnlyDirective",
            "intent": "Guards the system-prompt constant against truncation/accidental edits: it must still name the key extracted fields and demand a single JSON object only.",
            "asserts": "let i = AnthropicClient.instruction; XCTAssertFalse(i.isEmpty); for needle in [\"document_type\", \"uncertain_spans\", \"notable_quotes\", \"meta\", \"[illegible]\"] { XCTAssertTrue(i.contains(needle), \"instruction missing \\(needle)\") }; XCTAssertTrue(i.contains(\"ONLY a single JSON object\"))",
            "apiNotes": "Anchored on stable substrings rather than the whole paragraph to stay robust while still catching deletion of the JSON-only directive or a renamed field.",
            "catchesTypo": true
          }
        ]
      },
      {
        "module": "/Users/labern/Combray/Sources/CombrayCore/Backup.swift",
        "newTests": [
          {
            "name": "testTranscriptionTxtJoinsTitleAndBodyWithBlankLine",
            "intent": "Pin the exact transcription.txt content when a title is present: [title, transcription].joined(separator: \"\\n\\n\").",
            "asserts": "Write LetterFile(title: \"Hi\", transcription: \"Body\") to tempDir(); read String(contentsOf: dir/\"7\"/\"transcription.txt\", encoding: .utf8) and XCTAssertEqual it to exactly \"Hi\\n\\nBody\" (no trailing newline, exactly two newlines between).",
            "apiNotes": "Folder is lettersDir.appendingPathComponent(\"\\(number)\"); file is \"transcription.txt\".",
            "catchesTypo": true
          },
          {
            "name": "testTranscriptionTxtOmitsTitleWhenNil",
            "intent": "When title is nil, compactMap drops it so transcription.txt is just the body with no leading separator.",
            "asserts": "Write LetterFile(title: nil, transcription: \"Body\"); read transcription.txt and XCTAssertEqual to exactly \"Body\" (no leading \"\\n\\n\").",
            "catchesTypo": true
          },
          {
            "name": "testTranscriptionTxtEmptyTitleKeepsLeadingBlankLines",
            "intent": "Empty-string title (\"\") is NOT nil, so compactMap keeps it and the join prefixes \"\\n\\n\" — distinguishes \"\" from nil handling.",
            "asserts": "Write LetterFile(title: \"\", transcription: \"Body\"); read transcription.txt and XCTAssertEqual to exactly \"\\n\\nBody\".",
            "catchesTypo": true
          },
          {
            "name": "testTranscriptionTxtIsRawPlainTextNotJsonEscaped",
            "intent": "transcription.txt is plain UTF-8 text, not JSON: special chars (quotes, slashes) must appear literally, unescaped.",
            "asserts": "Write LetterFile(title: nil, transcription: \"He said \\\"hi\\\"/bye\"); read transcription.txt and XCTAssertEqual to exactly \"He said \\\"hi\\\"/bye\" (no backslash escaping of \" or /).",
            "catchesTypo": false
          },
          {
            "name": "testWriteEmptyBodyAndNilTitleStillWritesEmptyTxtFile",
            "intent": "title nil + transcription \"\" joins to \"\"; transcription.txt is still created as a zero-length file.",
            "asserts": "Write LetterFile(title: nil, transcription: \"\"); assert FileManager.fileExists at dir/\"1\"/\"transcription.txt\" is true AND String(contentsOf:) == \"\".",
            "catchesTypo": false
          },
          {
            "name": "testLetterJsonUsesCamelCaseKeysNotSnakeCase",
            "intent": "LetterFile declares no CodingKeys and Backup sets no keyEncodingStrategy, so JSON keys are the exact Swift camelCase names — regression guard against accidentally enabling convertToSnakeCase.",
            "asserts": "Write a LetterFile with non-nil meta(.init(location:\"L\",relationship:\"R\",relationshipState:\"S\",writerGoals:\"G\")), aiTranscription:\"AI\", notableQuotes:[\"q\"]. Read raw letter.json string; XCTAssertTrue it contains each of \"relationshipState\", \"writerGoals\", \"aiTranscription\", \"notableQuotes\", \"createdAt\", \"updatedAt\"; XCTAssertFalse it contains \"relationship_state\", \"writer_goals\", \"ai_transcription\", \"created_at\".",
            "apiNotes": "Optional keys are only emitted when non-nil, so all four meta subfields, aiTranscription, and notableQuotes must be set. Read via String(contentsOf: folder/\"letter.json\", encoding: .utf8).",
            "catchesTypo": true
          },
          {
            "name": "testLetterJsonDoesNotEscapeSlashes",
            "intent": "Backup.encoder sets .withoutEscapingSlashes, so path-like values keep literal '/'.",
            "asserts": "Write LetterFile with pages: [\"Letters/5/letter_5_page_1.jpg\"]; read raw letter.json; XCTAssertTrue it contains \"Letters/5/letter_5_page_1.jpg\" and XCTAssertFalse it contains \"\\\\/\" (an escaped slash).",
            "catchesTypo": true
          },
          {
            "name": "testLetterJsonIsPrettyPrintedWithSortedKeys",
            "intent": "Encoder sets .prettyPrinted + .sortedKeys: output is multi-line and top-level keys are alphabetical (aiTranscription < createdAt < date < id < number ...).",
            "asserts": "Write a fully-populated LetterFile; read raw json. XCTAssertTrue it contains a newline (\"\\n\"). Using range(of:) on the quoted keys, assert ordering: index of \"\\\"aiTranscription\\\"\" < \"\\\"createdAt\\\"\" < \"\\\"date\\\"\" < \"\\\"id\\\"\" < \"\\\"number\\\"\" < \"\\\"updatedAt\\\"\".",
            "catchesTypo": true
          },
          {
            "name": "testWriteIsDeterministicByteIdentical",
            "intent": ".sortedKeys makes encoding deterministic: writing the same LetterFile twice yields byte-identical letter.json.",
            "asserts": "Write the same file to two separate tempDirs; read both letter.json as Data; XCTAssertEqual the two Data values.",
            "catchesTypo": false
          },
          {
            "name": "testDatesEncodedAsISO8601Strings",
            "intent": "Backup.encoder uses .iso8601 date strategy (not the default reference-date Double).",
            "asserts": "Write LetterFile with createdAt: Date(timeIntervalSince1970: 0), updatedAt: Date(timeIntervalSince1970: 0); read raw json; XCTAssertTrue it contains \"1970-01-01T00:00:00Z\" and XCTAssertFalse it contains a bare numeric like \"-978307200\" (the .deferredToDate value).",
            "apiNotes": "ISO8601 default emits no fractional seconds; use an integral-second epoch so the literal is exact.",
            "catchesTypo": true
          },
          {
            "name": "testFullFidelityRoundTripAllFields",
            "intent": "Every field of LetterFile (beyond the 4 the existing round-trip checks) survives write -> scan unchanged.",
            "asserts": "Write a LetterFile with all fields distinct/non-nil: number, id, title, date(.init(value:\"1962-03\",source:\"written\",confidence:\"high\")), from:\"E\", to:[\"M\",\"Mum\"], summary:\"s\", meta(all four set), transcription, aiTranscription:\"ai\", pages:[\"p1\",\"p2\"], notableQuotes:[\"a\",\"b\"], pinned:false, createdAt/updatedAt: Date(timeIntervalSince1970: 1000). scan() count==1; XCTAssertEqual each field including date.value/source/confidence, to array, all four meta fields, aiTranscription, pages, notableQuotes, pinned, and createdAt/updatedAt (exact Date equality).",
            "apiNotes": "Use integral-second epochs so iso8601 round-trip preserves exact Date equality.",
            "catchesTypo": false
          },
          {
            "name": "testPagesOrderPreservedNotSorted",
            "intent": "pages is an ordered [String]; write/scan must preserve insertion order, not sort it.",
            "asserts": "Write pages: [\"c.jpg\", \"a.jpg\", \"b.jpg\"]; scan back; XCTAssertEqual scanned.pages to exactly [\"c.jpg\", \"a.jpg\", \"b.jpg\"] (NOT the sorted order).",
            "catchesTypo": false
          },
          {
            "name": "testPinnedFalsePersistsAndIsDistinctFromNil",
            "intent": "pinned is Bool?; false is non-nil so it must be encoded and round-trip as false, distinct from absent(nil).",
            "asserts": "Write pinned:false; scan back; XCTAssertEqual scanned.pinned, false (not nil). Separately write pinned:nil; read raw json and XCTAssertFalse it contains \"pinned\"; scan back and XCTAssertNil scanned.pinned.",
            "catchesTypo": false
          },
          {
            "name": "testWriteCreatesIntermediateDirectories",
            "intent": "write uses createDirectory(withIntermediateDirectories: true): a deep, not-yet-existing lettersDir is created.",
            "asserts": "Set lettersDir = tempDir()/\"a\"/\"b\"/\"c\" WITHOUT creating it; Backup.write(file, lettersDir:) must not throw; assert letter.json exists under lettersDir/\"1\"; scan(lettersDir:) returns the record.",
            "catchesTypo": false
          },
          {
            "name": "testWriteOverwritesExistingRecordForSameNumber",
            "intent": "Re-writing the same number replaces letter.json (atomic write); the folder count stays one.",
            "asserts": "Write number:5 title:\"Old\"; write number:5 title:\"New\" to same lettersDir; scan() count==1 and scanned.title==\"New\".",
            "catchesTypo": false
          },
          {
            "name": "testWriteDistinctNumbersProduceSeparateFolders",
            "intent": "Folder name derives from file.number, so different numbers create independent folders both visible to scan.",
            "asserts": "Write number:5 and number:6 to one lettersDir; assert letter.json exists under both \"5\" and \"6\"; scan() count==2 and Set(scanned.map(\\.number))==[5,6].",
            "catchesTypo": false
          },
          {
            "name": "testFolderNamedByNumberBoundaryZero",
            "intent": "Pin folder-naming = \"\\(number)\" at a boundary value (0), confirming no off-by-one or formatting surprise.",
            "asserts": "Write number:0; assert FileManager.fileExists at lettersDir/\"0\"/\"letter.json\"; scan() returns a record with number==0.",
            "catchesTypo": false
          },
          {
            "name": "testWriteThrowsWhenFolderPathBlockedByExistingFile",
            "intent": "Error path: if lettersDir/\"<number>\" already exists as a regular file, createDirectory throws and write propagates it.",
            "asserts": "In a fresh lettersDir, write Data(\"x\".utf8) to lettersDir/\"5\" (a file). Then XCTAssertThrowsError(try Backup.write(file with number:5, lettersDir: lettersDir)).",
            "apiNotes": "createDirectory(at:withIntermediateDirectories:true) throws when the final path component already exists as a file.",
            "catchesTypo": false
          },
          {
            "name": "testScanReturnsEmptyForMissingDirectory",
            "intent": "scan guards contentsOfDirectory with try? and returns [] (never throws) when lettersDir doesn't exist.",
            "asserts": "Let missing = tempDir()/UUID().uuidString (not created); XCTAssertTrue Backup.scan(lettersDir: missing).isEmpty.",
            "catchesTypo": false
          },
          {
            "name": "testScanReturnsEmptyForEmptyDirectory",
            "intent": "An existing but empty lettersDir scans to [].",
            "asserts": "XCTAssertTrue Backup.scan(lettersDir: tempDir()).isEmpty.",
            "catchesTypo": false
          },
          {
            "name": "testScanSkipsSubdirectoryWithoutLetterJson",
            "intent": "compactMap drops subdirectories that have no letter.json (Data(contentsOf:) fails -> nil).",
            "asserts": "Create empty subdir lettersDir/\"9\"; write a valid record number:5 in same lettersDir; scan() count==1 and only number 5 present.",
            "catchesTypo": false
          },
          {
            "name": "testScanSkipsCorruptLetterJson",
            "intent": "try? decoder.decode returns nil for malformed JSON; that entry is skipped, valid ones survive.",
            "asserts": "Write valid number:5; manually create lettersDir/\"9\"/\"letter.json\" containing \"{ not valid json\"; scan() count==1 and the only result has number==5.",
            "catchesTypo": false
          },
          {
            "name": "testScanSkipsRecordWithNonISO8601Date",
            "intent": "Pins the decoder's .iso8601 strategy from the read side: a letter.json whose createdAt isn't ISO8601 fails to decode and is skipped.",
            "asserts": "Manually write a complete-but-for-date letter.json into lettersDir/\"3\"/\"letter.json\" with \"createdAt\":\"01/01/2024\" (and a valid number:7 elsewhere). scan() must NOT include number 3; assert count==1 with number==7.",
            "apiNotes": "Construct the JSON by hand so the bad date string survives; everything else must be valid so only the date is the cause.",
            "catchesTypo": true
          },
          {
            "name": "testScanIgnoresStrayTopLevelFiles",
            "intent": "Non-directory entries at the top level are harmless: appending \"letter.json\" yields a missing path -> nil.",
            "asserts": "Write Data(\"hi\".utf8) to lettersDir/\"notes.txt\"; write a valid record number:5; scan() count==1 with number==5.",
            "catchesTypo": false
          },
          {
            "name": "testScanReturnsAllRecordsRegardlessOfOrder",
            "intent": "scan returns every valid record; contentsOfDirectory order is unspecified so results must be compared as a set.",
            "asserts": "Write numbers 1, 2, 3 (distinct fields); scan() count==3 and Set(scanned.map(\\.number))==[1,2,3]. Do NOT assert array order.",
            "apiNotes": "FileManager directory enumeration order is platform/filesystem-dependent; assert membership, not sequence.",
            "catchesTypo": false
          },
          {
            "name": "testDecodeThrowsWhenRequiredFieldMissing",
            "intent": "Pin which LetterFile fields are required (non-optional): a JSON missing 'number' (or 'to', 'transcription', 'createdAt', or DateInfo.source) must throw DecodingError.",
            "asserts": "Decode a JSON object identical to a valid one but with the 'number' key removed via JSONDecoder(.iso8601) -> XCTAssertThrowsError. Repeat asserting a throw for a JSON missing 'to', and for a date object missing 'source'.",
            "apiNotes": "Mirror Backup's decoder config (dateDecodingStrategy = .iso8601). These throwing decodes are exactly what scan() silently swallows.",
            "catchesTypo": false
          },
          {
            "name": "testDateInfoOptionalSubfieldsDefaultNilWhenAbsent",
            "intent": "DateInfo.value and .confidence are optional (only .source required): date {\"source\":\"written\"} decodes with value/confidence nil.",
            "asserts": "Decode a minimal-but-valid LetterFile whose date is {\"source\":\"written\"}; XCTAssertEqual date.source, \"written\"; XCTAssertNil date.value; XCTAssertNil date.confidence.",
            "catchesTypo": false
          },
          {
            "name": "testMetaInfoAllFieldsOptionalDefaultNil",
            "intent": "MetaInfo's four fields are all optional: \"meta\":{} decodes with location/relationship/relationshipState/writerGoals all nil.",
            "asserts": "Decode a valid LetterFile with \"meta\":{}; XCTAssertNil each of meta.location, meta.relationship, meta.relationshipState, meta.writerGoals.",
            "catchesTypo": false
          }
        ]
      },
      {
        "module": "CombrayCore (focus: ImageStore.swift; plus cross-module constant/enum/decoding pins)",
        "newTests": [
          {
            "name": "testDatabaseURLExactFilename",
            "intent": "Pin the exact sqlite filename constant so a typo (e.g. Combray.sqlite, combray.db) breaks the build.",
            "asserts": "ImageStore(root: tempDir()).databaseURL.lastPathComponent == \"combray.sqlite\" (exact lowercase).",
            "apiNotes": "databaseURL is fully visible in ImageStore.swift line 38. No external symbol risk.",
            "catchesTypo": true
          },
          {
            "name": "testDatabaseURLIsDirectChildOfRoot",
            "intent": "Pin that the sqlite lives directly under root, not in a subfolder.",
            "asserts": "store.databaseURL.deletingLastPathComponent().path == store.root.path for a custom temp root.",
            "apiNotes": "Visible source. Use standardized/resolved paths to avoid symlink (/var vs /private/var) mismatch on macOS temp dirs.",
            "catchesTypo": false
          },
          {
            "name": "testLettersDirIsDirectChildOfRoot",
            "intent": "Pin Letters/ placement relative to root (existing test only checks lastPathComponent).",
            "asserts": "store.lettersDir.deletingLastPathComponent().path == store.root.path.",
            "apiNotes": "Visible source line 37.",
            "catchesTypo": false
          },
          {
            "name": "testRootStoredVerbatim",
            "intent": "Pin that init stores the URL unchanged (no normalization/appending).",
            "asserts": "ImageStore(root: u).root == u for a constructed temp URL.",
            "apiNotes": "Visible source.",
            "catchesTypo": false
          },
          {
            "name": "testDefaultRootLastComponentIsCombray",
            "intent": "Pin the exact app folder name; complements existing contains-check with an exact equality.",
            "asserts": "ImageStore.defaultRoot().lastPathComponent == \"Combray\" (exact, capital C).",
            "apiNotes": "Visible source line 20. Also note: migrateFromDocumentsIfNeeded() is intentionally NOT exercised — it has no injectable roots and would touch real ~/Documents and real Application Support; see dedicated coverage-gap entry.",
            "catchesTypo": true
          },
          {
            "name": "testDefaultRootIsStableAcrossCalls",
            "intent": "Pin determinism of defaultRoot().",
            "asserts": "ImageStore.defaultRoot() == ImageStore.defaultRoot().",
            "apiNotes": "Visible source.",
            "catchesTypo": false
          },
          {
            "name": "testMigrateFromDocumentsIfNeeded_coverageGap",
            "intent": "Document that migrateFromDocumentsIfNeeded() is untestable safely as written and recommend a seam.",
            "asserts": "N/A — FLAG ONLY. Do not invoke against the real filesystem (it reads real ~/Documents/Combray and real Application Support; the sandbox forbids touching the real archive). Recommend adding an internal overload migrate(from oldDocs: URL, to new: URL) so the guard/no-op/move branches can be tested with two temp dirs.",
            "apiNotes": "Source lines 26-35. Both roots come from FileManager with no parameters — no safe unit test exists today.",
            "catchesTypo": false
          },
          {
            "name": "testImportImageNoExtensionDefaultsToJpg",
            "intent": "Pin the `source.pathExtension.isEmpty ? \"jpg\"` default-extension branch (not covered).",
            "asserts": "Import a source named \"scan\" (no extension) → page.imagePath ends with \"letter_1_page_1.jpg\".",
            "apiNotes": "Source line 47. Write bytes to a temp file with no extension.",
            "catchesTypo": true
          },
          {
            "name": "testImportImagePreservesNonJpgExtensionLowercased",
            "intent": "Pin that a non-jpg extension is preserved and lowercased (not force-converted to jpg).",
            "asserts": "Source \"scan.PNG\" with letterNumber 2 → page.imagePath == \"Letters/2/letter_2_page_1.png\".",
            "apiNotes": "Source line 47-48. Confirms .lowercased() and that ext is kept verbatim.",
            "catchesTypo": true
          },
          {
            "name": "testImportImageMixedCaseExtensionLowercased",
            "intent": "Pin lowercasing for a mixed-case multi-letter extension.",
            "asserts": "Source \"scan.JpEg\" → page.imagePath ends with \"letter_1_page_1.jpeg\".",
            "apiNotes": "Source line 47.",
            "catchesTypo": true
          },
          {
            "name": "testImportImagePageNumberIsIndexPlusOneWhilePageIndexStays",
            "intent": "Pin the off-by-one: filename page number is index+1, but the returned Page.pageIndex stays 0-based.",
            "asserts": "importImage(index: 2, letterNumber: 4) → page.imagePath == \"Letters/4/letter_4_page_3.jpg\" AND page.pageIndex == 2.",
            "apiNotes": "Source lines 48, 64. This is the load-bearing off-by-one; existing test only covers index 0.",
            "catchesTypo": true
          },
          {
            "name": "testImportImagePathUsesLetterNumberInDirAndFilename",
            "intent": "Pin that letterNumber appears in BOTH the directory and the filename.",
            "asserts": "letterNumber 9, index 0 → page.imagePath == \"Letters/9/letter_9_page_1.jpg\".",
            "apiNotes": "Source lines 44, 48, 65.",
            "catchesTypo": true
          },
          {
            "name": "testImportImageLetterIdIndependentOfLetterNumber",
            "intent": "Pin that the string letterId and the numeric letterNumber are not crossed.",
            "asserts": "letterId \"uuid-x\", letterNumber 4 → page.letterId == \"uuid-x\" AND page.imagePath contains \"Letters/4/letter_4_\".",
            "apiNotes": "Source lines 63, 65.",
            "catchesTypo": false
          },
          {
            "name": "testImportImageOverwritesExistingDestination",
            "intent": "Pin the `if fileExists { removeItem }` then copy branch (re-import overwrites, no throw).",
            "asserts": "Import source A (bytes \"one\") then source B (bytes \"two\") with same letterNumber/index → file at store.url(for: page2) contains \"two\"; no error thrown.",
            "apiNotes": "Source lines 50-53. Without the removeItem, copyItem would throw on existing dest.",
            "catchesTypo": false
          },
          {
            "name": "testImportImageCopiesNotMovesSource",
            "intent": "Pin copyItem (not moveItem) — original source must remain on disk.",
            "asserts": "After importImage, FileManager.default.fileExists(atPath: source.path) == true.",
            "apiNotes": "Source line 53.",
            "catchesTypo": false
          },
          {
            "name": "testImportImagePreservesBinaryBytesExactly",
            "intent": "Pin lossless copy for non-UTF8 binary content (the module's whole reason for existing).",
            "asserts": "Write Data([0x00,0xFF,0x10,0x80]) to source; after import, Data(contentsOf: store.url(for: page)) == that exact Data.",
            "apiNotes": "Source line 53. Stronger than existing UTF8-string check.",
            "catchesTypo": false
          },
          {
            "name": "testImportImageThrowsWhenSourceMissing",
            "intent": "Cover the throwing path when copyItem fails (nonexistent source).",
            "asserts": "XCTAssertThrowsError(try store.importImage(from: tempDir().appendingPathComponent(\"nope.jpg\"), letterId: \"L\", letterNumber: 1, index: 0)).",
            "apiNotes": "Source line 53 copyItem throws; function is `throws`.",
            "catchesTypo": false
          },
          {
            "name": "testImportImageNilDimensionsForNonImageBytes",
            "intent": "Pin the optional NSImage branch: a non-decodable file yields nil width/height (not 0).",
            "asserts": "Import a source containing \"not-really-a-jpeg\" → page.width == nil AND page.height == nil.",
            "apiNotes": "Source lines 55-60: NSImage(contentsOf:) returns nil → width/height stay nil.",
            "catchesTypo": false
          },
          {
            "name": "testImportImagePopulatesDimensionsForRealImage",
            "intent": "Exercise the real NSImage rep.pixelsWide/pixelsHigh path (existing tests never feed a decodable image).",
            "asserts": "Build a 4x3 PNG via NSBitmapImageRep(...).representation(using: .png, properties: [:]), write it, import → page.width == 4 AND page.height == 3.",
            "apiNotes": "Source lines 57-59. pixelsWide/High read from the first representation. Construct the bitmap rep directly to avoid scaling surprises.",
            "catchesTypo": false
          },
          {
            "name": "testUrlForPageJoinsRootAndImagePath",
            "intent": "Directly test url(for:) (currently only exercised transitively).",
            "asserts": "For ImageStore(root: r) and Page(imagePath: \"Letters/3/a.jpg\"), store.url(for: page).path == r.appendingPathComponent(\"Letters/3/a.jpg\").path.",
            "apiNotes": "Source lines 72-74. Page initializer: Page(letterId:pageIndex:imagePath:width:height:) per existing tests.",
            "catchesTypo": false
          },
          {
            "name": "testImportTwoPagesIntoSameLetterCoexist",
            "intent": "Pin that importing index 0 then index 1 into the same letter keeps both files (dir reuse, withIntermediateDirectories no-throw).",
            "asserts": "After importing index 0 and index 1 for letterNumber 5, both store.url(for: page0) and store.url(for: page1) exist; their imagePaths end page_1 and page_2.",
            "apiNotes": "Source line 45 createDirectory(withIntermediateDirectories: true) must not throw on existing dir.",
            "catchesTypo": false
          },
          {
            "name": "testTranscriptionResultEmptyObjectFillsAllDefaults",
            "intent": "Pin EVERY lenient default from a totally empty JSON object so a changed default string/array is caught.",
            "asserts": "decode(\"{}\") → transcription==\"\", title==\"\", document_type==\"\", summary==\"\", sender==\"\", recipients==[], people_mentioned==[], notable_quotes==[], uncertain_spans==[].",
            "apiNotes": "Confirm `sender` default is \"\" (it is a String in the full-object test). Verify all field names match the struct.",
            "catchesTypo": true
          },
          {
            "name": "testTranscriptionResultNestedDateDefaultsWhenDateKeyAbsent",
            "intent": "Pin nested `date` defaults when the whole date object is missing.",
            "asserts": "decode(\"{\\\"transcription\\\":\\\"t\\\"}\") → date.value==\"\", date.source==\"\", date.confidence==\"\".",
            "apiNotes": "Verify TranscriptionResult.date is non-optional with a default-constructed nested type.",
            "catchesTypo": true
          },
          {
            "name": "testTranscriptionResultNestedMetaDefaultsWhenMetaKeyAbsent",
            "intent": "Pin nested `meta` defaults when meta object is missing.",
            "asserts": "decode(\"{\\\"transcription\\\":\\\"t\\\"}\") → meta.location==\"\", meta.relationship==\"\", meta.relationship_state==\"\", meta.writer_goals==\"\".",
            "apiNotes": "Verify meta field names (snake_case relationship_state, writer_goals) against the struct.",
            "catchesTypo": true
          },
          {
            "name": "testTranscriptionResultPartialDateObject",
            "intent": "Pin partial nested decode: a date object with only value present.",
            "asserts": "decode(\"{\\\"date\\\":{\\\"value\\\":\\\"1981\\\"}}\") → date.value==\"1981\", date.source==\"\", date.confidence==\"\".",
            "apiNotes": "Confirms nested type is itself lenient.",
            "catchesTypo": false
          },
          {
            "name": "testTranscriptionResultPartialMetaObject",
            "intent": "Pin partial nested meta decode.",
            "asserts": "decode(\"{\\\"meta\\\":{\\\"location\\\":\\\"Paris\\\"}}\") → meta.location==\"Paris\", meta.relationship==\"\", meta.relationship_state==\"\", meta.writer_goals==\"\".",
            "apiNotes": "Nested meta leniency.",
            "catchesTypo": false
          },
          {
            "name": "testTranscriptionResultUncertainSpanElementFields",
            "intent": "Assert into uncertain_spans element fields (existing full-object test decodes them but never reads text/reason).",
            "asserts": "decode with uncertain_spans:[{\"text\":\"x\",\"reason\":\"y\"}] → uncertain_spans.first.text==\"x\", uncertain_spans.first.reason==\"y\".",
            "apiNotes": "Confirm the span element's property names are `text` and `reason`.",
            "catchesTypo": true
          },
          {
            "name": "testTranscriptionResultIgnoresUnknownKeys",
            "intent": "Pin that the decoder is forgiving of unexpected keys (model drift / extra fields).",
            "asserts": "decode(\"{\\\"transcription\\\":\\\"t\\\",\\\"surprise\\\":123,\\\"nested\\\":{\\\"a\\\":1}}\") does NOT throw and transcription==\"t\".",
            "apiNotes": "Default Swift Decodable already ignores unknown keys unless a custom init forbids them; this pins it.",
            "catchesTypo": false
          },
          {
            "name": "testTranscriptionResultNullFieldsBecomeDefaults",
            "intent": "Pin behavior for explicit JSON nulls on string/array fields.",
            "asserts": "decode(\"{\\\"title\\\":null,\\\"recipients\\\":null}\") → title==\"\" and recipients==[] (lenient null handling).",
            "apiNotes": "FLAG: this only passes if the custom decoder treats null as default. If it does NOT, this test documents that null currently throws — confirm against source and adjust expectation.",
            "catchesTypo": false
          },
          {
            "name": "testComposedTitleLetterNoRecipients",
            "intent": "Pin correspondence wording when recipients empty but type is explicitly a letter.",
            "asserts": "Archive.composedTitle(documentType: \"letter\", sender: \"Alice\", recipients: [], summary: \"\") == \"Letter from Alice\".",
            "apiNotes": "Mirrors existing blank-type case which yielded \"Letter from Alice\"; confirm explicit \"letter\" behaves identically.",
            "catchesTypo": true
          },
          {
            "name": "testComposedTitleCorrespondenceNoSender",
            "intent": "Pin the omission of the 'from' clause when sender is empty.",
            "asserts": "Archive.composedTitle(documentType: \"letter\", sender: \"\", recipients: [\"Bob\"], summary: \"\") == \"Letter to Bob\".",
            "apiNotes": "FLAG: confirm exact output ('from' dropped) against composedTitle source.",
            "catchesTypo": true
          },
          {
            "name": "testComposedTitleCorrespondenceNoSenderNoRecipients",
            "intent": "Pin the degenerate correspondence case (no parties at all).",
            "asserts": "Archive.composedTitle(documentType: \"letter\", sender: \"\", recipients: [], summary: \"\") == \"Letter\".",
            "apiNotes": "FLAG: confirm fallback string (likely just the capitalized kind \"Letter\").",
            "catchesTypo": true
          },
          {
            "name": "testComposedTitleMultipleRecipientsJoin",
            "intent": "Pin the join format for multiple recipients.",
            "asserts": "Archive.composedTitle(documentType: \"letter\", sender: \"Alice\", recipients: [\"Bob\",\"Carol\"], summary: \"\") == the exact joined string.",
            "apiNotes": "FLAG: expected join unknown — read source to choose between \"Bob and Carol\", \"Bob, Carol\", \"Bob & Carol\". Pin whatever it is.",
            "catchesTypo": true
          },
          {
            "name": "testComposedTitleCapitalizesArbitraryDocumentType",
            "intent": "Pin that a non-correspondence kind is capitalized (first letter upper) in the title.",
            "asserts": "Archive.composedTitle(documentType: \"note\", sender: \"\", recipients: [], summary: \"buy milk\") == \"Note — buy milk\" (em dash, capitalized kind).",
            "apiNotes": "FLAG: confirm the separator is an em dash \"—\" (matches existing \"List — weekly groceries\") and that capitalization is first-letter-only.",
            "catchesTypo": true
          },
          {
            "name": "testComposedTitleWhitespaceOnlySummaryFallsBackToKind",
            "intent": "Pin whitespace handling for non-correspondence summary.",
            "asserts": "Archive.composedTitle(documentType: \"receipt\", sender: \"\", recipients: [], summary: \"   \") == \"Receipt\".",
            "apiNotes": "FLAG: only passes if summary is trimmed before the empty check; if not trimmed, output would be \"Receipt —    \". Confirm and pin actual behavior.",
            "catchesTypo": false
          },
          {
            "name": "testYearRejectsThreeDigitString",
            "intent": "Pin that a 3-digit string is not accepted as a year (boundary below 4 digits).",
            "asserts": "Archive.year(from: \"962\") == nil.",
            "apiNotes": "Existing test shows \"62\" → nil; this nails the 3-digit boundary.",
            "catchesTypo": false
          },
          {
            "name": "testYearAcceptsFourDigitBoundaries",
            "intent": "Pin that any 4 digits parse (no semantic range validation).",
            "asserts": "Archive.year(from: \"0000\") == 0 AND Archive.year(from: \"9999\") == 9999.",
            "apiNotes": "FLAG: confirm there is no min-year filter that would reject 0000.",
            "catchesTypo": false
          },
          {
            "name": "testYearRejectsNonNumericFourChars",
            "intent": "Pin that 4 non-digit characters do not parse.",
            "asserts": "Archive.year(from: \"abcd\") == nil.",
            "apiNotes": "Complements existing \"spring\" → nil.",
            "catchesTypo": false
          },
          {
            "name": "testYearFromMessyDateValue",
            "intent": "Pin whether year() scans for a 4-digit run inside noisy text or requires a clean prefix (matters because date.value can be model-generated).",
            "asserts": "Archive.year(from: \"circa 1962\") == 1962 OR == nil — assert whichever the implementation actually does.",
            "apiNotes": "FLAG: behavior unknown. Read source: prefix(4)+Int(...) would give nil here; a regex search for \\\\d{4} would give 1962. Pin the real one.",
            "catchesTypo": false
          },
          {
            "name": "testYearWithLeadingWhitespace",
            "intent": "Pin trimming behavior for a leading space.",
            "asserts": "Archive.year(from: \" 1962\") == 1962 OR == nil — pin whichever the implementation does.",
            "apiNotes": "FLAG: depends on whether the string is trimmed before parsing.",
            "catchesTypo": false
          },
          {
            "name": "testSchemaTypeIsObject",
            "intent": "Pin the JSON-schema root type constant.",
            "asserts": "(AnthropicClient.schema[\"type\"] as? String) == \"object\".",
            "apiNotes": "FLAG: confirm a top-level \"type\":\"object\" key exists in the schema dictionary.",
            "catchesTypo": true
          },
          {
            "name": "testSchemaRequiredIsExactlyTheExpectedSet",
            "intent": "Tighten the existing contains-check to an EXACT set so an accidental added/removed required key fails (regression-prevention for the additionalProperties bug).",
            "asserts": "Set(schema[\"required\"] as! [String]) == [\"transcription\",\"title\",\"document_type\",\"summary\",\"date\",\"people_mentioned\",\"notable_quotes\",\"uncertain_spans\",\"meta\"] exactly.",
            "apiNotes": "Existing test only asserts containment; note required deliberately excludes sender/recipients.",
            "catchesTypo": true
          },
          {
            "name": "testSchemaEveryRequiredKeyHasAProperty",
            "intent": "Pin the invariant that required keys are a subset of declared properties.",
            "asserts": "For each key in schema[\"required\"], (schema[\"properties\"] as! [String:Any])[key] != nil.",
            "apiNotes": "Structural invariant; resilient to value-shape changes.",
            "catchesTypo": false
          },
          {
            "name": "testSchemaHasOptionalSenderAndRecipientsProperties",
            "intent": "Pin that sender/recipients exist as properties even though they are NOT required.",
            "asserts": "props[\"sender\"] != nil AND props[\"recipients\"] != nil AND !(schema[\"required\"] as! [String]).contains(\"sender\") AND !...contains(\"recipients\").",
            "apiNotes": "Documents the intentional optionality of the two party fields.",
            "catchesTypo": false
          },
          {
            "name": "testSchemaMetaNestedPropertyKeys",
            "intent": "Pin the exact nested meta property keys (snake_case) so a rename is caught.",
            "asserts": "Let metaProps = ((props[\"meta\"] as! [String:Any])[\"properties\"] as! [String:Any]); its keys contain location, relationship, relationship_state, writer_goals.",
            "apiNotes": "FLAG: confirm meta property is nested as properties[\"meta\"][\"properties\"]; adjust path if the schema nests differently.",
            "catchesTypo": true
          },
          {
            "name": "testSchemaPropertyKeysAreSnakeCase",
            "intent": "Pin snake_case spelling of multi-word keys (catches camelCase typo that would desync from the decoder).",
            "asserts": "props.keys contains exactly \"document_type\", \"people_mentioned\", \"notable_quotes\", \"uncertain_spans\" (and none of their camelCase variants).",
            "apiNotes": "These must match TranscriptionResult's CodingKeys / JSON.",
            "catchesTypo": true
          },
          {
            "name": "testSchemaArrayFieldsDeclaredAsArrays",
            "intent": "Pin that the collection fields are typed array in the schema.",
            "asserts": "For \"recipients\", \"people_mentioned\", \"notable_quotes\": (props[k] as! [String:Any])[\"type\"] as? String == \"array\".",
            "apiNotes": "FLAG: confirm each property dict carries a \"type\":\"array\" entry.",
            "catchesTypo": true
          },
          {
            "name": "testStoredCredentialKindRawValues",
            "intent": "Pin the enum rawValues so a rename (e.g. .apiKey -> \"api_key\") fails and would break on-disk credential compatibility.",
            "asserts": "StoredCredential.Kind.oauth.rawValue == \"oauth\" AND StoredCredential.Kind.apiKey.rawValue == \"apiKey\".",
            "apiNotes": "FLAG: confirm Kind is a String-raw enum and the exact rawValue strings ('apiKey' vs 'api_key').",
            "catchesTypo": true
          },
          {
            "name": "testStoredCredentialKindDecodesFromRawValue",
            "intent": "Pin the JSON wire form of kind round-trips to the right case.",
            "asserts": "Decoding a StoredCredential JSON with kind \"oauth\" yields .oauth; encoding .apiKey emits the pinned rawValue string.",
            "apiNotes": "Use JSONEncoder/Decoder; complements the rawValue pin by proving the Codable mapping.",
            "catchesTypo": true
          },
          {
            "name": "testApiKeyCredentialIgnoresPastExpiresAt",
            "intent": "Pin that kind .apiKey never expires even when an (irrelevant) past expiresAt is set.",
            "asserts": "StoredCredential(kind: .apiKey, apiKey: \"k\") with expiresAt in the past → isExpired == false.",
            "apiNotes": "FLAG: confirm an initializer path allows apiKey + expiresAt; otherwise set via the available init and assert isExpired stays false.",
            "catchesTypo": false
          },
          {
            "name": "testOAuthCredentialWithNilExpiresAtPolicy",
            "intent": "Pin the policy for an oauth credential lacking an expiry.",
            "asserts": "StoredCredential(kind: .oauth, accessToken: \"a\") with expiresAt == nil → isExpired == (the implemented policy: likely true 'unknown == needs refresh').",
            "apiNotes": "FLAG: confirm whether nil expiry counts as expired; pin the real result.",
            "catchesTypo": false
          },
          {
            "name": "testCredentialCodableRoundTripPreservesApiKeyAndExpiresAt",
            "intent": "Extend the existing oauth round-trip to also pin apiKey and expiresAt survival.",
            "asserts": "Round-trip a StoredCredential(kind: .oauth, accessToken: \"t\", refreshToken: \"r\", expiresAt: Date(timeIntervalSince1970: 1000)) → back.expiresAt == that date; separately round-trip an apiKey credential → back.apiKey preserved.",
            "apiNotes": "Existing test ignores expiresAt and apiKey field.",
            "catchesTypo": false
          },
          {
            "name": "testNextLetterNumberIsMaxPlusOneAfterDelete",
            "intent": "Pin whether nextLetterNumber is max(number)+1 vs count-based (matters after deletions).",
            "asserts": "Save letters numbered 1,2,3; delete the number-3 letter; nextLetterNumber() == 3 (if max-of-remaining) OR == 4 (if max-ever). Assert the implemented one.",
            "apiNotes": "FLAG: read the SQL — likely SELECT max(number)+1, which would give 3 after deleting #3. Pin actual.",
            "catchesTypo": false
          },
          {
            "name": "testAllLettersOrderingWithNilYear",
            "intent": "Pin where letters with nil dateYear sort relative to dated ones (existing ordering test uses only non-nil years).",
            "asserts": "Save letters with dateYear 1990, nil, 1975; allLetters().map(\\.dateYear) equals the implemented order (e.g. [1990,1975,nil] if NULLs last DESC).",
            "apiNotes": "FLAG: SQLite ORDER BY ... DESC puts NULLs last by default; confirm the query and pin the exact array including nil position.",
            "catchesTypo": false
          },
          {
            "name": "testSaveIsUpsertNotInsert",
            "intent": "Pin that re-saving a fetched letter updates in place (count stays 1) rather than duplicating.",
            "asserts": "Save Letter(number:1); fetch; mutate title to \"X\"; save again; allLetters().count == 1 AND letter(id:).title == \"X\".",
            "apiNotes": "Existing testPinnedPersists shows persistence but not the count==1 invariant.",
            "catchesTypo": false
          },
          {
            "name": "testDeleteLetterCascadesPagesAndParticipants",
            "intent": "Pin FK cascade: deleting a letter removes its pages and participant links.",
            "asserts": "Set participants + pages on a letter, deleteLetter(id:), then pages(forLetterId:) == [] AND participants(forLetterId:).sender == nil AND .recipients == [].",
            "apiNotes": "FLAG: confirm ON DELETE CASCADE (or app-level cleanup) exists; if not, this documents a leak.",
            "catchesTypo": false
          },
          {
            "name": "testDeleteNonexistentLetterDoesNotThrow",
            "intent": "Edge: deleting an unknown id is a no-op, not an error.",
            "asserts": "XCTAssertNoThrow(try a.deleteLetter(id: \"does-not-exist\")).",
            "apiNotes": "Light robustness check.",
            "catchesTypo": false
          },
          {
            "name": "testSetParticipantsOverwritesPrevious",
            "intent": "Pin replace (not append) semantics for participants.",
            "asserts": "setParticipants(sender:\"A\", recipients:[\"B\"]) then setParticipants(sender:\"C\", recipients:[\"D\"]) → participants.sender.displayName==\"C\" and recipients == [\"D\"] only.",
            "apiNotes": "Confirms no accumulation of stale links.",
            "catchesTypo": false
          },
          {
            "name": "testPeopleDeduplicatesByName",
            "intent": "Pin that the same person name across two letters yields a single Person.",
            "asserts": "Two letters both with sender \"Alice\"; people().filter { $0.displayName == \"Alice\" }.count == 1.",
            "apiNotes": "FLAG: confirm dedup is by displayName; existing people test uses all-distinct names so this is uncovered.",
            "catchesTypo": false
          },
          {
            "name": "testPagesReturnedOrderedByPageIndex",
            "intent": "Pin that pages(forLetterId:) returns rows ordered by pageIndex (existing test sorts in the assertion, hiding store order).",
            "asserts": "setPages with pageIndex order [2,0,1]; pages(forLetterId:).map(\\.pageIndex) == [0,1,2] WITHOUT sorting in the test.",
            "apiNotes": "FLAG: confirm the query has ORDER BY pageIndex.",
            "catchesTypo": false
          },
          {
            "name": "testSetPagesReplacesPreviousPages",
            "intent": "Pin replace semantics for setPages.",
            "asserts": "setPages([p0,p1]) then setPages([p0]) → pages(forLetterId:).count == 1.",
            "apiNotes": "Mirrors participant replace expectation.",
            "catchesTypo": false
          },
          {
            "name": "testSetPagesPersistsWidthAndHeight",
            "intent": "Pin that page dimensions survive the DB round-trip (existing pages use nil dims).",
            "asserts": "setPages([Page(letterId:..., pageIndex:0, imagePath:\"p.jpg\", width:1200, height:1600)]) → pages(forLetterId:).first.width==1200, .height==1600.",
            "apiNotes": "Confirm Page init exposes width/height labels (it does per ImageStore usage).",
            "catchesTypo": false
          },
          {
            "name": "testSearchMatchesTitleAndRecipient",
            "intent": "Pin that the FTS index covers title and recipient names (existing only covers transcription and sender).",
            "asserts": "Letter with title \"Venice\" and recipient \"Marcel\": search(\"Venice\").first.letterId == id AND search(\"Marcel\").first.letterId == id.",
            "apiNotes": "FLAG: confirm title and recipients are part of the FTS content; if not, split into separate tests and pin actual.",
            "catchesTypo": false
          },
          {
            "name": "testSearchIsCaseInsensitive",
            "intent": "Pin FTS case-folding.",
            "asserts": "Letter transcription \"Madeleine\"; search(\"madeleine\").count == 1.",
            "apiNotes": "FTS5 default tokenizer is case-insensitive; pins it.",
            "catchesTypo": false
          },
          {
            "name": "testSearchReflectsDeletionsAndEdits",
            "intent": "Pin that the FTS index stays consistent on delete and update (a common trigger/sync bug).",
            "asserts": "Save letter with \"alpha\" → search(\"alpha\") finds it; re-save with transcription \"beta\" → search(\"alpha\") empty AND search(\"beta\") finds it; delete → search(\"beta\") empty.",
            "apiNotes": "FLAG: requires FTS triggers or manual index maintenance on save/delete; this surfaces drift if absent.",
            "catchesTypo": false
          },
          {
            "name": "testSearchWithFtsSpecialCharactersDoesNotThrow",
            "intent": "Robustness: a query containing FTS-special characters must not crash the app.",
            "asserts": "XCTAssertNoThrow(try a.search(\"\\\"quick*\\\" -dog\")) (or returns []) for a query with quotes, asterisk, and a leading hyphen.",
            "apiNotes": "FLAG: raw FTS MATCH throws on malformed queries; if search doesn't sanitize, this documents the crash risk.",
            "catchesTypo": false
          },
          {
            "name": "testApplyTranscriptionEmptyDateYieldsNilYear",
            "intent": "Pin that an empty date.value produces dateYear nil (existing compose test has empty date but never asserts the year).",
            "asserts": "applyTranscription with date.value \"\" → updated.dateYear == nil AND updated.dateValue == \"\".",
            "apiNotes": "Ties applyTranscription to year(from:) returning nil.",
            "catchesTypo": false
          },
          {
            "name": "testApplyTranscriptionDateSourceEnumMapping",
            "intent": "Pin the date.source string → enum mapping for each known case.",
            "asserts": "source \"written\" → dateSource == .written; \"inferred\" → .inferred; \"unknown\" → .unknown.",
            "apiNotes": "FLAG: confirm the enum case names (.written exists per existing test; verify .inferred/.unknown). Existing tests use the strings but never assert .inferred/.unknown.",
            "catchesTypo": true
          },
          {
            "name": "testApplyTranscriptionUnknownDateSourceFallsBack",
            "intent": "Pin lenient fallback when the model returns an unrecognized date source.",
            "asserts": "applyTranscription with date.source \"garbage-value\" → updated.dateSource == .unknown (default), and no throw.",
            "apiNotes": "FLAG: confirm the default fallback case; important since source is free-form model output.",
            "catchesTypo": false
          },
          {
            "name": "testApplyTranscriptionNotableQuotesJoinFormat",
            "intent": "Pin how multiple notable_quotes are flattened into the single stored notableQuotes string.",
            "asserts": "notable_quotes [\"a\",\"b\"] → updated.notableQuotes == the exact joined string; notable_quotes [] → updated.notableQuotes == nil (or \"\").",
            "apiNotes": "FLAG: existing test stores a single quote verbatim; the multi-quote separator (newline? \"; \"?) and the empty-case (nil vs \"\") are unknown — read source and pin.",
            "catchesTypo": true
          },
          {
            "name": "testApplyTranscriptionReplacesExistingParticipants",
            "intent": "Pin that applying a new transcription overwrites prior participants rather than appending.",
            "asserts": "setParticipants(sender:\"Old\", recipients:[\"X\"]); then applyTranscription with sender \"New\", recipients [\"Y\"] → participants.sender.displayName==\"New\", recipients==[\"Y\"].",
            "apiNotes": "Guards against duplicate participant rows on re-transcription.",
            "catchesTypo": false
          },
          {
            "name": "testApplyTranscriptionResultIsPersistedAndPreservesIdentity",
            "intent": "Pin that the returned letter equals the persisted row and that number/id are unchanged.",
            "asserts": "let updated = applyTranscription(r, toLetterId: l.id); letter(id: l.id) reflects updated.title/summary; AND updated.id == l.id AND updated.number == l.number.",
            "apiNotes": "Confirms apply writes to DB (not just returns a value) and does not mutate identity.",
            "catchesTypo": false
          },
          {
            "name": "testBackupTranscriptionTxtContentMatches",
            "intent": "Pin the actual content written to transcription.txt (existing test only checks the file exists).",
            "asserts": "After Backup.write(file, lettersDir: dir), String(contentsOf: dir/\"5\"/\"transcription.txt\") == file.transcription.",
            "apiNotes": "FLAG: confirm transcription.txt is the plain transcription (vs aiTranscription) and whether a trailing newline is appended.",
            "catchesTypo": false
          },
          {
            "name": "testBackupScanIgnoresNonNumericFolders",
            "intent": "Pin that scan keys off numeric letter folders and skips stray directories/files.",
            "asserts": "Create dir \"5\" (valid letter.json) plus dir \"_notes\" and a loose file → Backup.scan(lettersDir:).count == 1 and its number == 5.",
            "apiNotes": "FLAG: confirm scan filters by numeric folder name / presence of letter.json.",
            "catchesTypo": false
          },
          {
            "name": "testBackupScanEmptyDirectoryReturnsEmpty",
            "intent": "Edge: scanning an empty/nonexistent letters dir returns [].",
            "asserts": "Backup.scan(lettersDir: tempDir()) == [] (empty).",
            "apiNotes": "Confirm scan tolerates a missing/empty dir without throwing (it returns [LetterFile], non-throwing per existing usage).",
            "catchesTypo": false
          },
          {
            "name": "testBackupRoundTripPreservesAllFields",
            "intent": "Tighten the write/scan round-trip to pin EVERY field (existing only checks number/title/pinned/notableQuotes).",
            "asserts": "Write a fully-populated LetterFile, scan it back, assert date.value/source/confidence, meta.location, from, to, summary, transcription, pages, createdAt, updatedAt all equal the originals.",
            "apiNotes": "Use Date(timeIntervalSince1970: 0) for createdAt/updatedAt and the iso8601 strategy to make date comparison exact.",
            "catchesTypo": false
          },
          {
            "name": "testLetterFileEncodesPinnedAndNotableQuotes",
            "intent": "Forward-compat mirror of the existing old-record decode test: pin that new fields ARE emitted on encode.",
            "asserts": "Encode a LetterFile with pinned:true and notableQuotes:[\"q\"]; the JSON string contains \"pinned\" and \"notableQuotes\" (or their exact JSON keys).",
            "apiNotes": "FLAG: confirm the on-disk JSON key spelling for these fields.",
            "catchesTypo": true
          },
          {
            "name": "testImportFromFilesIsIdempotentOnId",
            "intent": "Pin that re-importing the same id does not create duplicates (rebuild-index safety).",
            "asserts": "importFromFiles([file]) twice with the same id → allLetters().count == 1 AND pages(forLetterId:).count matches the file (no doubling).",
            "apiNotes": "FLAG: confirm importFromFiles upserts by id and replaces pages/participants rather than appending.",
            "catchesTypo": false
          },
          {
            "name": "testImportFromFilesMultipleRebuildsPeopleAndYears",
            "intent": "Pin batch import rebuilds the derived indexes (existing test imports a single file).",
            "asserts": "importFromFiles([f1(year 1970, from A to B), f2(year 1985, from C)]) → Set(years()) == [1970,1985] AND Set(people().map(\\.displayName)) == [\"A\",\"B\",\"C\"].",
            "apiNotes": "Confirms participants/years are derived during import, not just letter rows.",
            "catchesTypo": false
          },
          {
            "name": "testLetterFileOldRecordNestedDateAndMetaAreNil",
            "intent": "Tighten the existing old-record decode to assert INTO the nested date/meta (existing test only checks top-level nils).",
            "asserts": "Decoding the legacy JSON (date {\"source\":\"unknown\"}, meta {}) → file.date.value == nil, file.date.confidence == nil, and all meta.* == nil.",
            "apiNotes": "Confirms LetterFile.date.value, .confidence and meta fields are Optional.",
            "catchesTypo": false
          },
          {
            "name": "testSchemaVersionPinnedToCurrentValue",
            "intent": "Change-detector: pin Combray.schemaVersion to its exact current integer so an unintended migration bump is caught (the prompt asks to pin numeric defaults).",
            "asserts": "Combray.schemaVersion == <current N read from source>.",
            "apiNotes": "FLAG: this is intentionally a change-detector — when migrations are legitimately added, this expected value must be bumped in the same commit. Read the current value from source before pinning.",
            "catchesTypo": true
          }
        ]
      },
      {
        "module": "CombrayCore",
        "newTests": [
          {
            "name": "testDateSourceRawValuesAndCaseOrder",
            "intent": "Pin DateSource enum rawValues and CaseIterable order so a typo in any case name fails.",
            "asserts": "XCTAssertEqual(DateSource.allCases.map(\\.rawValue), [\"written\",\"inferred\",\"manual\",\"unknown\"]); XCTAssertEqual(DateSource.allCases.count, 4).",
            "catchesTypo": true,
            "apiNotes": "DateSource is public String/CaseIterable in Models.swift."
          },
          {
            "name": "testConfidenceRawValues",
            "intent": "Pin Confidence rawValues high/medium/low and count.",
            "asserts": "XCTAssertEqual(Confidence.allCases.map(\\.rawValue), [\"high\",\"medium\",\"low\"]); XCTAssertEqual(Confidence.allCases.count, 3).",
            "catchesTypo": true
          },
          {
            "name": "testPersonRoleRawValues",
            "intent": "Pin PersonRole rawValues sender/recipient (used as literal SQL args in participants()).",
            "asserts": "XCTAssertEqual(PersonRole.sender.rawValue, \"sender\"); XCTAssertEqual(PersonRole.recipient.rawValue, \"recipient\"); XCTAssertEqual(PersonRole.allCases.count, 2).",
            "catchesTypo": true
          },
          {
            "name": "testDateSourceInitFromRawValue",
            "intent": "Round-trip DateSource(rawValue:) and confirm unknown raw maps to nil (the path applyTranscription coalesces to .unknown).",
            "asserts": "XCTAssertEqual(DateSource(rawValue:\"written\"), .written); XCTAssertEqual(DateSource(rawValue:\"manual\"), .manual); XCTAssertNil(DateSource(rawValue:\"garbage\"))."
          },
          {
            "name": "testDatabaseTableNames",
            "intent": "Pin every GRDB databaseTableName constant; a typo silently breaks persistence at runtime.",
            "asserts": "XCTAssertEqual(Person.databaseTableName, \"person\"); Relationship==\"relationship\"; Letter==\"letter\"; Page==\"page\"; LetterPerson==\"letterPerson\".",
            "catchesTypo": true,
            "apiNotes": "These static lets match the CREATE TABLE names in AppDatabase migrator v1."
          },
          {
            "name": "testLetterDefaultInitValues",
            "intent": "Pin Letter() default values (numeric/enum/string/bool defaults).",
            "asserts": "Letter() -> number==0, dateSource==.unknown, transcription==\"\", pinned==false, dateConfidence==nil, title==nil; createdAt/updatedAt are non-nil Dates.",
            "catchesTypo": true
          },
          {
            "name": "testPersonDefaultInitGeneratesId",
            "intent": "Person() auto-id is a non-empty UUID; aka/notes default nil.",
            "asserts": "let p = Person(displayName:\"X\"); XCTAssertFalse(p.id.isEmpty); XCTAssertNil(p.aka); XCTAssertNil(p.notes); XCTAssertNotNil(UUID(uuidString: p.id))."
          },
          {
            "name": "testLetterPersonCodableEncodesRoleRawValue",
            "intent": "JSON-encode LetterPerson and confirm role serializes as its rawValue string, not an int.",
            "asserts": "Encode LetterPerson(role:.sender); decoded JSON string contains \"\\\"role\\\":\\\"sender\\\"\"; decode back equals original.",
            "catchesTypo": true
          },
          {
            "name": "testLetterCodableRoundTripPreservesEnums",
            "intent": "Full Letter Codable round-trip preserves enum rawValues and nil optionals.",
            "asserts": "Encode then decode a Letter with dateSource:.written, dateConfidence:.medium, nil title/summary; decoded equals original (dateSource/.dateConfidence/optionals intact)."
          },
          {
            "name": "testRelationshipPersistenceRoundTrip",
            "intent": "Relationship has no Archive helper but is Persistable; exercise insert/fetch against the 'relationship' table.",
            "asserts": "Via a.database.dbWriter.write insert a Person then Relationship(personId:p.id, relation:\"grandmother\"); fetchOne by key returns relation==\"grandmother\".",
            "apiNotes": "Archive.database is public; AppDatabase.dbWriter/reader are public, so raw GRDB access compiles under @testable."
          },
          {
            "name": "testNextLetterNumberUsesMaxNotCount",
            "intent": "Prove nextLetterNumber is MAX(number)+1, not COUNT+1 (so gaps/high numbers behave).",
            "asserts": "save Letter(number:100); XCTAssertEqual(try a.nextLetterNumber(), 101)."
          },
          {
            "name": "testAllLettersNilYearSortsLastWithCreatedAtTiebreak",
            "intent": "Pin the 'dateYear DESC NULLS LAST, createdAt DESC' ordering not covered by the existing all-dated test.",
            "asserts": "Save letters: year 1980, year 1980 (later createdAt), nil year; allLetters() puts the two 1980s first (newest createdAt first) and the nil-year letter last.",
            "apiNotes": "Set createdAt explicitly to control the tiebreak deterministically."
          },
          {
            "name": "testLettersForYearFiltersAndOrders",
            "intent": "letters(forYear:) returns only that year, ordered createdAt DESC.",
            "asserts": "Two letters in 1962 (diff createdAt) + one in 1970; letters(forYear:1962) returns exactly the two 1962 ids, newest createdAt first."
          },
          {
            "name": "testLettersForPersonIdOldestFirst",
            "intent": "letters(forPersonId:) returns the person's letters ordered dateYear ASC then createdAt ASC (opposite direction to allLetters) and excludes unrelated letters.",
            "asserts": "Person on letters of year 1970 and 1960 (plus an unrelated letter); result == [1960-letter, 1970-letter] and omits the unrelated one.",
            "apiNotes": "Resolve personId via participants(forLetterId:).sender?.id after setParticipants."
          },
          {
            "name": "testYearsDistinctDescending",
            "intent": "years() de-duplicates and returns DESC (existing test only checks the set after .sorted()).",
            "asserts": "Save letters with years [1962,1962,1970,nil]; XCTAssertEqual(try a.years(), [1970, 1962]).",
            "catchesTypo": true
          },
          {
            "name": "testPeopleOrderedByDisplayName",
            "intent": "people() is ordered ascending by displayName (existing test compares as a Set, losing order).",
            "asserts": "Create people Zed, Alice, Mary; XCTAssertEqual(try a.people().map(\\.displayName), [\"Alice\",\"Mary\",\"Zed\"])."
          },
          {
            "name": "testFindOrCreatePersonTrimsWhitespace",
            "intent": "findOrCreatePerson trims surrounding whitespace/newlines before storing.",
            "asserts": "findOrCreatePerson(named:\"  Eleanor Brun \\n\").displayName == \"Eleanor Brun\"."
          },
          {
            "name": "testFindOrCreatePersonIsIdempotent",
            "intent": "Calling findOrCreatePerson with the same (trimmed) name returns the existing row, not a duplicate.",
            "asserts": "let a1 = findOrCreatePerson(\"Bob\"); let a2 = findOrCreatePerson(\" Bob \"); XCTAssertEqual(a1.id, a2.id); people().count == 1."
          },
          {
            "name": "testSetParticipantsReplacesPrevious",
            "intent": "Re-calling setParticipants overwrites the prior sender/recipients rather than appending.",
            "asserts": "setParticipants sender Alice/[Bob]; then setParticipants sender Carol/[Dan]; participants().sender==Carol, recipients==[Dan] only."
          },
          {
            "name": "testSetPagesReplacesPrevious",
            "intent": "setPages replaces the existing page set (delete-then-insert) used on re-import.",
            "asserts": "setPages [p1,p2]; then setPages [p3]; pages(forLetterId:) returns exactly the single p3 imagePath."
          },
          {
            "name": "testPagesOrderedByPageIndex",
            "intent": "pages(forLetterId:) is ordered by pageIndex (existing test sorts the result, hiding DB ordering).",
            "asserts": "Insert pages with pageIndex 2,0,1 (do not sort in the test); fetched map(\\.pageIndex) == [0,1,2]."
          },
          {
            "name": "testSearchMatchesSummaryBody",
            "intent": "FTS body indexes summary too (transcription + summary joined); a summary-only term is found.",
            "asserts": "Letter(transcription:\"x\", summary:\"elephant parade\"); search(\"elephant\").first?.letterId == letter id."
          },
          {
            "name": "testSearchMatchesTitle",
            "intent": "FTS indexes the title column; a title-only term is found.",
            "asserts": "Letter(title:\"Venice\", transcription:\"nothing\"); search(\"Venice\") returns that letter."
          },
          {
            "name": "testSearchPrefixMatchesLastToken",
            "intent": "ftsPattern prefix-stars the final token, so a partial word matches.",
            "asserts": "transcription \"brown fox\"; search(\"brow\") returns the letter (prefix match)."
          },
          {
            "name": "testSearchMultiTokenIsAnd",
            "intent": "Multiple tokens are implicit-AND: all must be present.",
            "asserts": "transcription \"the quick brown fox\"; search(\"quick fox\") matches; search(\"quick zebra\") is empty."
          },
          {
            "name": "testSearchReindexedAfterEdit",
            "intent": "save() refreshes the FTS row, so old terms stop matching and new ones start.",
            "asserts": "Save letter \"fox\"; re-save with transcription \"cat\"; search(\"fox\") empty; search(\"cat\") returns it."
          },
          {
            "name": "testSearchFindsRecipientName",
            "intent": "setParticipants denormalizes recipient names into the FTS 'names' column (existing test only covers sender).",
            "asserts": "setParticipants sender:A recipients:[\"Persephone\"]; search(\"Persephone\") returns the letter."
          },
          {
            "name": "testDeleteLetterRemovesFromSearch",
            "intent": "deleteLetter explicitly clears the letterSearch FTS row (no cascade covers FTS).",
            "asserts": "Save searchable letter; deleteLetter(id:); search(term) is empty afterward."
          },
          {
            "name": "testSearchEmptyOrPunctuationQueryReturnsEmpty",
            "intent": "ftsPattern returns nil for blank/punctuation-only queries, so search short-circuits to [].",
            "asserts": "search(\"\"), search(\"   \"), search(\"!!!\") each return an empty array (no throw)."
          },
          {
            "name": "testSearchSnippetWrapsMatch",
            "intent": "snippet() uses the « » delimiters defined in the search SQL.",
            "asserts": "transcription containing 'madeleine'; search(\"madeleine\").first?.snippet contains \"«\" and \"»\".",
            "catchesTypo": true
          },
          {
            "name": "testFtsPatternSingleToken",
            "intent": "Pin exact ftsPattern output for one token (quoted + prefix star).",
            "asserts": "XCTAssertEqual(Archive.ftsPattern(from:\"fox\"), \"\\\"fox\\\"*\").",
            "catchesTypo": true,
            "apiNotes": "ftsPattern is static internal on Archive, reachable via @testable."
          },
          {
            "name": "testFtsPatternMultiTokenLastPrefixed",
            "intent": "Pin that only the last token is prefix-matched; earlier tokens are exact-quoted, space-joined.",
            "asserts": "XCTAssertEqual(Archive.ftsPattern(from:\"the quick\"), \"\\\"the\\\" \\\"quick\\\"*\").",
            "catchesTypo": true
          },
          {
            "name": "testFtsPatternSplitsOnPunctuation",
            "intent": "Tokenizer splits on any non-letter/non-number; verify it produces clean tokens.",
            "asserts": "XCTAssertEqual(Archive.ftsPattern(from:\"a-b.c\"), \"\\\"a\\\" \\\"b\\\" \\\"c\\\"*\")."
          },
          {
            "name": "testFtsPatternEmptyAndPunctuationIsNil",
            "intent": "No tokens -> nil (drives the search() empty-result guard).",
            "asserts": "XCTAssertNil(Archive.ftsPattern(from:\"\")); nil for \"   \"; nil for \"!!!\"."
          },
          {
            "name": "testCleanTrimsAndNilsEmpty",
            "intent": "Archive.clean trims whitespace/newlines and maps empty/whitespace-only to nil.",
            "asserts": "clean(\"  x \")==\"x\"; clean(\"\")==nil; clean(\"  \\n \")==nil; clean(nil)==nil.",
            "apiNotes": "static internal func Archive.clean(_:) via @testable."
          },
          {
            "name": "testYearBoundaryValues",
            "intent": "Pin Archive.year boundary behavior (1000..9999 range, 5-digit truncation, leading space).",
            "asserts": "year(\"1000\")==1000; year(\"9999\")==9999; year(\"0999\")==nil; year(\"10000\")==1000; year(\"1962x\")==1962; year(\" 1962\")==nil."
          },
          {
            "name": "testComposedTitleMultipleRecipients",
            "intent": "Recipients are comma-joined in the 'to …' clause.",
            "asserts": "composedTitle(documentType:\"letter\", sender:\"Carol\", recipients:[\"Alice\",\"Bob\"], summary:\"\") == \"Letter to Alice, Bob from Carol\"."
          },
          {
            "name": "testComposedTitleRecipientsOnlyNoSender",
            "intent": "With recipients but blank sender, the 'from' clause is omitted.",
            "asserts": "composedTitle(documentType:\"letter\", sender:\"\", recipients:[\"Bob\"], summary:\"\") == \"Letter to Bob\"."
          },
          {
            "name": "testComposedTitleAllCorrespondenceKinds",
            "intent": "Note/Card/Telegram/Invitation are treated as correspondence (to/from framing), not summary framing.",
            "asserts": "For documentType in note/card/telegram/invitation with sender Alice recipients [Bob]: title == \"<Kind> to Bob from Alice\" (e.g. \"Note to Bob from Alice\").",
            "catchesTypo": true,
            "apiNotes": "Pins the correspondence allow-list [Letter,Postcard,Card,Note,Telegram,Invitation]."
          },
          {
            "name": "testComposedTitleCorrespondenceNoPartiesUsesSummary",
            "intent": "A letter with no sender/recipients falls back to '<Kind> — <summary>'.",
            "asserts": "composedTitle(documentType:\"letter\", sender:\"\", recipients:[], summary:\"about the garden\") == \"Letter — about the garden\"."
          },
          {
            "name": "testComposedTitleSummaryTruncatedToEightWords",
            "intent": "Non-correspondence summary framing truncates to the first 8 words.",
            "asserts": "composedTitle(documentType:\"list\", sender:\"\", recipients:[], summary:\"one two three four five six seven eight nine ten\") == \"List — one two three four five six seven eight\"."
          },
          {
            "name": "testComposedTitleNonCorrespondenceWithSenderReturnsNil",
            "intent": "Non-correspondence with a sender but no summary returns nil (so applyTranscription keeps the existing title).",
            "asserts": "XCTAssertNil(Archive.composedTitle(documentType:\"receipt\", sender:\"Alice\", recipients:[], summary:\"\")).",
            "apiNotes": "composedTitle returns String?; assert nil."
          },
          {
            "name": "testComposedTitleCapitalizesMultiWordType",
            "intent": "document_type is .capitalized (each word) and used verbatim for unknown non-correspondence kinds.",
            "asserts": "composedTitle(documentType:\"diary entry\", sender:\"\", recipients:[], summary:\"\") == \"Diary Entry\"."
          },
          {
            "name": "testComposedTitleTrimsDocumentType",
            "intent": "Surrounding whitespace on document_type is cleaned before capitalization.",
            "asserts": "composedTitle(documentType:\"  postcard  \", sender:\"Alice\", recipients:[\"Bob\"], summary:\"\") == \"Postcard to Bob from Alice\"."
          },
          {
            "name": "testApplyTranscriptionThrowsForMissingLetter",
            "intent": "applyTranscription throws ArchiveError.letterNotFound for an unknown letter id.",
            "asserts": "XCTAssertThrowsError(try a.applyTranscription(r, toLetterId:\"nope\")) { if case Archive.ArchiveError.letterNotFound = $0 {} else { XCTFail() } }.",
            "apiNotes": "ArchiveError is a public nested enum with no payload; match with `if case`, not == (no Equatable synthesized)."
          },
          {
            "name": "testApplyTranscriptionPreservesFirstAiTranscription",
            "intent": "aiTranscription is captured once (first apply) and never overwritten, guaranteeing diffability against the original model output.",
            "asserts": "apply r1(transcription:\"first\"); apply r2(transcription:\"second\"); letter.transcription==\"second\" but letter.aiTranscription==\"first\"."
          },
          {
            "name": "testApplyTranscriptionDateSourceFallsBack",
            "intent": "Invalid date.source coalesces to .unknown; a valid non-schema value like 'manual' is honored.",
            "asserts": "source:\"garbage\" -> dateSource==.unknown; source:\"manual\" -> dateSource==.manual; source:\"written\" -> .written."
          },
          {
            "name": "testApplyTranscriptionConfidenceIsNilForInvalid",
            "intent": "dateConfidence is Confidence(rawValue:) — nil for blank/invalid, set for valid.",
            "asserts": "confidence:\"\" -> dateConfidence==nil; confidence:\"garbage\" -> nil; confidence:\"high\" -> .high."
          },
          {
            "name": "testApplyTranscriptionBlankFieldsBecomeNil",
            "intent": "Blank summary/date/meta values are cleaned to nil rather than stored as empty strings.",
            "asserts": "All-blank r -> updated.summary==nil, dateValue==nil, dateYear==nil, metaLocation==nil, metaRelationship==nil, metaRelationshipState==nil, metaWriterGoals==nil."
          },
          {
            "name": "testApplyTranscriptionJoinsOrNilsNotableQuotes",
            "intent": "notable_quotes join with newline, or nil when empty.",
            "asserts": "notable_quotes:[\"a\",\"b\"] -> notableQuotes==\"a\\nb\"; notable_quotes:[] -> notableQuotes==nil."
          },
          {
            "name": "testApplyTranscriptionCleansRecipients",
            "intent": "Recipient names are cleaned/compacted; blank entries are dropped before setParticipants.",
            "asserts": "recipients:[\"Marcel\",\"  \",\"\"] -> participants().recipients.map(displayName)==[\"Marcel\"]."
          },
          {
            "name": "testApplyTranscriptionKeepsExistingTitleWhenComposedNil",
            "intent": "When the model gives no title and composedTitle returns nil (non-correspondence with a sender), the prior letter title is preserved.",
            "asserts": "Pre-save letter title \"Original\"; apply r(title:\"\", document_type:\"receipt\", sender:\"Alice\", summary:\"\"); updated.title==\"Original\"."
          },
          {
            "name": "testApplyTranscriptionWhitespaceTitleComposes",
            "intent": "A whitespace-only model title is cleaned to nil, so a composed title is used.",
            "asserts": "apply r(title:\"   \", document_type:\"letter\", sender:\"Alice\", recipients:[\"Bob\"]); updated.title==\"Letter to Bob from Alice\"."
          },
          {
            "name": "testTranscriptionResultDateAsBareString",
            "intent": "Exercise the special-case in init where `date` is a JSON string instead of an object.",
            "asserts": "decode {\"transcription\":\"t\",\"date\":\"1850-07\"} -> date.value==\"1850-07\", date.source==\"unknown\", date.confidence==\"\".",
            "catchesTypo": true
          },
          {
            "name": "testTranscriptionResultDateObjectMissingFields",
            "intent": "DateField lenient defaults when sub-fields are absent.",
            "asserts": "decode {\"date\":{\"value\":\"1900\"}} -> value==\"1900\", source==\"unknown\", confidence==\"\"."
          },
          {
            "name": "testTranscriptionResultEmptyObjectDefaults",
            "intent": "An empty JSON object decodes to all defaults without throwing.",
            "asserts": "decode {} -> transcription==\"\", title==\"\", document_type==\"\", recipients==[], notable_quotes==[], date.source==\"unknown\"."
          },
          {
            "name": "testTranscriptionResultWrongTypesFallBack",
            "intent": "Type-mismatched fields fall back to defaults (lenient try? decode), not a thrown error.",
            "asserts": "decode {\"recipients\":\"Bob\",\"date\":42,\"transcription\":5} -> recipients==[], date==default DateField(), transcription==\"\"."
          },
          {
            "name": "testTranscriptionResultUncertainSpansDecode",
            "intent": "uncertain_spans decode populated and empty spans.",
            "asserts": "decode spans [{\"text\":\"x\",\"reason\":\"y\"},{}] -> [UncertainSpan(text:\"x\",reason:\"y\"), UncertainSpan(text:\"\",reason:\"\")]."
          },
          {
            "name": "testTranscriptionResultIgnoresUnknownKeys",
            "intent": "Unknown/future JSON keys are ignored (forward-compatible decode).",
            "asserts": "decode {\"transcription\":\"t\",\"future_field\":123} -> transcription==\"t\" (no throw)."
          },
          {
            "name": "testDateFieldDefaultsPinned",
            "intent": "Pin DateField() defaults — note source defaults to the literal \"unknown\", not \"\".",
            "asserts": "let d = TranscriptionResult.DateField(); d.value==\"\"; d.source==\"unknown\"; d.confidence==\"\".",
            "catchesTypo": true
          },
          {
            "name": "testMetaDefaultsPinned",
            "intent": "Pin TranscriptionResult.Meta() default empty strings.",
            "asserts": "let m = TranscriptionResult.Meta(); m.location==\"\"; m.relationship==\"\"; m.relationship_state==\"\"; m.writer_goals==\"\".",
            "catchesTypo": true
          },
          {
            "name": "testSchemaTopLevelTypeAndAdditionalProperties",
            "intent": "Pin schema root type:object and additionalProperties:false.",
            "asserts": "schema[\"type\"] as? String == \"object\"; schema[\"additionalProperties\"] as? Bool == false.",
            "catchesTypo": true
          },
          {
            "name": "testSchemaRequiredExactSetAndCount",
            "intent": "Pin the EXACT required key set (existing test only checks containment, not the count/extras).",
            "asserts": "Set(schema.required) == Set of the 11 keys [transcription,title,document_type,summary,sender,recipients,date,people_mentioned,notable_quotes,uncertain_spans,meta]; count==11.",
            "catchesTypo": true
          },
          {
            "name": "testSchemaDateSubschemaEnums",
            "intent": "Pin nested date schema: required + the source/confidence enum literal lists.",
            "asserts": "date.required==[value,source,confidence]; date.properties.source.enum==[\"written\",\"inferred\",\"unknown\"]; date.properties.confidence.enum==[\"high\",\"medium\",\"low\"].",
            "catchesTypo": true,
            "apiNotes": "Cast through [String:Any] layers: schema[\"properties\"] -> [\"date\"] -> [\"properties\"] -> [\"source\"] -> [\"enum\"] as [String]."
          },
          {
            "name": "testSchemaMetaSubschemaRequired",
            "intent": "Pin meta sub-schema required keys (must match Meta CodingKeys exactly).",
            "asserts": "meta.required == [\"location\",\"relationship\",\"relationship_state\",\"writer_goals\"]; additionalProperties false.",
            "catchesTypo": true
          },
          {
            "name": "testSchemaUncertainSpanItemsRequired",
            "intent": "Pin uncertain_spans items.required [text,reason].",
            "asserts": "schema.properties.uncertain_spans.items.required == [\"text\",\"reason\"]."
          },
          {
            "name": "testSchemaSourceEnumMatchesDateSourceMinusManual",
            "intent": "Cross-check the schema's date.source enum against the DateSource model (it intentionally excludes 'manual', which only the user sets) and confidence enum against Confidence.",
            "asserts": "Set(date.source.enum) == Set(DateSource.allCases.rawValues) minus [\"manual\"]; Set(date.confidence.enum) == Set(Confidence.allCases.rawValues).",
            "apiNotes": "Guards drift between the model enums and the API schema."
          },
          {
            "name": "testSchemaRequiredMatchesTranscriptionResultKeys",
            "intent": "The schema's required keys should equal TranscriptionResult's decodable fields, so the model is asked for exactly what we decode.",
            "asserts": "Set(schema.required) == the set of the 11 TranscriptionResult CodingKeys (transcription,title,document_type,summary,sender,recipients,date,people_mentioned,uncertain_spans,notable_quotes,meta)."
          },
          {
            "name": "testAnthropicClientDefaultModelConstant",
            "intent": "Pin the default model id (must track the app's chosen Claude model).",
            "asserts": "AnthropicClient().model == \"claude-opus-4-8\".",
            "catchesTypo": true
          },
          {
            "name": "testAnthropicClientCustomModelOverride",
            "intent": "init(model:) stores the override.",
            "asserts": "AnthropicClient(model:\"claude-haiku-x\").model == \"claude-haiku-x\"."
          },
          {
            "name": "testExtractJSONPlainPassthrough",
            "intent": "A bare JSON object is returned unchanged.",
            "asserts": "AnthropicClient.extractJSON(\"{\\\"a\\\":1}\") == \"{\\\"a\\\":1}\".",
            "apiNotes": "extractJSON is static internal; reachable via @testable."
          },
          {
            "name": "testExtractJSONStripsJsonFence",
            "intent": "A ```json fenced block has its fences/language line stripped.",
            "asserts": "extractJSON(\"```json\\n{\\\"a\\\":1}\\n```\") == \"{\\\"a\\\":1}\"."
          },
          {
            "name": "testExtractJSONStripsBareFence",
            "intent": "A ``` fence with no language line is also stripped.",
            "asserts": "extractJSON(\"```\\n{\\\"a\\\":1}\\n```\") == \"{\\\"a\\\":1}\"."
          },
          {
            "name": "testExtractJSONFromSurroundingProse",
            "intent": "Prose around the object is dropped via first-{ to last-} slicing.",
            "asserts": "extractJSON(\"Sure, here: {\\\"a\\\":1} done.\") == \"{\\\"a\\\":1}\"."
          },
          {
            "name": "testExtractJSONNoBracesReturnsTrimmed",
            "intent": "With no braces, the trimmed text is returned (it'll fail JSON decode downstream).",
            "asserts": "extractJSON(\"  hello  \") == \"hello\"."
          },
          {
            "name": "testExtractJSONSpansFirstToLastBrace",
            "intent": "Nested objects: slice runs from the first { to the LAST }.",
            "asserts": "extractJSON(\"{\\\"a\\\":{\\\"b\\\":1}}\") == \"{\\\"a\\\":{\\\"b\\\":1}}\"."
          },
          {
            "name": "testJpegDataThrowsBadImageForNonImage",
            "intent": "jpegData throws AnthropicError.badImage (with the filename) when the source isn't a decodable image.",
            "asserts": "Write 'not-an-image' to temp scan.jpg; XCTAssertThrowsError(try AnthropicClient.jpegData(from: url)); error is .badImage(\"scan.jpg\").",
            "apiNotes": "static throws; uses only a temp file (safe). Match via `if case .badImage(let p) = error { XCTAssertEqual(p, \"scan.jpg\") }`."
          },
          {
            "name": "testAnthropicErrorDescriptions",
            "intent": "Pin every AnthropicError.errorDescription string exactly.",
            "asserts": "missingAPIKey==\"No Anthropic API key set. Add one in Settings.\"; http(503,\"boom\")==\"Anthropic API error 503: boom\"; noContent==\"The model returned no content.\"; badImage(\"p.jpg\")==\"Couldn't read image: p.jpg\".",
            "catchesTypo": true
          },
          {
            "name": "testInstructionMentionsKeyContract",
            "intent": "Light guard that the system instruction still references the structured fields it promises.",
            "asserts": "AnthropicClient.instruction is non-empty and contains \"document_type\", \"uncertain_spans\", and \"JSON\".",
            "apiNotes": "instruction is static let internal."
          },
          {
            "name": "testBackupTranscriptionTxtWithTitle",
            "intent": "transcription.txt = title + blank line + transcription when a title exists.",
            "asserts": "write LetterFile(title:\"Hello\", transcription:\"body\"); read <dir>/<n>/transcription.txt == \"Hello\\n\\nbody\"."
          },
          {
            "name": "testBackupTranscriptionTxtNoTitle",
            "intent": "transcription.txt = just the body when title is nil (compactMap drops nil).",
            "asserts": "write LetterFile(title:nil, transcription:\"body\"); transcription.txt == \"body\"."
          },
          {
            "name": "testBackupEncoderDoesNotEscapeSlashes",
            "intent": "Pin the .withoutEscapingSlashes encoder option (paths/titles must stay human-readable).",
            "asserts": "write a LetterFile with title \"a/b\"; read letter.json raw string and assert it contains \"a/b\" and NOT \"a\\/b\".",
            "catchesTypo": true
          },
          {
            "name": "testBackupEncoderUsesISO8601Dates",
            "intent": "Pin the .iso8601 date strategy of the Backup encoder.",
            "asserts": "write a LetterFile with createdAt Date(timeIntervalSince1970:0); raw letter.json contains \"1970-01-01T00:00:00Z\"."
          },
          {
            "name": "testBackupScanMissingDirReturnsEmpty",
            "intent": "scan() of a non-existent directory returns [] (guarded contentsOfDirectory).",
            "asserts": "Backup.scan(lettersDir: tempDir().appendingPathComponent(\"does-not-exist\")).isEmpty."
          },
          {
            "name": "testBackupScanSkipsDirsWithoutJson",
            "intent": "scan() ignores subfolders lacking letter.json.",
            "asserts": "Create one valid letter folder + an empty junk subfolder; scan() count == 1 (the valid one)."
          },
          {
            "name": "testBackupScanSkipsMalformedJson",
            "intent": "scan() tolerates an unparsable letter.json (try? decode -> nil, skipped).",
            "asserts": "Write '{not json' to <dir>/9/letter.json plus one valid folder; scan() returns only the valid record."
          },
          {
            "name": "testBackupFullRoundTripPreservesAllFields",
            "intent": "write+scan preserves the fields the existing round-trip test doesn't assert (aiTranscription, meta, from/to, date, summary).",
            "asserts": "Round-trip a fully-populated LetterFile; scanned record equals on aiTranscription, meta.location/relationship/relationshipState/writerGoals, from, to, date.value/source/confidence, summary."
          },
          {
            "name": "testBackupPinnedNilRoundTrips",
            "intent": "A nil `pinned` is omitted from JSON and decodes back to nil (Codable optional behavior).",
            "asserts": "write LetterFile(pinned:nil); raw json does NOT contain \"pinned\"; scanned.pinned == nil."
          },
          {
            "name": "testBackupFileMapsLetterToFile",
            "intent": "Archive.backupFile derives LetterFile correctly: page paths -> lastPathComponent, notableQuotes split on \\n, pinned false -> nil.",
            "asserts": "Save letter (notableQuotes:\"q1\\nq2\", pinned:false) + setPages [Letters/3/letter_3_page_1.jpg] + participants A/[B]; backupFile.pages==[\"letter_3_page_1.jpg\"], notableQuotes==[\"q1\",\"q2\"], from==\"A\", to==[\"B\"], pinned==nil."
          },
          {
            "name": "testBackupFilePinnedTrueMaps",
            "intent": "A pinned letter maps to LetterFile.pinned == true.",
            "asserts": "Save pinned letter; backupFile(forLetterId:).pinned == true."
          },
          {
            "name": "testBackupFileNilForMissingLetter",
            "intent": "backupFile returns nil when the letter doesn't exist.",
            "asserts": "XCTAssertNil(try a.backupFile(forLetterId:\"nope\"))."
          },
          {
            "name": "testWriteBackupNoOpForMissingLetter",
            "intent": "writeBackup silently no-ops (no throw, no files) for an unknown id.",
            "asserts": "let dir = tempDir(); try a.writeBackup(forLetterId:\"nope\", lettersDir:dir); contentsOfDirectory(dir) is empty."
          },
          {
            "name": "testImportFromFilesSkipsAlreadyIndexed",
            "intent": "importFromFiles skips ids already present (the `continue`), so it never clobbers existing edits.",
            "asserts": "Pre-save Letter(id:\"x\", title:\"Original\"); importFromFiles([LetterFile(id:\"x\", title:\"New\",…)]); letter(id:\"x\").title still \"Original\"."
          },
          {
            "name": "testImportFromFilesBuildsPagePaths",
            "intent": "Imported pages get relative path \"Letters/<number>/<name>\" and ordered pageIndex.",
            "asserts": "importFromFiles with number:4 pages:[\"letter_4_page_1.jpg\",\"letter_4_page_2.jpg\"]; pages(forLetterId:) imagePaths == [\"Letters/4/letter_4_page_1.jpg\",\"Letters/4/letter_4_page_2.jpg\"] with pageIndex 0,1."
          },
          {
            "name": "testImportFromFilesPinnedNilDefaultsFalse",
            "intent": "A LetterFile with pinned:nil imports as pinned==false.",
            "asserts": "importFromFiles([file with pinned:nil]); letter(id:).pinned == false."
          },
          {
            "name": "testImportFromFilesEmptyNotableQuotesNil",
            "intent": "Empty/absent notableQuotes import as nil (not an empty string).",
            "asserts": "importFromFiles with notableQuotes:[] and another with nil; both -> letter.notableQuotes == nil."
          },
          {
            "name": "testCorrespondenceThreadOldestFirst",
            "intent": "correspondence returns every letter shared by the two principals, ordered dateYear ASC.",
            "asserts": "letter1 (1960) sender A/[B]; letter2 (1955) sender B/[A]; correspondence(forLetterId:letter1.id).map(dateYear) == [1955, 1960]."
          },
          {
            "name": "testCorrespondenceFallbackToSingleLetter",
            "intent": "With no resolvable second principal, correspondence returns just that letter.",
            "asserts": "letter with sender A and no recipients; correspondence(forLetterId:) == [that letter]; for a bogus id returns []."
          },
          {
            "name": "testCorrespondentsExcludesSelfDistinctOrdered",
            "intent": "correspondents returns co-participants, distinct, excluding the person, ordered by displayName.",
            "asserts": "Letter with sender A recipients [C,B]; correspondents(ofPersonId:A.id).map(displayName) == [\"B\",\"C\"] (no A)."
          },
          {
            "name": "testImportImageNoExtensionDefaultsJpg",
            "intent": "Source without an extension is stored as .jpg.",
            "asserts": "importImage from <tmp>/scan (no ext), letterNumber:2, index:0 -> imagePath == \"Letters/2/letter_2_page_1.jpg\"."
          },
          {
            "name": "testImportImagePngExtensionLowercased",
            "intent": "Extension is preserved but lowercased.",
            "asserts": "importImage from scan.PNG, number:2, index:0 -> imagePath ends \"letter_2_page_1.png\"."
          },
          {
            "name": "testImportImageIndexNamingIsOneBased",
            "intent": "Page filename uses index+1 while pageIndex stays the raw 0-based value.",
            "asserts": "importImage(...index:2) -> imagePath ends \"page_3.jpg\" and page.pageIndex == 2."
          },
          {
            "name": "testImportImageOverwritesExistingDest",
            "intent": "Re-importing the same letter/index removes the existing file then copies (no throw).",
            "asserts": "importImage twice (same number/index) with different source bytes; final url(for:) contents equal the SECOND source's bytes; no error thrown."
          },
          {
            "name": "testImageStoreDatabaseURLName",
            "intent": "Pin the sqlite filename.",
            "asserts": "ImageStore(root: tempDir()).databaseURL.lastPathComponent == \"combray.sqlite\".",
            "catchesTypo": true
          },
          {
            "name": "testImageStoreDefaultRootLastComponent",
            "intent": "Pin that defaultRoot ends in the 'Combray' folder (read-only path inspection; does not create it).",
            "asserts": "ImageStore.defaultRoot().lastPathComponent == \"Combray\".",
            "catchesTypo": true,
            "apiNotes": "defaultRoot() only builds a URL; it does not touch the filesystem, so this stays away from the real archive."
          },
          {
            "name": "testImageStoreUrlForPageJoinsRoot",
            "intent": "url(for:) is root + page.imagePath.",
            "asserts": "let s = ImageStore(root:r); url(for: Page(letterId:\"L\", pageIndex:0, imagePath:\"Letters/3/x.jpg\")) == r.appendingPathComponent(\"Letters/3/x.jpg\")."
          },
          {
            "name": "testMakeOnDiskCreatesUsableDatabase",
            "intent": "AppDatabase.makeOnDisk runs migrations on a real file path (in a temp dir, never the real archive) and supports CRUD.",
            "asserts": "let url = tempDir().appendingPathComponent(\"sub/combray.sqlite\"); db = makeOnDisk(at:url); save+fetch a letter via Archive(db); FileManager.fileExists(url.path).",
            "apiNotes": "Uses a unique temp dir only — explicitly NOT ImageStore.defaultRoot(); proves makeOnDisk creates the parent dir too."
          },
          {
            "name": "testDeleteLetterCascadesPagesAndParticipants",
            "intent": "ON DELETE CASCADE removes the letter's page and letterPerson rows when the letter is deleted.",
            "asserts": "Save letter + setPages + setParticipants; deleteLetter; pages(forLetterId:) empty and participants(forLetterId:) sender==nil/recipients==[]; the Person rows themselves still exist in people()."
          },
          {
            "name": "testClaudeAuthPublicConstants",
            "intent": "Pin the OAuth client_id, console redirect, scope, and loopback redirect format.",
            "asserts": "ClaudeAuth.clientID==\"9d1c250a-e61b-44d9-88ed-5944d1962f5e\"; consoleRedirect==\"https://console.anthropic.com/oauth/code/callback\"; scope==\"org:create_api_key user:profile user:inference\"; loopbackRedirect(port:1234)==\"http://localhost:1234/callback\".",
            "catchesTypo": true,
            "apiNotes": "scope/authorizeBase/tokenURL are internal; clientID/consoleRedirect/loopbackRedirect are public — all reachable via @testable."
          },
          {
            "name": "testMakePKCEChallengeIsSha256OfVerifier",
            "intent": "PKCE challenge must be base64URL(SHA256(verifier)); verifier/state non-empty and URL-safe.",
            "asserts": "let p = ClaudeAuth.makePKCE(); challenge == Data(SHA256.hash(data: Data(p.verifier.utf8))).base64URLEncodedString(); none of verifier/challenge/state contains '+','/','='.",
            "apiNotes": "Test target must import CryptoKit to recompute the digest."
          },
          {
            "name": "testAuthorizeURLQueryItems",
            "intent": "Pin the authorize URL query parameters and their fixed values.",
            "asserts": "authorizeURL(pkce) components query items include code=true, client_id=<clientID>, response_type=code, redirect_uri=<consoleRedirect default>, scope=<scope>, code_challenge=<pkce.challenge>, code_challenge_method=S256, state=<pkce.state>.",
            "catchesTypo": true
          },
          {
            "name": "testAuthorizeURLHonorsCustomRedirect",
            "intent": "A custom redirectURI overrides the console default in the URL.",
            "asserts": "authorizeURL(pkce, redirectURI:\"http://localhost:9/callback\") has redirect_uri == \"http://localhost:9/callback\"."
          },
          {
            "name": "testBase64URLEncoding",
            "intent": "Pin the Data.base64URLEncodedString transform: '+'->'-', '/'->'_', strip '='.",
            "asserts": "Data([0xFB,0xEF,0xFF]).base64URLEncodedString() == \"--__\"; Data([0x00]).base64URLEncodedString() == \"AA\".",
            "catchesTypo": true,
            "apiNotes": "Internal extension on Data; reachable via @testable. (0xFB,0xEF,0xFF -> base64 \"++//\"; single 0x00 -> \"AA==\" with padding stripped.)"
          },
          {
            "name": "testStoredCredentialIsExpiredEarlyRefreshWindow",
            "intent": "Pin the 60-second early-refresh window in isExpired (not covered by the existing +3600/-10 test).",
            "asserts": "expiresAt = now+30 -> isExpired true; now+120 -> false; expiresAt == now exactly -> true; expiresAt nil -> false."
          },
          {
            "name": "testStoredCredentialKindRawValues",
            "intent": "Pin StoredCredential.Kind rawValues (serialized into credentials.json).",
            "asserts": "StoredCredential.Kind.apiKey.rawValue==\"apiKey\"; .oauth.rawValue==\"oauth\".",
            "catchesTypo": true
          },
          {
            "name": "testStoredCredentialApiKeyRoundTripNilTokens",
            "intent": "An apiKey credential Codable round-trips, leaving token fields nil (existing test only covers the oauth shape).",
            "asserts": "Encode/decode StoredCredential(kind:.apiKey, apiKey:\"k\"); back.kind==.apiKey, back.apiKey==\"k\", back.accessToken==nil, back.refreshToken==nil, back.expiresAt==nil."
          },
          {
            "name": "testSchemaVersionIsExactlyOne",
            "intent": "Pin the exact schema version (existing test only checks > 0); bumping a migration should be deliberate.",
            "asserts": "XCTAssertEqual(Combray.schemaVersion, 1).",
            "catchesTypo": true
          }
        ]
      },
      {
        "module": "CombrayCore",
        "newTests": [
          {
            "name": "testSchemaVersionIsExactlyOne",
            "intent": "Existing test only checks >0; pin the exact constant so a bump or typo is caught.",
            "asserts": "XCTAssertEqual(Combray.schemaVersion, 1)",
            "catchesTypo": true
          },
          {
            "name": "testMigrationCreatesAllTables",
            "intent": "Pin the migrated table names at the DB level so renaming a table in AppDatabase's migration fails a test.",
            "asserts": "For each of person, relationship, letter, page, letterPerson, letterSearch: database.reader.read { try db.execute(sql: \"SELECT 1 FROM <t> LIMIT 0\") } does not throw.",
            "apiNotes": "database and database.dbWriter/reader are public on AppDatabase; use the in-memory archive's a.database.reader.",
            "catchesTypo": true
          },
          {
            "name": "testMakeOnDiskCreatesFileAndRoundTrips",
            "intent": "Cover the disk factory (currently only makeInMemory is exercised) without touching the real archive.",
            "asserts": "makeOnDisk(at: tempDir/combray.sqlite) returns a DB; save+fetch a Letter round-trips; FileManager.fileExists at that path is true.",
            "apiNotes": "Use a unique tempDir() URL ending in combray.sqlite; never ImageStore.defaultRoot(). Wrap in Archive(...) to save."
          },
          {
            "name": "testDeleteLetterCascadesPagesParticipantsAndSearch",
            "intent": "Existing testDeleteLetter only checks the letter is gone; verify ON DELETE CASCADE plus the explicit FTS cleanup.",
            "asserts": "After setPages+setParticipants then deleteLetter: pages(forLetterId:) is empty, participants(forLetterId:) == (nil, []), and search(<a body word>) is empty."
          },
          {
            "name": "testSaveBumpsUpdatedAtAndPreservesCreatedAt",
            "intent": "save() sets updatedAt = Date() but must not touch createdAt.",
            "asserts": "Save a Letter built with createdAt far in the past and an equally old updatedAt; returned.updatedAt > returned.createdAt and createdAt is unchanged.",
            "apiNotes": "Build Letter(createdAt: Date(timeIntervalSince1970: 0), updatedAt: Date(timeIntervalSince1970: 0))."
          },
          {
            "name": "testSaveIsUpsertNotDuplicate",
            "intent": "save() of an existing id should update in place, not insert a second row.",
            "asserts": "Save letter, mutate title, save again; allLetters().count == 1 and letter(id:).title is the new title."
          },
          {
            "name": "testLetterReturnsNilForUnknownId",
            "intent": "Lookup miss path.",
            "asserts": "XCTAssertNil(try a.letter(id: \"does-not-exist\"))"
          },
          {
            "name": "testNextLetterNumberIsMaxPlusOneNotCount",
            "intent": "nextLetterNumber uses MAX(number)+1; prove it with a gap so count/last can't pass by accident.",
            "asserts": "Save Letter(number:5) and Letter(number:2); nextLetterNumber() == 6.",
            "catchesTypo": true
          },
          {
            "name": "testAllLettersNullYearSortsLastWithCreatedAtTiebreak",
            "intent": "Pin the 'dateYear DESC NULLS LAST, createdAt DESC' ordering, including nil-year placement and the createdAt tiebreak the existing test never exercises.",
            "asserts": "Save dateYear:2000 (createdAt old), then two nil-year letters with createdAt early and late; allLetters() == [the 2000 letter, late-nil, early-nil].",
            "apiNotes": "Control order with explicit createdAt values since save() only overwrites updatedAt."
          },
          {
            "name": "testLettersForYearFiltersAndOrdersByCreatedAtDesc",
            "intent": "letters(forYear:) is untested; verify filtering plus createdAt DESC order.",
            "asserts": "Two 1962 letters (distinct createdAt) + one 1970; letters(forYear:1962) returns exactly the two, newest createdAt first."
          },
          {
            "name": "testLettersForYearEmptyWhenNoMatch",
            "intent": "Empty result path for letters(forYear:).",
            "asserts": "With only a 1970 letter, letters(forYear:1800).isEmpty"
          },
          {
            "name": "testLettersForPersonIdOrdersOldestFirst",
            "intent": "letters(forPersonId:) is untested; verify the join and 'dateYear ASC, createdAt ASC' order.",
            "asserts": "Attach one person to letters dated 1970 and 1962; letters(forPersonId:) maps to dateYear [1962, 1970].",
            "apiNotes": "Get the personId from participants(forLetterId:) after setParticipants."
          },
          {
            "name": "testYearsAreDistinctAndDescending",
            "intent": "Existing testPeopleAndYears sorts the result, hiding both ordering and de-duplication; pin DESC + DISTINCT.",
            "asserts": "Save letters dated 1962, 1962, 1970; years() == [1970, 1962] (no duplicate 1962, newest first).",
            "catchesTypo": true
          },
          {
            "name": "testYearsEmptyWhenNoDatedLetters",
            "intent": "years() must exclude NULL dateYear.",
            "asserts": "Save a letter with dateYear nil; years().isEmpty"
          },
          {
            "name": "testSetPagesReplacesExistingSet",
            "intent": "setPages deletes the prior set before inserting; existing test only inserts once.",
            "asserts": "setPages of 2 pages, then setPages of 1 page; pages(forLetterId:).count == 1."
          },
          {
            "name": "testPagesOrderedByPageIndex",
            "intent": "pages() orders by pageIndex; existing test sorts the result so order is untested.",
            "asserts": "Insert pages with pageIndex 2,0,1 (unsorted); pages(forLetterId:).map(\\.pageIndex) == [0,1,2]."
          },
          {
            "name": "testFindOrCreatePersonDeduplicatesByExactName",
            "intent": "findOrCreatePerson reuses an existing row for an identical name.",
            "asserts": "Call twice with \"Alice\"; both return same id and people().count == 1.",
            "apiNotes": "findOrCreatePerson is public @discardableResult -> Person."
          },
          {
            "name": "testFindOrCreatePersonTrimsWhitespace",
            "intent": "Names are trimmed before lookup/insert, so padded and unpadded names collapse to one person.",
            "asserts": "findOrCreatePerson(named: \"  Alice  \").displayName == \"Alice\"; a second call with \"Alice\" returns the same id."
          },
          {
            "name": "testPeopleOrderedByDisplayName",
            "intent": "people() orders alphabetically; untested.",
            "asserts": "Create Carol, Alice, Bob; people().map(\\.displayName) == [\"Alice\",\"Bob\",\"Carol\"]."
          },
          {
            "name": "testSetParticipantsNilSenderKeepsRecipients",
            "intent": "sender is optional; nil sender must not insert a sender row.",
            "asserts": "setParticipants(sender: nil, recipients: [\"X\"]); participants.sender == nil and recipients map to [\"X\"]."
          },
          {
            "name": "testSetParticipantsReplacesPreviousAssignment",
            "intent": "setParticipants clears prior LetterPerson rows first.",
            "asserts": "Set sender Alice, then set sender Bob; participants.sender?.displayName == \"Bob\" (not Alice)."
          },
          {
            "name": "testParticipantsRecipientsOrderedByDisplayName",
            "intent": "recipients query has ORDER BY person.displayName.",
            "asserts": "setParticipants(recipients: [\"Zoe\",\"Amy\"]); participants.recipients.map(\\.displayName) == [\"Amy\",\"Zoe\"]."
          },
          {
            "name": "testSearchMultiTokenRequiresAllTerms",
            "intent": "ftsPattern joins tokens with implicit AND; both terms must be present.",
            "asserts": "Letter body \"quick brown fox\": search(\"brown fox\") returns it; search(\"brown zzz\") is empty."
          },
          {
            "name": "testSearchPrefixMatchesLastToken",
            "intent": "The last token is prefix-matched with *.",
            "asserts": "Body contains \"transcription\": search(\"transcrip\") returns the letter."
          },
          {
            "name": "testSearchMatchesTitle",
            "intent": "title is an indexed FTS column distinct from body/names.",
            "asserts": "Letter with title \"Venice\" and unrelated body; search(\"Venice\") returns it."
          },
          {
            "name": "testSearchMatchesSummary",
            "intent": "refreshSearchIndex folds summary into the body column; pin that summary is searchable.",
            "asserts": "Letter with a unique word only in summary; search(<that word>) returns it.",
            "apiNotes": "Set summary via a saved Letter (summary param) before searching."
          },
          {
            "name": "testSearchSnippetWrapsMatchInGuillemets",
            "intent": "The snippet() call uses « » delimiters on the body column.",
            "asserts": "Body \"the quick brown fox\"; search(\"fox\").first?.snippet contains \"«\" and \"»\"."
          },
          {
            "name": "testSearchEmptyAndPunctuationOnlyQueryReturnsEmpty",
            "intent": "ftsPattern returns nil (→ []) when there are no alphanumeric tokens; guards against passing a bad MATCH to SQLite.",
            "asserts": "search(\"\").isEmpty and search(\"  --!! \").isEmpty"
          },
          {
            "name": "testFtsPatternSingleTokenIsQuotedAndPrefixed",
            "intent": "Pin the exact MATCH string shape for one token.",
            "asserts": "Archive.ftsPattern(from: \"fox\") == \"\\\"fox\\\"*\"",
            "apiNotes": "ftsPattern is internal static on Archive, reachable via @testable.",
            "catchesTypo": true
          },
          {
            "name": "testFtsPatternMultiTokenAndsAndPrefixesLastOnly",
            "intent": "Only the final token gets the * prefix; others are bare quoted terms joined by space.",
            "asserts": "Archive.ftsPattern(from: \"quick brown\") == \"\\\"quick\\\" \\\"brown\\\"*\"",
            "catchesTypo": true
          },
          {
            "name": "testFtsPatternSplitsOnPunctuation",
            "intent": "Tokenizer splits on any non-letter/non-number.",
            "asserts": "Archive.ftsPattern(from: \"a,b.c\") == \"\\\"a\\\" \\\"b\\\" \\\"c\\\"*\""
          },
          {
            "name": "testFtsPatternNilForNoTokens",
            "intent": "Empty / punctuation-only input yields nil, not an empty pattern.",
            "asserts": "XCTAssertNil(Archive.ftsPattern(from: \"\")) and XCTAssertNil(Archive.ftsPattern(from: \"  -- \"))"
          },
          {
            "name": "testApplyTranscriptionThrowsForUnknownLetter",
            "intent": "Error path: applyTranscription on a missing letter throws ArchiveError.letterNotFound.",
            "asserts": "XCTAssertThrowsError(try a.applyTranscription(r, toLetterId: \"nope\")) and the thrown error is Archive.ArchiveError.letterNotFound.",
            "apiNotes": "ArchiveError is public; switch/cast the caught error."
          },
          {
            "name": "testApplyTranscriptionPreservesOriginalAITranscription",
            "intent": "Non-obvious gotcha: aiTranscription is set only when nil, so re-applying a corrected transcription must NOT overwrite the original model output.",
            "asserts": "Apply r1(transcription:\"v1\"); apply r2(transcription:\"v2\") to same letter; updated.transcription == \"v2\" but updated.aiTranscription == \"v1\"."
          },
          {
            "name": "testApplyTranscriptionInvalidDateSourceFallsBackToUnknown",
            "intent": "DateSource(rawValue:) ?? .unknown for an unrecognized source.",
            "asserts": "date.source \"garbage\" → updated.dateSource == .unknown."
          },
          {
            "name": "testApplyTranscriptionInvalidConfidenceIsNil",
            "intent": "Confidence(rawValue:) returns nil for an unrecognized value (no default).",
            "asserts": "date.confidence \"garbage\" → updated.dateConfidence == nil."
          },
          {
            "name": "testApplyTranscriptionValidConfidenceParsed",
            "intent": "Valid confidence maps to the enum case.",
            "asserts": "date.confidence \"medium\" → updated.dateConfidence == .medium."
          },
          {
            "name": "testApplyTranscriptionJoinsNotableQuotesWithNewlines",
            "intent": "notable_quotes array is stored newline-joined.",
            "asserts": "notable_quotes [\"a\",\"b\"] → updated.notableQuotes == \"a\\nb\"."
          },
          {
            "name": "testApplyTranscriptionEmptyQuotesStoreNil",
            "intent": "Empty notable_quotes becomes nil, not \"\".",
            "asserts": "notable_quotes [] → updated.notableQuotes == nil."
          },
          {
            "name": "testApplyTranscriptionBlankFieldsBecomeNilViaClean",
            "intent": "Whitespace-only summary/meta are cleaned to nil.",
            "asserts": "summary \"  \" and meta.location \"  \" → updated.summary == nil and updated.metaLocation == nil."
          },
          {
            "name": "testApplyTranscriptionDropsBlankRecipients",
            "intent": "recipients are compactMap(clean)'d, so blank entries are dropped before becoming people.",
            "asserts": "recipients [\"Bob\",\"  \"] → participants.recipients has exactly one person named \"Bob\"."
          },
          {
            "name": "testApplyTranscriptionWhitespaceTitleComposes",
            "intent": "A whitespace-only title is cleaned to nil and triggers composedTitle (existing test only covers empty string).",
            "asserts": "title \"   \", sender Alice, recipients [Bob], document_type letter → updated.title == \"Letter to Bob from Alice\"."
          },
          {
            "name": "testComposedTitleJoinsMultipleRecipients",
            "intent": "Recipients are comma-joined in the title.",
            "asserts": "Archive.composedTitle(documentType:\"letter\", sender:\"Alice\", recipients:[\"Bob\",\"Carol\"], summary:\"\") == \"Letter to Bob, Carol from Alice\"."
          },
          {
            "name": "testComposedTitleRecipientsOnlyNoSender",
            "intent": "With recipients but blank sender, the 'from' clause is omitted.",
            "asserts": "Archive.composedTitle(documentType:\"letter\", sender:\"\", recipients:[\"Bob\"], summary:\"\") == \"Letter to Bob\"."
          },
          {
            "name": "testComposedTitleCorrespondenceKindsAndCapitalization",
            "intent": "Pin the correspondence whitelist (Letter, Postcard, Card, Note, Telegram, Invitation) and the .capitalized of document_type; a typo in any whitelist entry flips it to non-correspondence.",
            "asserts": "For (\"telegram\"->\"Telegram\"), (\"note\"->\"Note\"), (\"card\"->\"Card\"), (\"invitation\"->\"Invitation\") with sender Alice/recipient Bob, result == \"<Kind> to Bob from Alice\".",
            "catchesTypo": true
          },
          {
            "name": "testComposedTitleNonCorrespondenceTruncatesSummaryToEightWords",
            "intent": "Non-correspondence kinds use 'Kind — <first 8 summary words>'.",
            "asserts": "documentType \"list\", summary of 10 words → \"List — <first 8 words joined by space>\" (exactly 8 words, no ellipsis)."
          },
          {
            "name": "testComposedTitleNonCorrespondenceWithSenderReturnsNil",
            "intent": "Subtle branch: a non-correspondence doc with a sender but no summary returns nil (so applyTranscription falls back to the existing title).",
            "asserts": "XCTAssertNil(Archive.composedTitle(documentType:\"receipt\", sender:\"Alice\", recipients:[], summary:\"\"))."
          },
          {
            "name": "testComposedTitleCorrespondenceSummaryOnlyFallback",
            "intent": "Correspondence kind with no sender and no recipients but a summary uses the 'Kind — summary' fallback.",
            "asserts": "Archive.composedTitle(documentType:\"letter\", sender:\"\", recipients:[], summary:\"hello world\") == \"Letter — hello world\"."
          },
          {
            "name": "testComposedTitleUppercaseAndPaddedTypeNormalized",
            "intent": "document_type is trimmed then .capitalized before matching.",
            "asserts": "Archive.composedTitle(documentType:\"  POSTCARD  \", sender:\"Alice\", recipients:[\"Bob\"], summary:\"\") == \"Postcard to Bob from Alice\"."
          },
          {
            "name": "testCleanTrimsAndNilsBlank",
            "intent": "Pin the clean() helper contract directly.",
            "asserts": "Archive.clean(nil)==nil, Archive.clean(\"\")==nil, Archive.clean(\"  \\n \")==nil, Archive.clean(\" hi \")==\"hi\".",
            "apiNotes": "clean is internal static on Archive."
          },
          {
            "name": "testYearBoundaryValues",
            "intent": "Pin the 1000...9999 acceptance window and the prefix(4) rule that existing tests don't cover.",
            "asserts": "Archive.year(from:\"1000\")==1000, year(\"9999\")==9999, year(\"0999\")==nil, year(\"10000\")==1000, year(\"12a4\")==nil, year(\" 1962\")==nil.",
            "catchesTypo": true
          },
          {
            "name": "testDateSourceRawValuesAndCaseCount",
            "intent": "Typo guard on the DateSource enum (these rawValues are persisted in SQLite and JSON).",
            "asserts": "written/inferred/manual/unknown rawValues equal their names; DateSource.allCases.count == 4.",
            "catchesTypo": true
          },
          {
            "name": "testConfidenceRawValuesAndCaseCount",
            "intent": "Typo guard on Confidence rawValues.",
            "asserts": "high/medium/low rawValues equal their names; Confidence.allCases.count == 3.",
            "catchesTypo": true
          },
          {
            "name": "testPersonRoleRawValuesAndCaseCount",
            "intent": "Typo guard on PersonRole rawValues used in the letterPerson.role column and queries.",
            "asserts": "sender/recipient rawValues equal their names; PersonRole.allCases.count == 2.",
            "catchesTypo": true
          },
          {
            "name": "testDatabaseTableNames",
            "intent": "Pin the GRDB databaseTableName for every record; a rename here without a matching migration would corrupt queries.",
            "asserts": "Person.databaseTableName==\"person\", Letter==\"letter\", Page==\"page\", LetterPerson==\"letterPerson\", Relationship==\"relationship\".",
            "catchesTypo": true
          },
          {
            "name": "testLetterInitDefaults",
            "intent": "Pin the Letter initializer defaults (number 0, .unknown source, empty transcription, pinned false, nil optionals).",
            "asserts": "let l = Letter(); l.number==0, l.dateSource == .unknown, l.transcription==\"\", l.pinned==false, l.title==nil, l.dateConfidence==nil."
          },
          {
            "name": "testLetterCodableRoundTrip",
            "intent": "Letter is Codable; ensure a full round-trip is stable.",
            "asserts": "JSONDecoder().decode(Letter.self, JSONEncoder().encode(l)) == l for a fully-populated Letter."
          },
          {
            "name": "testTranscriptionResultDateAsBareString",
            "intent": "Lenient branch the existing tests miss: date given as a plain string rather than an object.",
            "asserts": "decode {\"date\":\"1962-03\"} → result.date.value == \"1962-03\" and result.date.source == \"unknown\".",
            "catchesTypo": true
          },
          {
            "name": "testTranscriptionResultDateObjectDefaults",
            "intent": "DateField defaults: missing source defaults to \"unknown\", value/confidence to \"\".",
            "asserts": "decode {\"date\":{}} → date.value==\"\", date.source==\"unknown\", date.confidence==\"\".",
            "catchesTypo": true
          },
          {
            "name": "testTranscriptionResultWrongTypedFieldsFallBackToDefaults",
            "intent": "Lenient decode swallows type mismatches per-field instead of throwing.",
            "asserts": "decode {\"title\":123,\"recipients\":\"x\",\"notable_quotes\":5,\"transcription\":\"t\"} → title==\"\", recipients==[], notable_quotes==[], transcription==\"t\"."
          },
          {
            "name": "testTranscriptionResultUncertainSpansDecodeAndDefault",
            "intent": "UncertainSpan decoding (the full-object existing test never asserts span contents) plus its empty defaults.",
            "asserts": "Full span {\"text\":\"x\",\"reason\":\"y\"} → first.text==\"x\", first.reason==\"y\"; {\"uncertain_spans\":[{}]} → first.text==\"\", first.reason==\"\"."
          },
          {
            "name": "testTranscriptionResultPeopleMentioned",
            "intent": "people_mentioned decode + default.",
            "asserts": "decode {\"people_mentioned\":[\"Odette\"]} → [\"Odette\"]; missing → []."
          },
          {
            "name": "testTranscriptionResultMetaDefaultsEmpty",
            "intent": "Missing meta yields a Meta() with all empty strings.",
            "asserts": "decode {\"transcription\":\"t\"} → meta.location/relationship/relationship_state/writer_goals all == \"\"."
          },
          {
            "name": "testSchemaDateEnumsMatchModelRawValues",
            "intent": "Cross-check that the JSON-schema date enums equal the Swift DateSource/Confidence rawValues — a divergence would silently let the model emit values the app can't parse.",
            "asserts": "schema.properties.date.properties.source.enum == [\"written\",\"inferred\",\"unknown\"] == DateSource.allCases.map(\\.rawValue); confidence.enum == [\"high\",\"medium\",\"low\"] == Confidence.allCases.map(\\.rawValue).",
            "apiNotes": "Schema is [String:Any]; downcast nested dictionaries/arrays step by step.",
            "catchesTypo": true
          },
          {
            "name": "testSchemaDateRequiresValueSourceConfidence",
            "intent": "Pin the nested date object's required keys.",
            "asserts": "schema...date.required == [\"value\",\"source\",\"confidence\"] (as a set).",
            "catchesTypo": true
          },
          {
            "name": "testSchemaMetaRequiredKeys",
            "intent": "Pin meta required keys (the bug the existing SchemaTests comment references: additionalProperties:false once dropped these).",
            "asserts": "schema...meta.required as a set == [\"location\",\"relationship\",\"relationship_state\",\"writer_goals\"].",
            "catchesTypo": true
          },
          {
            "name": "testSchemaUncertainSpanItemRequired",
            "intent": "Pin the uncertain_spans item-object required keys.",
            "asserts": "schema...uncertain_spans.items.required as a set == [\"text\",\"reason\"].",
            "catchesTypo": true
          },
          {
            "name": "testSchemaRootIsClosedObject",
            "intent": "Pin type:object and additionalProperties:false at the root.",
            "asserts": "schema[\"type\"] as? String == \"object\" and schema[\"additionalProperties\"] as? Bool == false.",
            "catchesTypo": true
          },
          {
            "name": "testSchemaRecipientsIsArrayOfStrings",
            "intent": "Pin the recipients array typing.",
            "asserts": "schema.properties.recipients.type == \"array\" and its items.type == \"string\"."
          },
          {
            "name": "testAnthropicClientDefaultModelConstant",
            "intent": "Pin the literal default model id in AnthropicClient.init so a change is intentional.",
            "asserts": "AnthropicClient().model == \"claude-opus-4-8\".",
            "apiNotes": "Pins the source literal only; no network call.",
            "catchesTypo": true
          },
          {
            "name": "testAnthropicErrorDescriptions",
            "intent": "Pin the user-facing error strings (shown in Settings/alerts) so a wording typo is caught.",
            "asserts": ".missingAPIKey.errorDescription == \"No Anthropic API key set. Add one in Settings.\"; .noContent == \"The model returned no content.\"; .http(404,\"x\").errorDescription contains \"Anthropic API error 404: x\"; .badImage(\"p.jpg\") contains \"Couldn't read image: p.jpg\".",
            "catchesTypo": true
          },
          {
            "name": "testExtractJSONPassesThroughPlainObject",
            "intent": "extractJSON returns bare JSON unchanged.",
            "asserts": "AnthropicClient.extractJSON(\"{\\\"a\\\":1}\") == \"{\\\"a\\\":1}\".",
            "apiNotes": "extractJSON is internal static on AnthropicClient."
          },
          {
            "name": "testExtractJSONStripsJsonFence",
            "intent": "Handles a ```json fenced block.",
            "asserts": "extractJSON(\"```json\\n{\\\"a\\\":1}\\n```\") == \"{\\\"a\\\":1}\"."
          },
          {
            "name": "testExtractJSONStripsBareFence",
            "intent": "Handles a ``` fence with no language tag.",
            "asserts": "extractJSON(\"```\\n{\\\"a\\\":1}\\n```\") == \"{\\\"a\\\":1}\"."
          },
          {
            "name": "testExtractJSONTrimsSurroundingProse",
            "intent": "Falls back to first { … last } slice when prose surrounds the object.",
            "asserts": "extractJSON(\"Here you go: {\\\"a\\\":1} thanks\") == \"{\\\"a\\\":1}\"."
          },
          {
            "name": "testExtractJSONKeepsNestedBraces",
            "intent": "Uses lastIndex of } so nested objects survive.",
            "asserts": "extractJSON(\"{\\\"a\\\":{\\\"b\\\":1}}\") == \"{\\\"a\\\":{\\\"b\\\":1}}\"."
          },
          {
            "name": "testExtractJSONNoBracesReturnsTrimmed",
            "intent": "With no braces, returns the trimmed input unchanged.",
            "asserts": "extractJSON(\"  no json here  \") == \"no json here\"."
          },
          {
            "name": "testJpegDataThrowsBadImageForNonImage",
            "intent": "jpegData throws AnthropicError.badImage when NSImage can't read the file (safe to test with a temp text file).",
            "asserts": "Write \"not an image\" to a temp .jpg; XCTAssertThrowsError(try AnthropicClient.jpegData(from: url)) and the error is AnthropicError.badImage.",
            "apiNotes": "jpegData is internal static; uses AppKit which is available in the macOS test bundle."
          },
          {
            "name": "testBackupWriteTranscriptionTxtContent",
            "intent": "transcription.txt is title + blank line + transcription.",
            "asserts": "After Backup.write with title \"T\" and transcription \"body\", transcription.txt content == \"T\\n\\nbody\".",
            "apiNotes": "Read the file from lettersDir/<number>/transcription.txt in a tempDir()."
          },
          {
            "name": "testBackupWriteTranscriptionTxtNilTitleOmitsTitle",
            "intent": "compactMap drops a nil title so the file is just the transcription.",
            "asserts": "With title nil, transcription \"body\", transcription.txt content == \"body\"."
          },
          {
            "name": "testBackupScanEmptyDirReturnsEmpty",
            "intent": "scan tolerates an empty/nonexistent letters dir.",
            "asserts": "Backup.scan(lettersDir: freshTempDir).isEmpty and Backup.scan(lettersDir: nonexistentURL).isEmpty"
          },
          {
            "name": "testBackupScanIgnoresInvalidAndMissingJSON",
            "intent": "scan skips subfolders without letter.json and those with unparseable JSON (compactMap).",
            "asserts": "Create one folder with no letter.json and one with garbage letter.json; Backup.scan returns []. With one valid folder added, returns exactly that one."
          },
          {
            "name": "testBackupRoundTripPreservesAllFields",
            "intent": "Full write→scan round-trip preserves date, meta, aiTranscription, from/to, and dates (existing test only spot-checks a few).",
            "asserts": "Write a fully-populated LetterFile (non-nil meta, aiTranscription, date, dates); scanned.first equals on number,id,title,date.value/source/confidence,from,to,meta.location,aiTranscription,createdAt,updatedAt."
          },
          {
            "name": "testBackupJSONUsesISO8601Dates",
            "intent": "Pin the encoder's iso8601 date strategy.",
            "asserts": "With createdAt = Date(timeIntervalSince1970: 0), letter.json text contains \"1970-01-01T00:00:00Z\".",
            "catchesTypo": true
          },
          {
            "name": "testBackupPinnedNilOmittedFromJSON",
            "intent": "A nil pinned must be absent from letter.json and decode back as nil (back-compat with old records).",
            "asserts": "Write LetterFile with pinned nil; letter.json text does not contain \"pinned\"; Backup.scan().first?.pinned == nil."
          },
          {
            "name": "testBackupFileMapsArchiveFields",
            "intent": "Archive.backupFile builds the LetterFile: page imagePaths reduced to lastPathComponent, pinned false→nil, notableQuotes split on newline.",
            "asserts": "For a saved letter with pages imagePath \"Letters/3/letter_3_page_1.jpg\", pinned false, notableQuotes \"a\\nb\": backupFile.pages==[\"letter_3_page_1.jpg\"], .pinned==nil, .notableQuotes==[\"a\",\"b\"]."
          },
          {
            "name": "testBackupFileTrueWhenPinned",
            "intent": "pinned true maps to Optional(true) (the ternary's other branch).",
            "asserts": "Saved letter with pinned true → backupFile.pinned == true."
          },
          {
            "name": "testBackupFileNilForUnknownId",
            "intent": "backupFile returns nil for a missing letter.",
            "asserts": "XCTAssertNil(try a.backupFile(forLetterId: \"nope\"))."
          },
          {
            "name": "testWriteBackupNoOpForUnknownId",
            "intent": "writeBackup silently returns (no throw, no files) when the letter doesn't exist.",
            "asserts": "try a.writeBackup(forLetterId:\"nope\", lettersDir: tempDir) does not throw and creates no <number> folder."
          },
          {
            "name": "testImportFromFilesSkipsAlreadyIndexed",
            "intent": "importFromFiles is idempotent: an already-present id is skipped, not overwritten.",
            "asserts": "Import file id1 title \"A\"; import file id1 title \"B\"; letter(id:\"id1\").title remains \"A\"."
          },
          {
            "name": "testImportFromFilesBuildsPagePaths",
            "intent": "Page imagePath is composed as \"Letters/<number>/<name>\".",
            "asserts": "Import a file number 7 with pages [\"letter_7_page_1.jpg\"]; pages(forLetterId:).first?.imagePath == \"Letters/7/letter_7_page_1.jpg\"."
          },
          {
            "name": "testImportFromFilesPinnedNilDefaultsFalse",
            "intent": "A nil pinned in the file becomes false on the Letter.",
            "asserts": "Import a file with pinned nil; letter(id:).pinned == false."
          },
          {
            "name": "testImportFromFilesDerivesYearFromDateValue",
            "intent": "dateYear is recomputed from date.value during import.",
            "asserts": "Import a file with date.value \"1962-03\"; letter(id:).dateYear == 1962."
          },
          {
            "name": "testImportImageDefaultsExtensionToJpg",
            "intent": "A source URL with no extension is named .jpg.",
            "asserts": "importImage(from: tempDir/\"scan\" with no extension, ...) → page.imagePath ends with \"letter_1_page_1.jpg\"."
          },
          {
            "name": "testImportImagePreservesLowercasedExtension",
            "intent": "Existing test covers .JPG→jpg; cover a different non-jpeg extension preserved lowercased.",
            "asserts": "importImage from a \"page.PNG\" source → page.imagePath ends with \"letter_1_page_1.png\"."
          },
          {
            "name": "testImportImageIndexNaming",
            "intent": "Filename uses index+1 while pageIndex stores index.",
            "asserts": "importImage(..., index: 2) → imagePath ends with \"letter_4_page_3.jpg\" (letterNumber 4) and page.pageIndex == 2."
          },
          {
            "name": "testImportImageOverwritesExistingDestination",
            "intent": "Re-importing the same index removes the old file first, leaving the new bytes.",
            "asserts": "Import bytes \"v1\" then \"v2\" at the same letterNumber/index; the stored file's Data == \"v2\" and there is exactly one file."
          },
          {
            "name": "testImageStoreDatabaseURLName",
            "intent": "Pin the sqlite filename.",
            "asserts": "ImageStore(root: tempDir()).databaseURL.lastPathComponent == \"combray.sqlite\".",
            "catchesTypo": true
          },
          {
            "name": "testImageStoreURLForPageJoinsRootAndPath",
            "intent": "url(for:) appends imagePath to root.",
            "asserts": "For root R and page.imagePath \"Letters/3/x.jpg\", url(for: page).path == R.appendingPathComponent(\"Letters/3/x.jpg\").path."
          },
          {
            "name": "testImageStoreDefaultRootLastComponentIsCombray",
            "intent": "Pin the archive folder name (existing test only substring-checks the parent path).",
            "asserts": "ImageStore.defaultRoot().lastPathComponent == \"Combray\".",
            "catchesTypo": true
          },
          {
            "name": "testStoredCredentialExpiryWindowIsSixtySeconds",
            "intent": "Pin the 60-second early-refresh window in isExpired (existing test uses a 10s margin which doesn't probe the boundary).",
            "asserts": "oauth expiresAt now+30 → isExpired true; oauth expiresAt now+120 → isExpired false; oauth expiresAt nil → isExpired false.",
            "apiNotes": "Construct StoredCredential in-memory only; never read Keychain's file.",
            "catchesTypo": true
          },
          {
            "name": "testStoredCredentialKindRawValues",
            "intent": "Pin Kind rawValues persisted in credentials.json.",
            "asserts": "StoredCredential.Kind.apiKey.rawValue == \"apiKey\" and .oauth.rawValue == \"oauth\".",
            "catchesTypo": true
          },
          {
            "name": "testStoredCredentialDecodesMinimalJSON",
            "intent": "Back-compat: a credential with only a kind decodes with nil token fields.",
            "asserts": "decode {\"kind\":\"apiKey\"} → kind == .apiKey, apiKey/accessToken/refreshToken/expiresAt all nil."
          },
          {
            "name": "testStoredCredentialApiKeyRoundTrip",
            "intent": "Existing CredentialTests only round-trips oauth; cover apiKey kind.",
            "asserts": "Encode/decode StoredCredential(kind:.apiKey, apiKey:\"k\") → back.kind==.apiKey, back.apiKey==\"k\"."
          },
          {
            "name": "testClaudeAuthConstants",
            "intent": "Pin the OAuth client id, redirect URI, scope, and loopback format (used verbatim by the auth flow).",
            "asserts": "ClaudeAuth.clientID == \"9d1c250a-e61b-44d9-88ed-5944d1962f5e\"; consoleRedirect == \"https://console.anthropic.com/oauth/code/callback\"; loopbackRedirect(port: 8080) == \"http://localhost:8080/callback\".",
            "catchesTypo": true
          },
          {
            "name": "testAuthorizeURLQueryItems",
            "intent": "Pin the authorize URL's query parameters including the exact scope string and S256 method.",
            "asserts": "Parse authorizeURL(pkce) via URLComponents; query items include code=true, client_id==clientID, response_type==code, redirect_uri==consoleRedirect, scope==\"org:create_api_key user:profile user:inference\", code_challenge==pkce.challenge, code_challenge_method==S256, state==pkce.state.",
            "apiNotes": "scope is internal static; reachable via @testable, or assert the literal directly in the query item.",
            "catchesTypo": true
          },
          {
            "name": "testAuthorizeURLHonorsCustomRedirect",
            "intent": "A custom redirectURI overrides the default in the URL.",
            "asserts": "authorizeURL(pkce, redirectURI: \"http://localhost:1234/callback\") has redirect_uri query == that value."
          },
          {
            "name": "testMakePKCEProducesUrlSafeUniqueValues",
            "intent": "PKCE values are non-empty, base64url (no +, /, =), and randomized per call.",
            "asserts": "verifier/challenge/state are non-empty and contain none of \"+\",\"/\",\"=\"; two makePKCE() calls produce different verifiers/states."
          },
          {
            "name": "testPKCEChallengeIsSha256OfVerifier",
            "intent": "challenge must be base64url(SHA256(verifier)) — the server-side PKCE check depends on it.",
            "asserts": "Recompute Data(SHA256.hash(data: Data(pkce.verifier.utf8))) base64url (replace +→-, /→_, strip =) and assert it equals pkce.challenge.",
            "apiNotes": "import CryptoKit in the test file."
          },
          {
            "name": "testBase64URLEncodingMapsSpecialChars",
            "intent": "Pin the Data.base64URLEncodedString() char mapping and padding strip (used for PKCE and OAuth).",
            "asserts": "Data([0xFB,0xFF]).base64URLEncodedString() == \"-_8\" (covers +→-, /→_, = stripped) and Data([0xFF]).base64URLEncodedString() == \"_w\".",
            "apiNotes": "The extension on Data is internal; reachable via @testable.",
            "catchesTypo": true
          }
        ]
      },
      {
        "module": "ClaudeAuth",
        "newTests": [
          {
            "name": "testClientIDIsExactConstant",
            "intent": "Pin the public OAuth client_id verbatim so a single edited character fails the build's tests.",
            "asserts": "XCTAssertEqual(ClaudeAuth.clientID, \"9d1c250a-e61b-44d9-88ed-5944d1962f5e\")",
            "apiNotes": "clientID is public. This UUID is the registered app identity; a typo silently breaks sign-in in prod but compiles fine.",
            "catchesTypo": true
          },
          {
            "name": "testAuthorizeBaseIsExactConstant",
            "intent": "Pin the authorize endpoint string exactly (host claude.ai, path /oauth/authorize).",
            "asserts": "XCTAssertEqual(ClaudeAuth.authorizeBase, \"https://claude.ai/oauth/authorize\")",
            "apiNotes": "authorizeBase is internal — reachable via @testable import CombrayCore.",
            "catchesTypo": true
          },
          {
            "name": "testTokenURLIsExactConstant",
            "intent": "Pin the token-exchange endpoint absoluteString exactly (note the /v1/ segment, host console.anthropic.com).",
            "asserts": "XCTAssertEqual(ClaudeAuth.tokenURL.absoluteString, \"https://console.anthropic.com/v1/oauth/token\")",
            "apiNotes": "tokenURL is internal (force-unwrapped URL). exchange()/refresh()/post() POST here via URLSession.shared with no injectable seam, so their happy paths and the `code#state` split inside exchange() are NOT unit-testable without hitting the real network — intentionally omitted to honor the 'never touch real credentials/network' rule. If coverage is wanted, refactor post() to accept an injected URLSession (URLProtocol stub).",
            "catchesTypo": true
          },
          {
            "name": "testConsoleRedirectIsExactConstant",
            "intent": "Pin the copy-paste callback redirect URI exactly (/oauth/code/callback, not /oauth/token).",
            "asserts": "XCTAssertEqual(ClaudeAuth.consoleRedirect, \"https://console.anthropic.com/oauth/code/callback\")",
            "apiNotes": "consoleRedirect is public and is the default redirectURI for authorizeURL/exchange; a typo here desyncs the PKCE redirect_uri and breaks token exchange.",
            "catchesTypo": true
          },
          {
            "name": "testScopeConstantAndItsThreeComponents",
            "intent": "Pin the exact scope string AND that it is the three space-separated scopes (catches a dropped/renamed scope).",
            "asserts": "XCTAssertEqual(ClaudeAuth.scope, \"org:create_api_key user:profile user:inference\"); XCTAssertEqual(ClaudeAuth.scope.split(separator: \" \").map(String.init), [\"org:create_api_key\", \"user:profile\", \"user:inference\"])",
            "apiNotes": "scope is internal. Splitting also guards against a doubled space or reordered scopes.",
            "catchesTypo": true
          },
          {
            "name": "testAuthorizeHostVsTokenHostAreNotSwapped",
            "intent": "Cross-pin the two distinct hosts so an accidental swap (authorize<->console) is caught.",
            "asserts": "XCTAssertEqual(URLComponents(string: ClaudeAuth.authorizeBase)!.host, \"claude.ai\"); XCTAssertEqual(ClaudeAuth.tokenURL.host, \"console.anthropic.com\"); XCTAssertEqual(URLComponents(string: ClaudeAuth.consoleRedirect)!.host, \"console.anthropic.com\")",
            "apiNotes": "Encodes the invariant 'authorize is on claude.ai, token+redirect are on console.anthropic.com' — the most plausible copy-paste mistake among the constants.",
            "catchesTypo": true
          },
          {
            "name": "testLoopbackRedirectFormat",
            "intent": "Pin the loopback redirect template exactly for a representative port.",
            "asserts": "XCTAssertEqual(ClaudeAuth.loopbackRedirect(port: 8080), \"http://localhost:8080/callback\")",
            "apiNotes": "loopbackRedirect is public.",
            "catchesTypo": true
          },
          {
            "name": "testLoopbackRedirectUsesHTTPNotHTTPS",
            "intent": "Lock that the loopback URL is plain http (a loopback listener can't serve TLS) and ends in /callback.",
            "asserts": "let s = ClaudeAuth.loopbackRedirect(port: 1455); XCTAssertTrue(s.hasPrefix(\"http://localhost:\")); XCTAssertFalse(s.hasPrefix(\"https://\")); XCTAssertTrue(s.hasSuffix(\"/callback\"))",
            "apiNotes": "Distinct from consoleRedirect which is https — guards against someone 'fixing' it to https.",
            "catchesTypo": true
          },
          {
            "name": "testLoopbackRedirectBoundaryPorts",
            "intent": "Exercise UInt16 boundary ports (0 and max 65535) interpolate without overflow or formatting surprises.",
            "asserts": "XCTAssertEqual(ClaudeAuth.loopbackRedirect(port: 0), \"http://localhost:0/callback\"); XCTAssertEqual(ClaudeAuth.loopbackRedirect(port: 65535), \"http://localhost:65535/callback\")",
            "apiNotes": "port is UInt16; 65535 is the type max.",
            "catchesTypo": false
          },
          {
            "name": "testMakePKCEFieldsAreNonEmptyBase64URLSafe",
            "intent": "Every PKCE field must be non-empty and contain only URL-safe base64 characters (no +, /, =, or whitespace).",
            "asserts": "let p = ClaudeAuth.makePKCE(); let safe = CharacterSet(charactersIn: \"ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789-_\"); for f in [p.verifier, p.challenge, p.state] { XCTAssertFalse(f.isEmpty); XCTAssertTrue(f.unicodeScalars.allSatisfy(safe.contains)) }",
            "apiNotes": "makePKCE/PKCE are public.",
            "catchesTypo": false
          },
          {
            "name": "testMakePKCEChallengeIsBase64URLSHA256OfVerifier",
            "intent": "Pin the core PKCE invariant: challenge == base64url(SHA256(verifier utf8)) — i.e. genuine S256, not plain.",
            "asserts": "let p = ClaudeAuth.makePKCE(); let expected = Data(SHA256.hash(data: Data(p.verifier.utf8))).base64URLEncodedString(); XCTAssertEqual(p.challenge, expected)",
            "apiNotes": "Add `import CryptoKit` to the test file. base64URLEncodedString() is the internal Data extension, reachable via @testable. This is the security-critical relationship the whole flow depends on.",
            "catchesTypo": true
          },
          {
            "name": "testMakePKCEFieldLengthsPinByteCounts",
            "intent": "Pin verifier=43 chars (32 random bytes), state=32 chars (24 bytes), challenge=43 chars (32-byte SHA256) — catches changes to the entropy byte counts.",
            "asserts": "let p = ClaudeAuth.makePKCE(); XCTAssertEqual(p.verifier.count, 43); XCTAssertEqual(p.state.count, 32); XCTAssertEqual(p.challenge.count, 43)",
            "apiNotes": "base64url length is deterministic from byte count: 32B→43, 24B→32. Changing randomBase64URL(32) to (16) would drop verifier to 22 chars and fail here.",
            "catchesTypo": true
          },
          {
            "name": "testMakePKCEIsRandomAcrossCalls",
            "intent": "Two invocations must yield different verifier, state, and (consequently) challenge — proves randomness, not a constant.",
            "asserts": "let a = ClaudeAuth.makePKCE(); let b = ClaudeAuth.makePKCE(); XCTAssertNotEqual(a.verifier, b.verifier); XCTAssertNotEqual(a.state, b.state); XCTAssertNotEqual(a.challenge, b.challenge)",
            "apiNotes": "Collision probability is ~2^-192; effectively zero flake risk.",
            "catchesTypo": false
          },
          {
            "name": "testAuthorizeURLQueryItemNamesAndOrder",
            "intent": "Pin the exact set and order of all 8 query parameter keys (catches a renamed or dropped param).",
            "asserts": "let url = ClaudeAuth.authorizeURL(ClaudeAuth.makePKCE()); let names = URLComponents(url: url, resolvingAgainstBaseURL: false)!.queryItems!.map(\\.name); XCTAssertEqual(names, [\"code\", \"client_id\", \"response_type\", \"redirect_uri\", \"scope\", \"code_challenge\", \"code_challenge_method\", \"state\"])",
            "apiNotes": "authorizeURL is public; defaults redirectURI to consoleRedirect.",
            "catchesTypo": true
          },
          {
            "name": "testAuthorizeURLFixedQueryValues",
            "intent": "Pin the literal values that never vary: code=\"true\", response_type=\"code\", code_challenge_method=\"S256\", plus client_id and scope echo the constants.",
            "asserts": "let comps = URLComponents(url: ClaudeAuth.authorizeURL(ClaudeAuth.makePKCE()), resolvingAgainstBaseURL: false)!; func v(_ n: String) -> String? { comps.queryItems!.first { $0.name == n }?.value }; XCTAssertEqual(v(\"code\"), \"true\"); XCTAssertEqual(v(\"response_type\"), \"code\"); XCTAssertEqual(v(\"code_challenge_method\"), \"S256\"); XCTAssertEqual(v(\"client_id\"), ClaudeAuth.clientID); XCTAssertEqual(v(\"scope\"), ClaudeAuth.scope)",
            "apiNotes": "queryItems values are already percent-decoded, so scope compares to the literal constant (spaces intact). 'true' is the string literal, not a bool.",
            "catchesTypo": true
          },
          {
            "name": "testAuthorizeURLBindsPKCEChallengeAndState",
            "intent": "The URL must carry THIS pkce's challenge and state (not a fresh/empty one) — wiring check.",
            "asserts": "let p = ClaudeAuth.makePKCE(); let comps = URLComponents(url: ClaudeAuth.authorizeURL(p), resolvingAgainstBaseURL: false)!; func v(_ n: String) -> String? { comps.queryItems!.first { $0.name == n }?.value }; XCTAssertEqual(v(\"code_challenge\"), p.challenge); XCTAssertEqual(v(\"state\"), p.state)",
            "apiNotes": "Guards against the challenge/verifier or challenge/state being swapped in the query construction.",
            "catchesTypo": true
          },
          {
            "name": "testAuthorizeURLDefaultRedirectIsConsole",
            "intent": "With no redirectURI argument, redirect_uri must equal consoleRedirect (the copy-paste default).",
            "asserts": "let comps = URLComponents(url: ClaudeAuth.authorizeURL(ClaudeAuth.makePKCE()), resolvingAgainstBaseURL: false)!; XCTAssertEqual(comps.queryItems!.first { $0.name == \"redirect_uri\" }?.value, ClaudeAuth.consoleRedirect)",
            "apiNotes": "Pins the default-argument value of authorizeURL.",
            "catchesTypo": true
          },
          {
            "name": "testAuthorizeURLCustomRedirectOverridesDefault",
            "intent": "An explicit redirectURI (e.g. loopback) must replace the console default in redirect_uri.",
            "asserts": "let redir = ClaudeAuth.loopbackRedirect(port: 1455); let comps = URLComponents(url: ClaudeAuth.authorizeURL(ClaudeAuth.makePKCE(), redirectURI: redir), resolvingAgainstBaseURL: false)!; XCTAssertEqual(comps.queryItems!.first { $0.name == \"redirect_uri\" }?.value, redir)",
            "apiNotes": "Covers the loopback-listener sign-in variant.",
            "catchesTypo": false
          },
          {
            "name": "testAuthorizeURLSchemeHostPath",
            "intent": "Pin scheme/host/path of the built authorize URL (https, claude.ai, /oauth/authorize) independent of the query.",
            "asserts": "let comps = URLComponents(url: ClaudeAuth.authorizeURL(ClaudeAuth.makePKCE()), resolvingAgainstBaseURL: false)!; XCTAssertEqual(comps.scheme, \"https\"); XCTAssertEqual(comps.host, \"claude.ai\"); XCTAssertEqual(comps.path, \"/oauth/authorize\")",
            "apiNotes": "Catches a regression where authorizeBase is pointed at the console host.",
            "catchesTypo": true
          },
          {
            "name": "testRandomBase64URLLengthsForByteCounts",
            "intent": "Pin deterministic output lengths across byte counts including the empty boundary: 0->0, 1->2, 2->3, 3->4, 24->32, 32->43.",
            "asserts": "XCTAssertEqual(ClaudeAuth.randomBase64URL(0).count, 0); XCTAssertEqual(ClaudeAuth.randomBase64URL(1).count, 2); XCTAssertEqual(ClaudeAuth.randomBase64URL(2).count, 3); XCTAssertEqual(ClaudeAuth.randomBase64URL(3).count, 4); XCTAssertEqual(ClaudeAuth.randomBase64URL(24).count, 32); XCTAssertEqual(ClaudeAuth.randomBase64URL(32).count, 43)",
            "apiNotes": "randomBase64URL is internal (static). Length depends only on byteCount mod 3 (padding stripped), so it's deterministic despite random bytes. byteCount 0 yields \"\".",
            "catchesTypo": false
          },
          {
            "name": "testRandomBase64URLContainsOnlySafeChars",
            "intent": "Output must never contain +, /, or = (it is URL-safe, unpadded).",
            "asserts": "let s = ClaudeAuth.randomBase64URL(48); XCTAssertFalse(s.contains(\"+\")); XCTAssertFalse(s.contains(\"/\")); XCTAssertFalse(s.contains(\"=\")); let safe = CharacterSet(charactersIn: \"ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789-_\"); XCTAssertTrue(s.unicodeScalars.allSatisfy(safe.contains))",
            "apiNotes": "48 bytes guarantees at least one byte triple likely to hit + // — but the alphabet check is the real guarantee.",
            "catchesTypo": false
          },
          {
            "name": "testRandomBase64URLIsRandomAcrossCalls",
            "intent": "Two calls with the same byte count return different strings (proves SecRandomCopyBytes is actually used).",
            "asserts": "XCTAssertNotEqual(ClaudeAuth.randomBase64URL(32), ClaudeAuth.randomBase64URL(32))",
            "apiNotes": "Negligible collision probability at 32 bytes.",
            "catchesTypo": false
          },
          {
            "name": "testBase64URLEncodingReplacesPlusAndSlash",
            "intent": "Pin that standard-base64 '+' and '/' become '-' and '_' using a byte vector that exercises both.",
            "asserts": "XCTAssertEqual(Data([0xFB, 0xFF, 0xFE]).base64URLEncodedString(), \"-__-\")",
            "apiNotes": "Verified vector: [0xFB,0xFF,0xFE] is standard-base64 \"+//+\" -> base64url \"-__-\". base64URLEncodedString() is the internal Data extension (@testable).",
            "catchesTypo": true
          },
          {
            "name": "testBase64URLEncodingStripsPadding",
            "intent": "Pin that '=' padding is removed, across the 1-pad and 2-pad cases and a normal ASCII case.",
            "asserts": "XCTAssertEqual(Data([0x00]).base64URLEncodedString(), \"AA\"); XCTAssertEqual(Data([0xFF, 0xFF]).base64URLEncodedString(), \"__8\"); XCTAssertEqual(Data(\"Ma\".utf8).base64URLEncodedString(), \"TWE\")",
            "apiNotes": "Verified vectors: [0x00]->\"AA==\"->\"AA\" (2 pad); [0xFF,0xFF]->\"//8=\"->\"__8\" (1 pad, also slash->underscore); \"Ma\"->\"TWE=\"->\"TWE\".",
            "catchesTypo": true
          },
          {
            "name": "testBase64URLEncodingEmptyData",
            "intent": "Empty Data encodes to the empty string (boundary).",
            "asserts": "XCTAssertEqual(Data().base64URLEncodedString(), \"\")",
            "apiNotes": "Guards the degenerate input.",
            "catchesTypo": false
          },
          {
            "name": "testBase64URLEncodingNeverEmitsUnsafeCharsForRandomInput",
            "intent": "Property check over random byte buffers: output is always free of +, /, = regardless of content.",
            "asserts": "for _ in 0..<200 { var bytes = [UInt8](repeating: 0, count: Int.random(in: 0...64)); for i in bytes.indices { bytes[i] = UInt8.random(in: 0...255) }; let s = Data(bytes).base64URLEncodedString(); XCTAssertFalse(s.contains(\"+\")); XCTAssertFalse(s.contains(\"/\")); XCTAssertFalse(s.contains(\"=\")) }",
            "apiNotes": "Fuzzes the three replacement rules together; cheap and deterministic in outcome (only the inputs vary).",
            "catchesTypo": false
          }
        ]
      },
      {
        "module": "CombrayCore — Keychain.swift (StoredCredential, StoredCredential.Kind, Keychain)",
        "newTests": [
          {
            "name": "testKindRawValuesArePinned",
            "intent": "Pin the exact persisted enum rawValues so a typo (e.g. 'apikey', 'apiToken', 'oAuth') that would silently corrupt every credentials.json fails the build.",
            "asserts": "XCTAssertEqual(StoredCredential.Kind.apiKey.rawValue, \"apiKey\")\nXCTAssertEqual(StoredCredential.Kind.oauth.rawValue, \"oauth\")",
            "catchesTypo": true,
            "apiNotes": "Kind: String, Codable — rawValue IS the on-disk/JSON token. Reachable via @testable; existing tests only ever use the .apiKey/.oauth case labels, never their string form."
          },
          {
            "name": "testKindDecodesFromRawStrings",
            "intent": "Confirm the wire/JSON strings 'apiKey' and 'oauth' decode to the right cases (the contract the saved file relies on).",
            "asserts": "let dec = JSONDecoder()\nXCTAssertEqual(try dec.decode(StoredCredential.self, from: Data(#\"{\"kind\":\"oauth\"}\"#.utf8)).kind, .oauth)\nXCTAssertEqual(try dec.decode(StoredCredential.self, from: Data(#\"{\"kind\":\"apiKey\"}\"#.utf8)).kind, .apiKey)",
            "catchesTypo": true,
            "apiNotes": "Other fields are optional, so a kind-only object decodes cleanly. Doubles as a rawValue-typo guard from the decode side."
          },
          {
            "name": "testUnknownKindRawValueThrows",
            "intent": "Error path: an unrecognized kind string must throw, not default silently to a case.",
            "asserts": "XCTAssertThrowsError(try JSONDecoder().decode(StoredCredential.self, from: Data(#\"{\"kind\":\"password\"}\"#.utf8)))",
            "catchesTypo": false,
            "apiNotes": "Kind has no unknown/default case, so RawRepresentable decoding throws DecodingError.dataCorrupted."
          },
          {
            "name": "testMissingKindThrows",
            "intent": "Error path: kind is the one non-optional field; a JSON object without it must throw (keyNotFound), proving the required-key contract.",
            "asserts": "XCTAssertThrowsError(try JSONDecoder().decode(StoredCredential.self, from: Data(#\"{\"apiKey\":\"k\"}\"#.utf8)))",
            "catchesTypo": false,
            "apiNotes": "All fields except kind are Optional and may be absent; kind is non-optional so its absence is a hard decode failure."
          },
          {
            "name": "testIsExpiredWithinEarlyRefreshWindow",
            "intent": "Pin the non-obvious 60-second early-refresh buffer: a token expiring in 30s is already considered expired. Brackets the -60 constant from below — would pass wrongly if the buffer were 0 or removed.",
            "asserts": "XCTAssertTrue(StoredCredential(kind: .oauth, accessToken: \"a\", expiresAt: Date().addingTimeInterval(30)).isExpired)",
            "catchesTypo": true,
            "apiNotes": "isExpired = Date() >= expiresAt.addingTimeInterval(-60). +30s lies inside the refresh window. Existing testOAuthExpiry uses +3600 (well outside) and -10 (well past), so the window itself is currently unguarded."
          },
          {
            "name": "testIsNotExpiredBeyondEarlyRefreshWindow",
            "intent": "Bracket the -60s buffer from above: a token expiring in 90s is NOT yet expired. Together with the +30s case this pins the constant to (30,90), i.e. 60.",
            "asserts": "XCTAssertFalse(StoredCredential(kind: .oauth, accessToken: \"a\", expiresAt: Date().addingTimeInterval(90)).isExpired)",
            "catchesTypo": true,
            "apiNotes": "Uses comfortable margins (30/90) around 60 so wall-clock jitter during the test can't flip the result."
          },
          {
            "name": "testOAuthNilExpiryNeverExpires",
            "intent": "Cover the guard's nil branch for an OAuth credential (existing test only checks nil-expiry on an apiKey credential).",
            "asserts": "XCTAssertFalse(StoredCredential(kind: .oauth, accessToken: \"a\").isExpired)",
            "catchesTypo": false,
            "apiNotes": "guard let e = expiresAt else { return false } — kind-agnostic; existing coverage of this branch only exercises .apiKey."
          },
          {
            "name": "testInitDefaultsAreNil",
            "intent": "Pin the memberwise-init defaults: constructing with only a kind leaves all four optionals nil (so a future signature change adding a non-nil default is caught).",
            "asserts": "let c = StoredCredential(kind: .apiKey)\nXCTAssertEqual(c.kind, .apiKey)\nXCTAssertNil(c.apiKey)\nXCTAssertNil(c.accessToken)\nXCTAssertNil(c.refreshToken)\nXCTAssertNil(c.expiresAt)",
            "catchesTypo": true,
            "apiNotes": "init(kind:apiKey:accessToken:refreshToken:expiresAt:) all default to nil except kind."
          },
          {
            "name": "testExpiresAtSurvivesCodableRoundTrip",
            "intent": "Fill a real gap: existing testCodableRoundTrip asserts kind/accessToken/refreshToken but never that expiresAt round-trips — the field the whole refresh logic depends on.",
            "asserts": "let cred = StoredCredential(kind: .oauth, accessToken: \"t\", expiresAt: Date(timeIntervalSince1970: 1000))\nlet back = try JSONDecoder().decode(StoredCredential.self, from: try JSONEncoder().encode(cred))\nXCTAssertEqual(back.expiresAt, Date(timeIntervalSince1970: 1000))",
            "catchesTypo": false,
            "apiNotes": "Default JSONEncoder/Decoder (no .iso8601 strategy) — Date encodes as timeIntervalSinceReferenceDate Double; use a fixed epoch date for exact equality."
          },
          {
            "name": "testApiKeyCredentialRoundTrip",
            "intent": "Round-trip the apiKey shape (existing test only round-trips an oauth credential): kind and apiKey preserved, token fields stay nil.",
            "asserts": "let cred = StoredCredential(kind: .apiKey, apiKey: \"sk-1\")\nlet back = try JSONDecoder().decode(StoredCredential.self, from: try JSONEncoder().encode(cred))\nXCTAssertEqual(back.kind, .apiKey)\nXCTAssertEqual(back.apiKey, \"sk-1\")\nXCTAssertNil(back.accessToken)\nXCTAssertNil(back.refreshToken)\nXCTAssertNil(back.expiresAt)",
            "catchesTypo": false
          },
          {
            "name": "testMinimalJSONDecodesWithNilOptionals",
            "intent": "Lenient/partial decode: a credentials.json with only {kind} (an older or hand-written minimal file) decodes with every optional nil rather than throwing.",
            "asserts": "let c = try JSONDecoder().decode(StoredCredential.self, from: Data(#\"{\"kind\":\"apiKey\"}\"#.utf8))\nXCTAssertEqual(c.kind, .apiKey)\nXCTAssertNil(c.apiKey)\nXCTAssertNil(c.accessToken)\nXCTAssertNil(c.refreshToken)\nXCTAssertNil(c.expiresAt)",
            "catchesTypo": false,
            "apiNotes": "Mirrors the spirit of BackupTests.testOldRecordWithoutNewFieldsStillDecodes but for the credential type — forward/backward-compat of the on-disk format."
          },
          {
            "name": "testEncodingOmitsNilFields",
            "intent": "Pin the serialized shape: an apiKey-only credential encodes to exactly {kind, apiKey} with no null token/expiry keys, so the saved credentials.json stays minimal and never persists stray nulls.",
            "asserts": "let data = try JSONEncoder().encode(StoredCredential(kind: .apiKey, apiKey: \"k\"))\nlet obj = try XCTUnwrap(JSONSerialization.jsonObject(with: data) as? [String: Any])\nXCTAssertEqual(obj[\"kind\"] as? String, \"apiKey\")\nXCTAssertEqual(obj[\"apiKey\"] as? String, \"k\")\nXCTAssertNil(obj[\"accessToken\"])\nXCTAssertNil(obj[\"refreshToken\"])\nXCTAssertNil(obj[\"expiresAt\"])",
            "catchesTypo": true,
            "apiNotes": "Swift's synthesized Codable omits nil Optionals from output. Also pins the JSON key names 'kind'/'apiKey' against renames."
          },
          {
            "name": "testEnvironmentOverrideProducesApiKeyCredential",
            "intent": "Exercise the only file-free branch of the Keychain enum: when ANTHROPIC_API_KEY is set, credential()/hasCredential() return an .apiKey credential carrying that value and short-circuit before any disk read. Also pins the exact env-var name.",
            "asserts": "let prev = getenv(\"ANTHROPIC_API_KEY\").map { String(cString: $0) }\nsetenv(\"ANTHROPIC_API_KEY\", \"sk-env\", 1)\ndefer { if let p = prev { setenv(\"ANTHROPIC_API_KEY\", p, 1) } else { unsetenv(\"ANTHROPIC_API_KEY\") } }\nlet cred = Keychain.credential()\nXCTAssertEqual(cred?.kind, .apiKey)\nXCTAssertEqual(cred?.apiKey, \"sk-env\")\nXCTAssertTrue(Keychain.hasCredential())",
            "catchesTypo": true,
            "apiNotes": "SAFE because a non-empty env value makes credential() return before touching fileURL (the real ~/Library/Application Support/Combray/credentials.json). MUST always set a non-empty value and restore the prior value in a defer; NEVER let this branch fall through to the file. The empty-env fallthrough, plus save()/setAPIKey()/clear() and the file-read branch, are intentionally NOT covered: fileURL is private and hardcoded to the real Application Support path with no injection seam, so exercising them would write/delete the user's real credential file."
          },
          {
            "name": "testApiKeyReadsFromEnvironment",
            "intent": "Cover Keychain.apiKey() over the env branch: it returns the env value because the credential's kind is .apiKey (the kind==.apiKey ? apiKey : nil filter).",
            "asserts": "let prev = getenv(\"ANTHROPIC_API_KEY\").map { String(cString: $0) }\nsetenv(\"ANTHROPIC_API_KEY\", \"sk-env\", 1)\ndefer { if let p = prev { setenv(\"ANTHROPIC_API_KEY\", p, 1) } else { unsetenv(\"ANTHROPIC_API_KEY\") } }\nXCTAssertEqual(Keychain.apiKey(), \"sk-env\")",
            "catchesTypo": true,
            "apiNotes": "Same env-var safety contract as the previous test. apiKey() returning nil for an .oauth credential cannot be tested safely (would require seeding the real file), so it's left uncovered by design."
          },
          {
            "name": "testIsExpiredIgnoresKind",
            "intent": "Edge case: isExpired keys solely off expiresAt and ignores kind — an apiKey credential with a past expiresAt is reported expired, documenting that the buffer logic is kind-agnostic.",
            "asserts": "XCTAssertTrue(StoredCredential(kind: .apiKey, apiKey: \"k\", expiresAt: Date(timeIntervalSince1970: 0)).isExpired)",
            "catchesTypo": false,
            "apiNotes": "The 1970 epoch date is far in the past, so even with the -60s buffer it is unambiguously expired regardless of test wall-clock."
          }
        ]
      }
    ],
    "audit": [
      {
        "findings": [
          {
            "kind": "dead code",
            "location": "/Users/labern/Combray/Sources/CombrayCore/Archive.swift:67-73",
            "detail": "public func letters(forYear year: Int) has zero callers anywhere in Sources/Combray or Tests (the UI groups by year via years() + allLetters() instead). Verified by grep: the only occurrence of 'forYear' is this definition. Unused public API.",
            "severity": "medium"
          },
          {
            "kind": "dead code",
            "location": "/Users/labern/Combray/Sources/CombrayCore/Keychain.swift:71-74",
            "detail": "public static func apiKey() -> String? has zero callers. Callers everywhere use credential()/hasCredential()/authHeaders() instead. Verified by grep across Sources and Tests. Unused public API.",
            "severity": "medium"
          },
          {
            "kind": "dead code",
            "location": "/Users/labern/Combray/Sources/CombrayCore/Models.swift:58-73 and /Users/labern/Combray/Sources/CombrayCore/AppDatabase.swift:40-46",
            "detail": "The entire Relationship feature is unused. The struct Relationship is never instantiated (grep for 'Relationship(' returns nothing; the only non-meta references are its own definition/extension), and the 'relationship' table created in the v1 migration is never inserted into or queried. The UI's 'relationship' is the unrelated Letter.metaRelationship field. Dead model + dead schema table.",
            "severity": "medium"
          },
          {
            "kind": "logic inconsistency",
            "location": "/Users/labern/Combray/Sources/CombrayCore/Keychain.swift:28-31 (doc) vs 33-39 (fileURL)",
            "detail": "The doc comment claims the credential is 'Kept out of the Finder-browsable archive so a backup of ~/Documents/Combray never contains the token.' But fileURL resolves to <AppSupport>/Combray/credentials.json, and ImageStore.defaultRoot() is that exact same <AppSupport>/Combray directory (with combray.sqlite and Letters/ as siblings). After the move to Application Support, credentials.json now lives INSIDE the archive root the app treats as source-of-truth, directly contradicting the comment's premise.",
            "severity": "medium"
          },
          {
            "kind": "DRY",
            "location": "/Users/labern/Combray/Sources/CombrayCore/Keychain.swift:33-39 vs /Users/labern/Combray/Sources/CombrayCore/ImageStore.swift:17-21",
            "detail": "Keychain.fileURL independently recomputes the archive base directory — FileManager.default.urls(for: .applicationSupportDirectory, in: .userDomainMask).first! + appendingPathComponent(\"Combray\", isDirectory: true) — which is exactly ImageStore.defaultRoot(). The Application-Support/'Combray' root is defined in two places; if defaultRoot() ever changes, the credential location silently won't follow. Keychain should derive from ImageStore.defaultRoot().",
            "severity": "medium"
          },
          {
            "kind": "dead code",
            "location": "/Users/labern/Combray/Sources/CombrayCore/Models.swift:37,38,94,164 and corresponding columns in AppDatabase.swift:36,37,59,77",
            "detail": "Speculative model fields that are never written and never read in production: Person.aka (Models.swift:37 / column AppDatabase.swift:36), Person.notes (Models.swift:38 / column :37), Letter.notes (Models.swift:94 / column :59), Page.thumbnailPath (Models.swift:164 / column :77). Grep finds no producer or consumer outside the struct definitions/initializers. (Related: Page.width/height at Models.swift:165-166 are write-only — set in ImageStore.importImage but never read.)",
            "severity": "low"
          },
          {
            "kind": "dead code",
            "location": "/Users/labern/Combray/Sources/CombrayCore/AnthropicClient.swift:56-57 (and consumer at Archive.swift:243-266)",
            "detail": "TranscriptionResult.people_mentioned and TranscriptionResult.uncertain_spans are requested in the JSON schema, returned by the model, and decoded, but applyTranscription never persists or otherwise consumes them (only notable_quotes, meta, date, sender, recipients, title, summary, transcription are used). Outside the decoder they appear only in tests. The extracted 'people mentioned' and uncertain-span data is silently discarded.",
            "severity": "low"
          },
          {
            "kind": "logic inconsistency",
            "location": "/Users/labern/Combray/Sources/CombrayCore/Backup.swift:7 and /Users/labern/Combray/Sources/CombrayCore/ImageStore.swift:42",
            "detail": "Stale path documentation after the migration to Application Support. Backup.swift:7 states 'Layout: ~/Documents/Combray/Letters/<number>/' and ImageStore.swift:42 gives the example '~/Documents/Combray/Letters/3/letter_3_page_1.jpg', yet ImageStore.defaultRoot()/migrateFromDocumentsIfNeeded() deliberately moved the archive OUT of ~/Documents into Application Support (the whole reason for the move, per the same file's own comment). The comments now point at the wrong, abandoned location.",
            "severity": "low"
          },
          {
            "kind": "DRY",
            "location": "/Users/labern/Combray/Sources/CombrayCore/Archive.swift:197-207, 289-295, 407-411",
            "detail": "The same person↔letterPerson join (JOIN letterPerson ON letterPerson.personId = person.id WHERE letterPerson.letterId = ?) is hand-written three times — in participants(forLetterId:), correspondents(ofPersonId:), and refreshSearchIndex's names query. Minor duplicated raw SQL.",
            "severity": "low"
          },
          {
            "kind": "logic inconsistency",
            "location": "/Users/labern/Combray/Sources/CombrayCore/Archive.swift:260 (store) and :323 (LetterFile read)",
            "detail": "Notable quotes are stored as a single newline-joined string (r.notable_quotes.joined(separator: \"\\n\")) and reconstructed by splitting on \"\\n\" (backupFile: letter.notableQuotes?.split(separator: \"\\n\")). A notable_quote that itself contains a newline — plausible for a multi-line verbatim excerpt — round-trips as multiple separate quotes. The separator collides with legal field content.",
            "severity": "low"
          },
          {
            "kind": "typo",
            "location": "/Users/labern/Combray/Sources/CombrayCore (all files)",
            "detail": "No genuine user-facing or identifier typos found. Schema property keys, CodingKeys, and decode keys all match exactly (transcription/title/document_type/summary/sender/recipients/date/people_mentioned/notable_quotes/uncertain_spans/meta, plus date.{value,source,confidence}, meta.{location,relationship,relationship_state,writer_goals}); error strings and instruction prose checked clean. Reported for completeness — no action needed.",
            "severity": "low"
          }
        ]
      },
      {
        "findings": [
          {
            "kind": "dead code",
            "location": "/Users/labern/Combray/Sources/Combray/ArchiveController.swift:408",
            "detail": "Second `deleteLetter(_ id: String)` overload is never called anywhere. The actually-used delete path is `deleteLetter(_ letter: Letter)` at line 359 (called from RowMenu.swift:75), which additionally trashes the source folder and calls goHome(). This id-only version only does archive.deleteLetter + select(nil) + reload — a leftover earlier version that no longer matches behaviour. Safe to remove.",
            "severity": "low"
          },
          {
            "kind": "dead code",
            "location": "/Users/labern/Combray/Sources/Combray/LetterDetail.swift:294",
            "detail": "`private func labeled(_ title:_ value:) -> some View` is defined but never referenced (grep: only the definition exists). The detail pane builds its labelled fields via metaField/MetaPanel instead. Dead.",
            "severity": "low"
          },
          {
            "kind": "dead code",
            "location": "/Users/labern/Combray/Sources/Combray/LetterDetail.swift:301",
            "detail": "`private var titleBinding: Binding<String>` is defined but never used; the title field now drives off the `$titleText` @State binding (line 163). Dead.",
            "severity": "low"
          },
          {
            "kind": "dead code",
            "location": "/Users/labern/Combray/Sources/Combray/Sheets.swift:121",
            "detail": "`private extension String { func ifEmpty(_ fallback:) }` is never called (grep: only the definition). Dead.",
            "severity": "low"
          },
          {
            "kind": "dead code",
            "location": "/Users/labern/Combray/Sources/Combray/QuoteBar.swift:82",
            "detail": "`struct SpeechBubble: Shape` is never instantiated. The QuoteBar renders the Proust line as plain centered Text in a ZStack — no speech bubble is drawn anywhere. ~14 lines of dead shape code.",
            "severity": "low"
          },
          {
            "kind": "dead code",
            "location": "/Users/labern/Combray/Sources/Combray/Theme.swift:196",
            "detail": "`ProustMark` (lines 196-255) and its only consumer `ProustAvatar` (lines 258-267) are never referenced outside Theme.swift (grep confirms no call sites). That's ~70 lines of unused Canvas portrait-drawing code. The app's hero/icon mark is MadeleineMark, which is used; the Proust drawing is orphaned.",
            "severity": "low"
          },
          {
            "kind": "DRY",
            "location": "/Users/labern/Combray/Sources/Combray/ArchiveController.swift:221 and :501",
            "detail": "`importLetter(from:)` (221-236) and `importFromCapture(batch:urls:)` (501-519) duplicate the entire new-letter pipeline almost line-for-line: new Letter(), number = nextLetterNumber() ?? 1, save, enumerate urls -> images.importImage, archive.setPages, reload/select/backup, then auto-transcribe. `runCaptureServerHeadless`'s onLetter (612-622) is a third copy of the same sequence. Extract a single `private func makeLetter(from urls: [URL]) throws -> Letter` and have the two controller paths share it (capture path adds only its status reporting).",
            "severity": "medium"
          },
          {
            "kind": "DRY",
            "location": "/Users/labern/Combray/Sources/Combray/ArchiveController.swift:551 and :575",
            "detail": "`exportDOCX` (551-554) and `shareViaGmail` (575-579) each build the same From/To/Date header list with identical logic (`if let sender { append(\"From: …\") }`, recipients joined by \", \", `if let date { append(\"Date: …\") }`). One shared helper returning the [String] header lines would remove the duplication.",
            "severity": "low"
          },
          {
            "kind": "DRY",
            "location": "/Users/labern/Combray/Sources/Combray/LetterDetail.swift:284 and ArchiveController.swift:346",
            "detail": "Clipboard copy is implemented twice: `ArchiveController.copyTranscription(_:)` (346-350, used by RowMenu) and `LetterDetailView.copyTranscript()` (284-292) both do NSPasteboard.general.clearContents() + setString(letter.transcription, .string). The view could call `c.copyTranscription(letter)` and keep only its `copied` flash, instead of re-implementing the pasteboard write.",
            "severity": "low"
          },
          {
            "kind": "DRY",
            "location": "/Users/labern/Combray/Sources/Combray/CaptureServer.swift:180 and WebServer.swift:232",
            "detail": "The madeleine logo SVG `<path d=\"M50 91 …\">` path data is byte-identical in the capture page and the web-viewer header. This is duplicated markup (not the capture-vs-viewer CSS that is intentionally different), so it's a genuine copy that will drift if the mark is ever tweaked. Could live in one shared string constant. Flagging the path duplication only — not the surrounding CSS.",
            "severity": "low"
          },
          {
            "kind": "typo",
            "location": "/Users/labern/Combray/Sources/Combray/QuoteBar.swift:29",
            "detail": "Double space in a user-facing string: `…\\u{201D}  — Proust, In Search of Lost Time` has two spaces between the closing quote and the em-dash (verified in bytes). Renders as a visible double gap before the attribution. Should be a single space.",
            "severity": "low"
          },
          {
            "kind": "typo",
            "location": "/Users/labern/Combray/Sources/Combray/LetterDetail.swift:318",
            "detail": "Typographic inconsistency in a label: the native MetaPanel uses `\"Writer's goals\"` with a straight ASCII apostrophe (0x27), whereas the rest of the UI uses curly apostrophes (e.g. \"isn't\", \"it's\") and the web viewer's matching label at WebServer.swift:160 uses the curly `Writer's goals` (’ / U+2019). Make the native label use the curly apostrophe for consistency.",
            "severity": "low"
          },
          {
            "kind": "typo",
            "location": "/Users/labern/Combray/Sources/Combray/Views.swift:71",
            "detail": "The WhatsApp pre-filled message text is `\"Combray question -- \"` — a double hyphen, where the app's house style is the em-dash (—) used in every other user-facing string. Minor inconsistency in text the user will see in their WhatsApp compose field.",
            "severity": "low"
          }
        ]
      }
    ]
  },
  "workflowProgress": [
    {
      "type": "workflow_phase",
      "index": 1,
      "title": "Design tests"
    },
    {
      "type": "workflow_phase",
      "index": 2,
      "title": "Audit"
    },
    {
      "type": "workflow_agent",
      "index": 1,
      "label": "tests:Archive.swift",
      "phaseIndex": 1,
      "phaseTitle": "Design tests",
      "agentId": "afcfdec49ee31f88e",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1782609614634,
      "queuedAt": 1782609614619,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CombrayCore",
      "promptPreview": "You are designing XCTest cases for the CombrayCore Swift library of the Combray app (a macOS handwritten-letter archive).\n\nREAD both:\n- /Users/labern/Combray/Sources/CombrayCore/Archive.swift   (the module under test)\n- /Users/labern/Combray/Tests/CombrayCoreTests/CombrayCoreTests.swift   (the EXISTING tests — do NOT duplicate these; only propose what is MISSING)\n\nHow tests in this suite are writt…",
      "lastProgressAt": 1782610207666,
      "tokens": 50936,
      "toolCalls": 12,
      "durationMs": 593032,
      "resultPreview": "{\"module\":\"CombrayCore\",\"newTests\":[{\"name\":\"testSaveBumpsUpdatedAtButPreservesCreatedAt\",\"intent\":\"save() must set updatedAt to ~now while leaving createdAt untouched (it only mutates updatedAt).\",\"asserts\":\"Save Letter(createdAt: Date(timeIntervalSince1970:0), updatedAt: same). Returned.updatedAt is recent (> 1970, within seconds of now); returned.createdAt == epoch 0. Re-fetch confirms persiste…"
    },
    {
      "type": "workflow_agent",
      "index": 2,
      "label": "tests:AnthropicClient.swift",
      "phaseIndex": 1,
      "phaseTitle": "Design tests",
      "agentId": "a6d74afe6bf24d377",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1782609614635,
      "queuedAt": 1782609614619,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CombrayCore (Sources/CombrayCore/AnthropicClient.swift)",
      "promptPreview": "You are designing XCTest cases for the CombrayCore Swift library of the Combray app (a macOS handwritten-letter archive).\n\nREAD both:\n- /Users/labern/Combray/Sources/CombrayCore/AnthropicClient.swift   (the module under test)\n- /Users/labern/Combray/Tests/CombrayCoreTests/CombrayCoreTests.swift   (the EXISTING tests — do NOT duplicate these; only propose what is MISSING)\n\nHow tests in this suite a…",
      "lastProgressAt": 1782609856513,
      "tokens": 30588,
      "toolCalls": 3,
      "durationMs": 241878,
      "resultPreview": "{\"module\":\"CombrayCore (Sources/CombrayCore/AnthropicClient.swift)\",\"newTests\":[{\"name\":\"testDecodesEmptyObjectToAllDefaults\",\"intent\":\"An empty JSON object {} must decode (not throw) with every field at its documented default, including the non-obvious date.source default 'unknown'.\",\"asserts\":\"let r = decode(\\\"{}\\\"); XCTAssertEqual(r.transcription, \\\"\\\"); XCTAssertEqual(r.title, \\\"\\\"); XCTAssert…"
    },
    {
      "type": "workflow_agent",
      "index": 3,
      "label": "tests:Backup.swift",
      "phaseIndex": 1,
      "phaseTitle": "Design tests",
      "agentId": "a13974e3668aad0fd",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1782609614635,
      "queuedAt": 1782609614619,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "/Users/labern/Combray/Sources/CombrayCore/Backup.swift",
      "promptPreview": "You are designing XCTest cases for the CombrayCore Swift library of the Combray app (a macOS handwritten-letter archive).\n\nREAD both:\n- /Users/labern/Combray/Sources/CombrayCore/Backup.swift   (the module under test)\n- /Users/labern/Combray/Tests/CombrayCoreTests/CombrayCoreTests.swift   (the EXISTING tests — do NOT duplicate these; only propose what is MISSING)\n\nHow tests in this suite are writte…",
      "lastProgressAt": 1782609812763,
      "tokens": 25674,
      "toolCalls": 3,
      "durationMs": 198128,
      "resultPreview": "{\"module\":\"/Users/labern/Combray/Sources/CombrayCore/Backup.swift\",\"newTests\":[{\"name\":\"testTranscriptionTxtJoinsTitleAndBodyWithBlankLine\",\"intent\":\"Pin the exact transcription.txt content when a title is present: [title, transcription].joined(separator: \\\"\\\\n\\\\n\\\").\",\"asserts\":\"Write LetterFile(title: \\\"Hi\\\", transcription: \\\"Body\\\") to tempDir(); read String(contentsOf: dir/\\\"7\\\"/\\\"transcriptio…"
    },
    {
      "type": "workflow_agent",
      "index": 4,
      "label": "tests:ImageStore.swift",
      "phaseIndex": 1,
      "phaseTitle": "Design tests",
      "agentId": "a31acf8b9c92314d6",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1782609614635,
      "queuedAt": 1782609614619,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CombrayCore (focus: ImageStore.swift; plus cross-module con…",
      "promptPreview": "You are designing XCTest cases for the CombrayCore Swift library of the Combray app (a macOS handwritten-letter archive).\n\nREAD both:\n- /Users/labern/Combray/Sources/CombrayCore/ImageStore.swift   (the module under test)\n- /Users/labern/Combray/Tests/CombrayCoreTests/CombrayCoreTests.swift   (the EXISTING tests — do NOT duplicate these; only propose what is MISSING)\n\nHow tests in this suite are wr…",
      "lastProgressAt": 1782610012607,
      "tokens": 25841,
      "toolCalls": 3,
      "durationMs": 397972,
      "resultPreview": "{\"module\":\"CombrayCore (focus: ImageStore.swift; plus cross-module constant/enum/decoding pins)\",\"newTests\":[{\"name\":\"testDatabaseURLExactFilename\",\"intent\":\"Pin the exact sqlite filename constant so a typo (e.g. Combray.sqlite, combray.db) breaks the build.\",\"asserts\":\"ImageStore(root: tempDir()).databaseURL.lastPathComponent == \\\"combray.sqlite\\\" (exact lowercase).\",\"apiNotes\":\"databaseURL is fu…"
    },
    {
      "type": "workflow_agent",
      "index": 5,
      "label": "tests:Models.swift",
      "phaseIndex": 1,
      "phaseTitle": "Design tests",
      "agentId": "a1351d7d46bc35304",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1782609614635,
      "queuedAt": 1782609614619,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CombrayCore",
      "promptPreview": "You are designing XCTest cases for the CombrayCore Swift library of the Combray app (a macOS handwritten-letter archive).\n\nREAD both:\n- /Users/labern/Combray/Sources/CombrayCore/Models.swift   (the module under test)\n- /Users/labern/Combray/Tests/CombrayCoreTests/CombrayCoreTests.swift   (the EXISTING tests — do NOT duplicate these; only propose what is MISSING)\n\nHow tests in this suite are writte…",
      "lastProgressAt": 1782610047411,
      "tokens": 51332,
      "toolCalls": 12,
      "durationMs": 432776,
      "resultPreview": "{\"module\":\"CombrayCore\",\"newTests\":[{\"name\":\"testDateSourceRawValuesAndCaseOrder\",\"intent\":\"Pin DateSource enum rawValues and CaseIterable order so a typo in any case name fails.\",\"asserts\":\"XCTAssertEqual(DateSource.allCases.map(\\\\.rawValue), [\\\"written\\\",\\\"inferred\\\",\\\"manual\\\",\\\"unknown\\\"]); XCTAssertEqual(DateSource.allCases.count, 4).\",\"catchesTypo\":true,\"apiNotes\":\"DateSource is public Strin…"
    },
    {
      "type": "workflow_agent",
      "index": 6,
      "label": "tests:AppDatabase.swift",
      "phaseIndex": 1,
      "phaseTitle": "Design tests",
      "agentId": "a0955e6b266f12d45",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1782609614635,
      "queuedAt": 1782609614619,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CombrayCore",
      "promptPreview": "You are designing XCTest cases for the CombrayCore Swift library of the Combray app (a macOS handwritten-letter archive).\n\nREAD both:\n- /Users/labern/Combray/Sources/CombrayCore/AppDatabase.swift   (the module under test)\n- /Users/labern/Combray/Tests/CombrayCoreTests/CombrayCoreTests.swift   (the EXISTING tests — do NOT duplicate these; only propose what is MISSING)\n\nHow tests in this suite are w…",
      "lastProgressAt": 1782610033518,
      "tokens": 51502,
      "toolCalls": 12,
      "durationMs": 418882,
      "resultPreview": "{\"module\":\"CombrayCore\",\"newTests\":[{\"name\":\"testSchemaVersionIsExactlyOne\",\"intent\":\"Existing test only checks >0; pin the exact constant so a bump or typo is caught.\",\"asserts\":\"XCTAssertEqual(Combray.schemaVersion, 1)\",\"catchesTypo\":true},{\"name\":\"testMigrationCreatesAllTables\",\"intent\":\"Pin the migrated table names at the DB level so renaming a table in AppDatabase's migration fails a test.\",\"…"
    },
    {
      "type": "workflow_agent",
      "index": 7,
      "label": "tests:ClaudeAuth.swift",
      "phaseIndex": 1,
      "phaseTitle": "Design tests",
      "agentId": "a5def103ae0b4d382",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1782609614635,
      "queuedAt": 1782609614619,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "ClaudeAuth",
      "promptPreview": "You are designing XCTest cases for the CombrayCore Swift library of the Combray app (a macOS handwritten-letter archive).\n\nREAD both:\n- /Users/labern/Combray/Sources/CombrayCore/ClaudeAuth.swift   (the module under test)\n- /Users/labern/Combray/Tests/CombrayCoreTests/CombrayCoreTests.swift   (the EXISTING tests — do NOT duplicate these; only propose what is MISSING)\n\nHow tests in this suite are wr…",
      "lastProgressAt": 1782609819177,
      "tokens": 37561,
      "toolCalls": 4,
      "durationMs": 204542,
      "resultPreview": "{\"module\":\"ClaudeAuth\",\"newTests\":[{\"name\":\"testClientIDIsExactConstant\",\"intent\":\"Pin the public OAuth client_id verbatim so a single edited character fails the build's tests.\",\"asserts\":\"XCTAssertEqual(ClaudeAuth.clientID, \\\"9d1c250a-e61b-44d9-88ed-5944d1962f5e\\\")\",\"apiNotes\":\"clientID is public. This UUID is the registered app identity; a typo silently breaks sign-in in prod but compiles fine.\"…"
    },
    {
      "type": "workflow_agent",
      "index": 8,
      "label": "tests:Keychain.swift",
      "phaseIndex": 1,
      "phaseTitle": "Design tests",
      "agentId": "aedbb86b67f3a660c",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1782609614636,
      "queuedAt": 1782609614619,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CombrayCore — Keychain.swift (StoredCredential, StoredCrede…",
      "promptPreview": "You are designing XCTest cases for the CombrayCore Swift library of the Combray app (a macOS handwritten-letter archive).\n\nREAD both:\n- /Users/labern/Combray/Sources/CombrayCore/Keychain.swift   (the module under test)\n- /Users/labern/Combray/Tests/CombrayCoreTests/CombrayCoreTests.swift   (the EXISTING tests — do NOT duplicate these; only propose what is MISSING)\n\nHow tests in this suite are writ…",
      "lastProgressAt": 1782609798240,
      "tokens": 25649,
      "toolCalls": 3,
      "durationMs": 183604,
      "resultPreview": "{\"module\":\"CombrayCore — Keychain.swift (StoredCredential, StoredCredential.Kind, Keychain)\",\"newTests\":[{\"name\":\"testKindRawValuesArePinned\",\"intent\":\"Pin the exact persisted enum rawValues so a typo (e.g. 'apikey', 'apiToken', 'oAuth') that would silently corrupt every credentials.json fails the build.\",\"asserts\":\"XCTAssertEqual(StoredCredential.Kind.apiKey.rawValue, \\\"apiKey\\\")\\nXCTAssertEqual(…"
    },
    {
      "type": "workflow_agent",
      "index": 9,
      "label": "audit:core",
      "phaseIndex": 2,
      "phaseTitle": "Audit",
      "agentId": "a5aa11a830fc28c35",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1782610207690,
      "queuedAt": 1782610207675,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "promptPreview": "Read every file in /Users/labern/Combray/Sources/CombrayCore/. Find: dead code (unused symbols), genuine DRY violations, logic inconsistencies, and user-facing or identifier TYPOS. Be precise with file:location. Do NOT propose splitting files. Return findings with severity (high/medium/low).",
      "lastProgressAt": 1782610513249,
      "tokens": 67691,
      "toolCalls": 22,
      "durationMs": 305559,
      "resultPreview": "{\"findings\":[{\"kind\":\"dead code\",\"location\":\"/Users/labern/Combray/Sources/CombrayCore/Archive.swift:67-73\",\"detail\":\"public func letters(forYear year: Int) has zero callers anywhere in Sources/Combray or Tests (the UI groups by year via years() + allLetters() instead). Verified by grep: the only occurrence of 'forYear' is this definition. Unused public API.\",\"severity\":\"medium\"},{\"kind\":\"dead cod…"
    },
    {
      "type": "workflow_agent",
      "index": 10,
      "label": "audit:app",
      "phaseIndex": 2,
      "phaseTitle": "Audit",
      "agentId": "ae41554377708d943",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1782610207691,
      "queuedAt": 1782610207675,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "promptPreview": "Read every file in /Users/labern/Combray/Sources/Combray/ (the app target). Find: dead code, genuine DRY violations, and especially user-facing TYPOS in strings shown to the user (SwiftUI labels, the served web HTML, the transcription prompt). Do NOT propose splitting files; do NOT flag the LocalHTTP server dedup or the deliberate capture-page vs viewer CSS differences. Return findings with severi…",
      "lastProgressAt": 1782610500652,
      "tokens": 98712,
      "toolCalls": 29,
      "durationMs": 292961,
      "resultPreview": "{\"findings\":[{\"kind\":\"dead code\",\"location\":\"/Users/labern/Combray/Sources/Combray/ArchiveController.swift:408\",\"detail\":\"Second `deleteLetter(_ id: String)` overload is never called anywhere. The actually-used delete path is `deleteLetter(_ letter: Letter)` at line 359 (called from RowMenu.swift:75), which additionally trashes the source folder and calls goHome(). This id-only version only does a…"
    }
  ],
  "totalTokens": 465486,
  "totalToolCalls": 103
}